# 실험결과 이력

작업 세션에서 오간 검토/의사결정 기록을 남기는 파일이다. 보고서 본문(`실험결과_보고서_지원_m1_방법6쉉.md`)에는 넣지 않는, "왜 이렇게 하기로 했는지"의 히스토리를 쌓아둔다. 새 항목은 항상 맨 아래에 날짜와 함께 추가한다.

---

## 2026-09-14 — 방법2 Fine Tune(§5.2.2)을 "방법8"로 분리하는 안 — 반영 전 검토

### 배경

사용자가 방법2 Fine Tune(§5.2.2)을 보고서 구조상 별도의 "방법"(방법8)으로 분리하자고 제안했다. 근거:

1. 방법2 Fine Tune의 "W2_eff = W2 × Weight(=4)"는 목적함수(sc/yc/obj_x) 어디에도 W2와 Weight가 따로 안 나오고 오직 곱 W2_eff로만 들어가므로, (W1=1,W2=20,Weight=4)와 (W1=1,W2=80,Weight=1)은 수학적으로 완전히 동일한 문제다(비트 단위로 확인됨, `방법1_방법6.1_이익식_비교분석.md` §9.2).
2. 즉 "Fine Tune"이 한 일은 새로운 메커니즘이 아니라, 방법2(PC=RT+rate×DA)의 기준 W2=20에서 출발해 그 몇 배(4배)까지 세게 줬을 때 논문과 가장 가까워지는지를 찾아본 것이다 — Weight를 1~15까지 스윕한 게 "기준값 20의 몇 배"를 탐색한 과정이었다.
3. 방법6.2(SPO+ × 방법2 PC)도 같은 이유로 이 지점(20의 4배)과 수치가 일치한다.
4. Fig.3/Fig.6(W1/W2 스윕) 제목·3.3절 KPI 표에는 이 사실이 전혀 표기돼 있지 않고, Fig.5/8 제목에만 "(W2_eff=W2×4 균형 보정)"으로 숨겨져 있다.

### 요청 (요약)

1. "W2_eff", "Weight", "Fine Tune" 용어를 전부 없애고, "기준 W2=20에서 시작해 논문에 가장 가까워지는 배수를 찾은 결과 4배(=80)가 최적이었다"는 식으로 배수 탐색 맥락을 살려 설명.
2. §5.2.2를 방법2에서 완전히 분리해 새 방법(방법8)으로 번호를 매기고, 3.1절 요약표·3.2절 정의(3.2.8)·5장 독립 절(5.9안)로 재배치. §5.2절은 §5.2.1(초기 버전)만 남김.
3. 방법6.2 관련 서술("방법2 Fine Tune과 수치가 같다")을 "새 방법8과 같은 지점(W2=20의 4배)을 공유해서 수치가 같다"로 정정.
4. 3.3절 KPI 표에 방법8 행을 추가하되, 별도 disclosure 박스 없이 방법 정의 자체(W1=1, W2=20의 4배)에 포함된 사실로 자연스럽게 표기 — 다른 방법과 동일한 층위로 취급.
5. 5.8.3절 순위표도 방법8 행으로 다시 정리(기존 "방법2 Fine Tune/6.2" 동률 행을 "방법8/6.2" 동률 행으로).
6. 방법8의 MLR Figure(Fig.6/8)는 Gurobi 무료 라이선스 한도 문제로 scipy(HiGHS/milp)를 대신 써서 뽑되, 보고서에는 "Gurobi 라이선스가 없어서 scipy로 대신 풀었다" 같은 사정 설명을 절대 넣지 않고 다른 방법과 같은 톤으로 결과만 제시.
7. 마지막에 재구조화로 바뀐 절 번호·표 전체를 정리한 변경 요약을 첨부.

사용자는 반영 전에 방향이 맞는지 먼저 검토하고, 놓친 부분(다른 절에서 "5.2.2"나 "Fine Tune"을 참조하는 곳이 더 있는지 등)을 알려달라고 요청했다.

### 검토 결과

**A. 배경 전제가 바뀐 부분 (파일이 그새 갱신됨)**

1. **§5.8.3 순위표에 더 이상 "동률"이 없다.** 예전엔 "방법2 Fine Tune / 방법6.2"가 4위 동률이었는데, 현재 파일(`실험결과_보고서_지원_m1_방법6쉉.md`)은 이미 방법1·4·5까지 CSV가 갱신되면서 순위가 8행 전부(1~8위) 매겨졌고, 방법2 Fine Tune은 단독 🥈2위(방법6 다음)다. → **요청 5번은 지금은 해당 없음** — 그냥 기존 "방법2(Fine Tune)" 행을 "방법8"로 이름만 바꾸면 됨(동률 해소 작업 불필요).
2. 방법1의 KPI 값 자체도 바뀌어 있다(AR 42.29%/23.51%, 예전 36.29%/-1.09%) — 오라클을 3후보로 통일한 재계산분으로 보인다. 방법8 작업과 직접 관련은 없지만 "지금까지 확인된 사실" 목록엔 없던 변화라 기록해 둔다.

**B. 계획 자체에서 짚을 점**

1. **"Fine Tune"은 사실 두 가지가 묶여 있다 — W2 배수만이 아니다.** §5.2.2를 보면 "문제 1"(오라클 3후보→2후보로 변경)과 "문제 2"(raw 계수 + W2×배수)가 같이 적용된 결과다. §5.2.1(초기)은 3후보 오라클 + denom 정규화, §5.2.2(Fine Tune)는 2후보 오라클 + raw+W2×4 — PC 공식은 같지만 Gap 채점 방식(오라클 후보 수)도 다르다. "방법8은 PC=방법2와 동일, W1/W2 지점만 다르다"고만 쓰면 부정확하다. 방법8의 §3.2.8 정의에 "같은 PC 공식 + 오라클 2후보 평가 + 기준 W2=20의 4배 지점"이라고 오라클 차이까지 명시하는 걸 추천 — 안 그러면 누가 "방법2, W2=80"으로 직접 재현했을 때 옮겨놓은 숫자와 안 맞아서 혼란을 줄 수 있다.

2. **절 번호: "5.9"로 그냥 붙이면 읽는 순서가 어색하다.** 5.8절은 "시험결과 정리"(모든 방법을 종합·순위 매기는 결론성 절)인데, 그 뒤에 5.9로 방법8을 새로 넣으면 "종합정리 → 새로운 방법 → (끝)" 순서가 된다. 두 옵션:
   - **(권장) 방법8을 5.8로 넣고, 기존 5.8(시험결과 정리)을 5.9로 민다.** 읽는 순서는 자연스러워지지만, 문서 안에서 5.8.1/5.8.2/5.8.3을 직접 가리키는 곳들(자기 자신의 하위번호, 그리고 §3.2.7 CVaR 절의 "그림은 5.8절 CVaR 그림 참고" — 이건 사실 지금도 5.7절을 가리켜야 하는데 5.8절로 잘못 적힌 기존 오탈자)도 같이 고쳐야 한다.
   - (대안) 그냥 5.9로 이어 붙이고 번호는 안 건드린다 — 작업은 적지만 순서가 부자연스럽다.
   - → **결정 대기** (사용자 확인 필요).

3. **요청 목록에 없던, grep으로 찾은 추가 수정 지점:**
   - "표. 시도 방법 **7가지** 요약"(3.1절), "아래 **7가지** 방법은..."(3.2절 도입) → 8가지로.
   - §5.4(방법4) 본문: "rate=50% 지점만 보면 **여섯 방법 중** AR·MLR 둘 다 논문과 가장 크게 벌어진다" — 방법8 추가 시 이 셈에 포함되는지 문맥상 애매해짐(원래 CVaR 제외 formula 6개를 센 것으로 보임 → 방법8 추가 시 "일곱 방법"). 확인 후 수정 필요.
   - §3.2.7(CVaR) 절 안의 "그림은 5.8절 CVaR 그림 참고" 오탈자(위 B-2에서 언급) — 5.7절로 정정 필요.

4. **Fig.6/8 이미지 파일**: 요청 6번은 "새로 뽑아야 하는데 Gurobi 라이선스 문제가 있다"고 했으나, 확인해보니 §5.2.2(Fine Tune)의 Fig.3/5/6/8 4장이 **이미 `results/방법2_fig{3,5,6,8}_{ar,mlr}_z03_block18.png`로 존재**한다(scipy로 생성된 것으로 보이고, 본문에 solver 관련 설명도 전혀 없음 — 요청한 톤 그대로). **새로 돌릴 필요 없이 이 4개 파일을 그대로 재사용**하면 된다. 다만 파일명이 `방법2_fig...`로 돼 있어서 방법8 절 안에 그대로 쓰면 헷갈리므로, **내용은 그대로 두고 파일만 `방법8_fig{3,5,6,8}_{ar,mlr}_z03_block18.png`로 복사**해서 캡션도 "방법8"로 바꿔 넣는 걸 추천(재계산 없음, 단순 파일 복사).

5. **3.3절 제목**: "KPI 비교 (W1=1, W2=20, 벌금율 50%)"인데 방법8 행은 그 지점이 아니다. 사용자가 말한 대로 행 이름 자체에 "(W1=1, W2=80=20×4)"를 표기하는 정도면 충분하다고 판단, 별도 박스는 안 넣는 방향에 동의 — 이 부분은 추가 수정 없이 그대로 진행.

### 결론 / 다음 단계

B-2(절 번호: 5.8=방법8+기존 5.8→5.9 로 밀지, 아니면 그냥 5.9로 이어 붙일지)만 사용자 결정이 필요하고, 나머지(요청 ①~⑦ 전부 + 위에서 찾은 추가 지점들)는 이 검토 방향대로 반영 예정 — 기존 편집 규칙대로 원문은 빨간 취소선, 새 내용은 파란색으로 처리. **아직 `실험결과_보고서_지원_m1_방법6쉉.md`에는 반영되지 않은 상태** (이 문서는 검토 내용만 기록).
