# 방법1 vs 방법6.1(SPO+ base) — "이익식이 같다"는 전제 검증

## 0. 질문 배경

논문(Karimi & Kwon, *Applied Energy* 326, 2022) 재현 프로젝트에서 방법1과 방법6.1(SPO+ base)을 비교하다가 발견한 의문:

- **방법1**: 3항 이익식 그대로, PC = 0.5×DA, 학습 손실 = `-profit(β)` (이익을 그대로 손실로 씀)
- **방법6.1(SPO+ base)**: 이익식은 그대로 두고(PC=rate×DA, rate=0.5), 학습 손실만 regret(`ζ_i = oracle_i - profit_i(β)`)으로 바꿈. `oracle_i`는 {약정 0, 실제발전량, 설비최대} 3후보 중 최댓값.

수식으로 따지면: `oracle_i`는 학습 대상 β와 무관한 상수이므로

```
Σζ_i = Σoracle_i - Σprofit_i(β)
```

이고 `Σoracle_i`는 β에 대해 상수이므로, `Σζ_i`를 최소화하는 것과 `Σ(-profit_i(β))`를 최소화하는 것은 **PC 식이 완전히 같다면** 수학적으로 동일한 최적화 문제(같은 argmin β)여야 한다.

그런데 실제 결과는 방법1이 최악(구조적 붕괴), 방법6.1이 최상위권으로 극단적으로 다르다. PC=rate×DA가 두 방법에서 정말 완전히 같은 식인지 검증이 필요했다.

---

## 1. 코드 원문 대조

**방법1** (`paper_eq_ar_mlr_z03_block18.py:546-576`, 학습용 MILP 계수):

```python
oracle[i] = max(p0, pa, p1)          # p1 = scale*(dp*1.0 + rp*s1 - pc*y1), pc = rate*dp
denom = oracle.sum()                  # 시간대별 90개 학습표본 오라클의 합

sc[i] = (-W1*scale*rt_h[i]/denom) + (W2/n_obs)   # surplus cost
yc[i] = ( W1*scale*pc     /denom) + (W2/n_obs)   # shortage cost, pc = rate*da_h[i]
obj[xs+i] = -W1*scale*da_h[i]/denom
```

**방법6.1** (`방법6_SPO_base_AR_MLR.py:341-345`, 학습용 MILP 계수):

```python
pc_h = penalty_rate * da_h                      # PC = rate*DA (raw)
indicator = (da_h > rt_h).astype(float)          # I[DA>RT]

sc = -W1 * rt_h + W2_eff                         # surplus cost
yc = W1 * rt_h + W1 * pc_h * indicator + W2_eff  # shortage cost — RT 무조건 + PC 조건부
obj[xs:xs+n_obs] = -W1 * da_h
# W2_eff = W2 * W2_BALANCE_WEIGHT(=4)
```

평가(Gap 계산) 함수도 다르다:

- **방법1** (`compute_gap`): `realized = DA*x + RT*y⁺ − PC*y⁻`, oracle **2후보** {0, actual}.
- **방법6.1** (`compute_gap_4term`): `realized = DA*x + RT*y⁺ − RT*y⁻ − PC*y⁻`(RT가 무조건 shortage 비용에 들어감), oracle **3후보** {0, actual, 1.0}.

---

## 2. 정말 같은 식인가? — 아니다, 세 군데가 다르다

| | 방법1 | 방법6.1 |
|---|---|---|
| shortage 비용(경제식) | `PC·y⁻` (PC=rate·DA) | `(RT + PC·I[DA>RT])·y⁻` |
| 목적함수 정규화 | `scale/denom(Σoracle)/n_obs` | raw + `W2_eff=W2×4` |
| 평가 oracle | 2후보 {0, actual} | 3후보 {0, actual, 1.0} |

방법6.1의 헤더 주석에도 명시돼 있다:

> "SPO_base — **PC=rate×DA (RT 재정의 이전의 원래 4항식 벌금)**... PC는 DA>RT일 때만 shortage 비용에 추가된다 — I[DA>RT]"

즉 "이익식은 그대로 두고 학습손실만 바꿨다"는 전제와 달리, 방법6.1은 **RT를 shortage 비용에 무조건 포함시키는 "4항식" + DA>RT 조건부 지시함수**라는, 방법1의 3항 Eq.1a와는 다른 경제 모형을 쓰고 있다.

---

## 3. 유도한 논리, 어디가 안 맞나

`oracle_i가 β와 무관한 상수`라는 전제 자체는 **맞다**. `Σζ`와 `Σ(-profit)`이 상수 차이뿐이라는 것도 **"profit_i(β)가 정확히 같은 함수일 때는" 맞다.** 하지만 실제로 어긋나는 지점은 두 곳이다.

### (a) 정규화가 "상수 곱"이 아니라 "W1/W2 상대 비중을 바꾸는 것"이다

방법1은 `W1`항만 `denom`(≈4~7만, 시간대마다 다름)으로 나누고 `W2`항은 별도로 `n_obs`(=90)로 나눈다. 한 시간대 안에서 목적함수 **전체**를 스칼라 하나로 나누는 거라면 argmin이 안 바뀌겠지만, **W1 항과 W2 항을 서로 다른 스칼라로 따로 나누면 "경제성 대 정확도"의 상대 가중치 자체가 바뀐다.**

실측(§4 참고)해보니 방법1의 계수 크기는 방법6.1보다 **467~553배 작다.** 이 정도면 W1(경제) 신호가 W2(정확도)에 완전히 묻혀버린다 — 이게 이 프로젝트 전체에서 반복 확인된 "denom 정규화 → W1 실종 → 붕괴" 패턴이다(방법2 raw+weight 보정 관련 절 참고). 즉 oracle이 상수라는 사실과 별개로, **정규화 스칼라가 W1과 W2에 다르게 적용된다는 사실 때문에 애초에 같은 최적화 문제가 아니다.**

### (b) profit_i(β) 함수 자체가 다르다

방법6.1은 RT를 shortage 비용에 무조건 추가하고 PC에 지시함수를 곱한다 — 이건 "같은 함수를 regret으로 재표현한 것"이 아니라 "다른 함수를 최소화하는 것"이다.

두 요인 중 **(a)가 지배적**이다 — 계수 크기 차이가 500배 수준으로, (b)의 형식 차이보다 압도적으로 크다.

---

## 4. 실증 검증 — 실제로 β를 풀어서 비교

`temp_debug_method1_vs_6base_compare.py`로, 같은 데이터·같은 W1=1·W2=20·rate=0.5(KPI 지점)에서 두 방법의 시간대별 MILP를 각자의 방식 그대로 풀어 비교했다.

### 4.1 목적함수 계수 비교 (시간대 0, 6)

```
--- 시간대 0 (DA 평균=35.13, RT 평균=35.86, DA>RT 비율=62.2%) ---
  [방법1]  denom(Σoracle)=73070.4
           obj_x  범위=[-0.024470, -0.010207]
           sc(surplus) 범위=[0.181531, 0.214360]
           yc(shortage) 범위=[0.227326, 0.234457]
  [방법6.1] obj_x  범위=[-59.6000, -24.8600]
           sc(surplus) 범위=[-19.1100, 60.8500]
           yc(shortage) 범위=[105.2100, 179.1100]
  → 방법1 계수는 denom(~73070)로 나눠서 방법6.1보다 대략 553배 작다 (자릿수 자체가 다름)

--- 시간대 6 (DA 평균=20.48, RT 평균=19.40, DA>RT 비율=75.6%) ---
  [방법1]  denom(Σoracle)=43833.7
           obj_x  범위=[-0.020128, -0.006844]
           sc(surplus) 범위=[0.184402, 0.238415]
           yc(shortage) 범위=[0.225644, 0.232286]
  [방법6.1] obj_x  범위=[-29.4100, -10.0000]
           sc(surplus) 범위=[24.7400, 103.6600]
           yc(shortage) 범위=[63.4850, 135.2600]
  → 방법1 계수는 denom(~43834)로 나눠서 방법6.1보다 대략 467배 작다 (자릿수 자체가 다름)
```

### 4.2 실제 학습된 β(AR 계수) 비교 — 전체 12시간대

```
최대 |β1-β6.1| = 4.428227  (시간대 0, 계수 인덱스 1)
평균 |β1-β6.1| = 0.216538
```

시간대별 절편(β0) 비교:

```
h= 0: 방법1 β0=  +0.2103   방법6.1 β0=  +0.1259   차이=  +0.0844
h= 1: 방법1 β0=  +0.3888   방법6.1 β0=  +0.3259   차이=  +0.0630
h= 2: 방법1 β0=  +0.5696   방법6.1 β0=  +0.4998   차이=  +0.0698
h= 3: 방법1 β0=  +0.5193   방법6.1 β0=  +0.4790   차이=  +0.0404
h= 4: 방법1 β0=  +0.5089   방법6.1 β0=  +0.5082   차이=  +0.0007
h= 5: 방법1 β0=  +0.3981   방법6.1 β0=  +0.3913   차이=  +0.0067
h= 6: 방법1 β0=  +0.2385   방법6.1 β0=  +0.2224   차이=  +0.0161
h= 7: 방법1 β0=  +0.1293   방법6.1 β0=  +0.1302   차이=  -0.0010
h= 8: 방법1 β0=  +0.0475   방법6.1 β0=  +0.0470   차이=  +0.0005
h= 9: 방법1 β0=  +0.0024   방법6.1 β0=  +0.0024   차이=  -0.0000
h=10: 방법1 β0=  +0.0004   방법6.1 β0=  +0.0004   차이=  +0.0000
h=11: 방법1 β0=  +0.0001   방법6.1 β0=  +0.0001   차이=  -0.0000
```

절편만 보면 태양광이 활발한 시간대(h=0~3)에서 차이가 크고(0.04~0.08), 발전량 자체가 0에 가까운 야간 시간대(h=8~11)에서는 차이가 거의 없다(값 자체가 0 근처로 수렴하기 때문에 어느 쪽으로 풀어도 비슷해짐). **최대 4.43이라는 차이는 수치오차(보통 1e-9 수준) 범위를 훨씬 넘어서고, 두 스크립트가 실제로 서로 다른 최적화 문제를 풀고 있다는 걸 직접 확인시켜준다.**

---

## 5. 정리 (1차)

방법1의 구조적 붕괴와 방법6.1의 우수한 재현은 "이익식은 같은데 학습손실만 regret으로 바꿔서" 생긴 차이가 **아니라**,

1. 정규화 방식(denom-scale vs raw+weight)이 W1/W2 상대 비중을 완전히 다르게 만들고,
2. 애초에 shortage 비용 함수 자체(RT 무조건 포함 + 지시함수)가 다르기 때문이다.

이 시점까지는 두 요인 중 **①(정규화 방식)이 지배적**이라고 봤다(계수 크기 차이가 500배 수준으로, ②의 형식 차이보다 압도적으로 커 보였기 때문). 하지만 §6에서 정규화 방식만 따로 떼어 실제로 돌려본 결과, 이 판단은 **수정이 필요하다** — 아래 §6.5 참고.

---

## 6. 후속 검증 — denom(오라클 합) 정규화가 §2.5("정규화 시도")의 그것과 같은가?

### 6.1 배경 질문

보고서 2.5절("W1 항 정규화 시도")에는 다음과 같이 적혀 있다:

> "이후 W1 항만 학습기간의 oracle profit 합으로 나누는 방식도 시도했으나, 이번에는 economic term의 영향이 지나치게 작아져 Proposed AR이 Baseline AR과 거의 동일하게 움직였다. ... 최종 실험에서는 모든 항에 별도의 정규화를 적용하지 않았다."

그런데 옛 SPO+ 스크립트(`spo_plus_ar_mlr_z01_block21.py`, 삭제된 `integrated_spo_plus_4term_fig3568_AR_MLR.py`)를 열어보면 여전히 `denom`(오라클 합)으로 W1 항을 나누는 코드가 남아 있다:

```python
den = oc.sum()                                          # ← "학습기간 oracle profit 합"
sc[i] = (-W1*scale*rh[i]/den) + (W2/no)                  # W1만 den으로, W2는 no(표본수)로 별도
yc[i] = (W1*scale*rh[i]/den) + (W1*scale*pc*indicator/den) + (W2/no)
obj[xs+i] = -W1*scale*dh[i]/den
```

이게 §2.5에서 "뺐다"고 한 그 정규화와 같은 것인지, 같다면 왜 SPO+에는 남아 있었는지, 빼면 결과가 방법6.1처럼 좋아지는지를 검증했다.

### 6.2 같은 연산인가? — 예, 형태가 100% 일치한다

**W1쪽 항만 오라클 합(denom)으로 나누고 W2쪽은 표본수(n_obs)로 따로 나눈다** — §2.5가 기술한 방식과 코드 구조가 정확히 일치한다. 이름만 같은 게 아니라 실제로 같은 수식이다.

### 6.3 의도적 설계인가, 리팩터링 누락인가? — git 이력으로 확인

```
09/08  spo_plus_ar_mlr_z01_block21.py 생성 (denom, PC=rate*DA+indicator)         ← 지금도 존재
09/08  integrated_spo_plus_4term_fig3568_AR_MLR.py 생성 (denom, PC=RT+rate*DA)
09/12  (커밋 a71dc50) 방법6_SPO_base_AR_MLR.py 신규 생성 (raw+weight4)
       방법6_SPO_plus_AR_MLR.py 신규 생성 (raw+weight4)
       integrated_spo_plus_4term_fig3568_AR_MLR.py 삭제 ← 같은 커밋에서
```

`방법6_SPO_base_AR_MLR.py` 헤더 주석: *"원본: spo_plus_ar_mlr_z01_block21.py를 방법2/4/5/6과 같은 컨벤션(**raw 계수+W2 balance weight**)으로 재정리한 버전"*.

→ **denom 제거는 의도적 리팩터링이었다.** `spo_plus_ar_mlr_z01_block21.py`(원본)가 `integrated_spo_plus_...`와 달리 삭제되지 않고 남아있는 건 정리 누락(청소 안 함)일 뿐이고, 현재 실행 경로 어디서도 이 파일을 참조하지 않는다 — 로직상 살아있는 버그는 아니다.

### 6.4 denom을 빼면 방법6.1처럼 좋아지는가? — 실행 결과: "그렇지 않다, 더 복잡하다"

`temp_debug_spo_denom_ablation.py`로 **SPO+ 4항식(PC=rate·DA·I[DA>RT])을 고정한 채** 정규화만 세 가지로 바꿔 z03/블록18에서 비교했다.

**KPI 지점(W1=1,W2=20,rate=0.5)** — 논문 참고값 34.89%/13.91%

| 정규화 | nRMSE | Gap |
|---|---:|---:|
| (A) denom (옛 z01 방식) | 35.60% | 14.71% |
| (B) raw, 가중치 없음 | 37.47% | 14.45% |
| (C) raw+weight4 (현재 방법6.1) | **35.01%** | **14.06%** |

**W1/W2=5/1, 1/0 (방법1이 116%대로 완전 붕괴하는 지점)**

| 정규화 | 5/1 nRMSE | 1/0 nRMSE |
|---|---:|---:|
| (A) denom | **43.71%** | 65.68% |
| (B) raw | 66.35% | 65.68% |
| (C) raw+weight4 | 64.63% | 65.68% |

**핵심 관찰**:

1. **셋 다 방법1의 116% 붕괴가 재현되지 않는다** — denom을 그대로 둬도(A) SPO+는 무너지지 않는다.
2. 오히려 고-W1 구간(5/1)에서는 **denom 방식(A)이 raw(B)·raw+weight(C)보다 더 안정적**이다(43.71% vs 64~66%). KPI 지점에서만 raw+weight4가 근소하게 제일 좋다.
3. W1/W2=1/0(W2=0)에서는 세 방식이 **완전히 같은 값**을 낸다 — W2=0이면 W2쪽 항이 통째로 사라지므로, 남은 W1쪽 전체를 양수 상수 하나로 나누는 건 argmin을 바꾸지 않는다는 이론과 정확히 일치한다.

### 6.5 왜 이런 차이가 나나 — 진짜 원인은 이익식의 구조

denom 정규화가 SPO+ 4항식에서는 치명적이지 않았다는 사실은, **§2.5의 실패 원인이 정규화 자체가 아니라 방법1의 3항 이익식이 가진 구조적 결함**임을 보여준다.

방법1의 3항 이익식 `π = DA·x + RT·y⁺ − PC·y⁻`(PC=rate·DA)에서, shortage 구간(x>실제발전량)의 이익 기울기는

```
∂π/∂x = DA − PC = DA·(1 − rate)   →  rate<1이면 항상 양수
```

즉 **약정을 늘릴수록 무조건 이익이 커진다** — 이게 방법1이 W1/W2가 커지면 x→1로 쏠려 붕괴하는 이유다(3.2.1절).

SPO+ 4항식은 shortage 비용에 RT를 무조건 포함시킨다: `π = DA·x + RT·y⁺ − RT·y⁻ − PC·y⁻·I[DA>RT]`. 이때 shortage 구간 기울기는

```
∂π/∂x = DA − RT − PC·I[DA>RT]
       = DA(1−rate) − RT   (DA>RT일 때, indicator=1)
       = DA − RT            (DA≤RT일 때, indicator=0  →  항상 0 이하!)
```

**DA≤RT인 시간대에서는 기울기가 항상 0 이하**가 되어, "무조건 더 약정하는 게 유리하다"는 유인이 원천 차단된다. DA>RT인 시간대에서도 RT가 충분히 크면 기울기가 음수가 될 수 있어, 방법1처럼 "모든 시간대에서 항상 양수"인 상황 자체가 생기지 않는다.

이것은 **방법2가 PC를 `RT+rate·DA`로 재정의해서 얻은 것과 본질적으로 같은 구조적 수정**이다 — 형태는 다르지만(SPO+는 RT를 무조건 shortage 비용에 넣고 PC에만 지시함수를 곱함, 방법2는 PC 정의 자체에 RT를 합침), 둘 다 **"shortage 비용에 RT를 넣어 방법1의 무조건-과잉약정 유인을 없앤다"**는 같은 원리로 작동한다.

**결론(갱신)**: §5에서 "정규화 방식(①)이 지배적"이라고 판단했던 것은 **틀렸다.** 실제로는 **이익식 자체의 구조(②)가 붕괴 여부를 결정하는 필요조건**이고, 정규화 방식(①)은 (붕괴하지 않는 이익식 안에서) 논문 수치에 얼마나 가깝게 맞추느냐를 결정하는 미세조정 요인이다. 방법1이 어떤 정규화를 쓰든(denom이든 raw든) 3항 이익식 자체의 결함 때문에 붕괴할 가능성이 높고 — 반대로 SPO+/방법2류의 "RT 포함 shortage 비용" 이익식은 어떤 정규화를 쓰든 붕괴하지 않는다(다만 정규화에 따라 논문과의 근접도는 달라진다).

### 6.6 파일 대응 관계 — "방법4" 라벨은 옛 번호 체계

- `spo_plus_ar_mlr_z01_block21.py` (PC=rate·DA + `I[DA>RT]`) → **방법6.1(SPO+ base)의 전신.** 헤더에 "방법4: Smart SPO+ Loss"라고 적혀 있는데, 이건 프로젝트 초기(09/08)의 임시 번호다 — 지금 보고서의 "방법4"(PC=max(DA,RT))와는 무관한 이름 충돌이고, 이후 최종 번호 체계(방법1~7)가 확정되며 "방법6"으로 재배치됐다. 파일명 자체는 리네임되지 않고 그대로 남아있을 뿐이다.
- `integrated_spo_plus_4term_fig3568_AR_MLR.py` (PC=RT+rate·DA, indicator 없음) → **방법6.2(SPO+ plus)의 전신.** 현재 작업트리에는 없고 git 이력(커밋 `1cfc370`)에만 남아 있다 — `방법6_SPO_plus_AR_MLR.py`로 리팩터링되며 삭제됐다.
- 두 파일 다 z01/블록21 대상이었고(방법6.1/6.2가 나중에 z03/블록18로 전환되기 이전 버전), 방법4(PC=max)와는 완전히 별개의 실험이다.

---

## 7. 최종 확인 — "SPO+ 손실"은 결과에 실질적 영향이 없었다

### 7.1 결론(확인 요청)

지금까지 나온 검증 결과를 종합하면 다음 결론에 도달한다:

> "SPO+로 학습 loss를 regret으로 바꿨다"는 것 자체는 이 프로젝트의 결과에 실질적으로 아무 영향을 주지 않았고 — oracle이 β와 무관한 상수인 이상 그냥 raw profit maximization을 다른 이름으로 부른 것뿐이다 — 방법6.1/6.2가 방법1과 다른 결과를 낸 진짜 이유는 100% "이익식 자체를 방법2류로 고쳤기 때문"이다. 즉 방법6은 손실함수 혁신이 아니라 그냥 방법2/3 계열의 이익식 수정에 "SPO+"라는 이름표를 붙인 것에 불과하다.

이 절에서는 이 결론을 코드·수식·실측 세 가지로 최종 확인한다.

### 7.2 실측 검증 — 이익식을 완전히 고정하고 손실만 바꿔본다

`temp_debug_regret_vs_profit_exact_equivalence.py`로, **방법6.1과 완전히 같은 4항 이익식**(PC=rate·DA, RT 무조건 포함, PC엔 `I[DA>RT]`)을 고정한 채 목적함수만 두 가지로 실제로 구성했다:

- **(I) "regret" 버전**: `obj = Σ[W1·(oracle_i − profit_i(β))] + W2·MAE` — `oracle_i`를 하한=상한=1로 고정한 더미 변수 `k`의 계수로 실제로 얹어서, "정말로 오라클을 포함한 regret 목적함수"를 코드로 구현했다(scipy `milp`는 목적함수에 상수항을 직접 못 받기 때문).
- **(II) "profit-max" 버전**: `obj = Σ[W1·(−profit_i(β))] + W2·MAE` — 오라클 없음.

**결과 (12시간대 전부, KPI 지점 W1=1·W2=20·rate=0.5):**

```
h= 0: max|β_profit-β_regret|=0.00e+00   목적함수값 차이=+73070.4396   Σoracle=+73070.4396   ✓ 일치
h= 1: max|β_profit-β_regret|=0.00e+00   목적함수값 차이=+94318.6490   Σoracle=+94318.6490   ✓ 일치
h= 2: max|β_profit-β_regret|=0.00e+00   목적함수값 차이=+85999.6620   Σoracle=+85999.6620   ✓ 일치
h= 3: max|β_profit-β_regret|=0.00e+00   목적함수값 차이=+73575.4549   Σoracle=+73575.4549   ✓ 일치
h= 4: max|β_profit-β_regret|=0.00e+00   목적함수값 차이=+62280.4679   Σoracle=+62280.4679   ✓ 일치
h= 5: max|β_profit-β_regret|=0.00e+00   목적함수값 차이=+53249.0455   Σoracle=+53249.0455   ✓ 일치
h= 6: max|β_profit-β_regret|=0.00e+00   목적함수값 차이=+43833.6749   Σoracle=+43833.6749   ✓ 일치
h= 7: max|β_profit-β_regret|=0.00e+00   목적함수값 차이=+36170.1184   Σoracle=+36170.1184   ✓ 일치
h= 8: max|β_profit-β_regret|=0.00e+00   목적함수값 차이=+29976.5669   Σoracle=+29976.5669   ✓ 일치
h= 9: max|β_profit-β_regret|=0.00e+00   목적함수값 차이=+25935.5461   Σoracle=+25935.5461   ✓ 일치
h=10: max|β_profit-β_regret|=0.00e+00   목적함수값 차이=+25197.2136   Σoracle=+25197.2136   ✓ 일치
h=11: max|β_profit-β_regret|=0.00e+00   목적함수값 차이=+27548.7606   Σoracle=+27548.7606   ✓ 일치

전체 12시간대 중 최대 |β_profit-max − β_regret| = 0.00e+00
```

**β가 근사가 아니라 완전히 비트 단위로 동일**하고, 목적함수 값의 차이는 정확히 `Σoracle_i`(예상한 상수)와 소수점까지 일치한다.

추가로 `방법6_SPO_base_AR_MLR.py`의 실제 학습 함수(`solve_ar_spo_base`)를 다시 확인해보면, **"oracle"이라는 단어 자체가 학습 함수에는 전혀 등장하지 않는다** — Gap 평가 함수(`compute_gap_4term`)에만 나온다. 즉 실제 production 코드는 애초부터 regret을 명시적으로 구성하지도 않고, 처음부터 순수 profit-max로 짜여 있었다. **§7.1의 결론이 코드·수식·실측 세 가지 모두에서 정확히 확인된다.**

### 7.3 보고서 3.2.6절 정정 제안

현재 3.2.6절 첫 문장:

> "방법 6은 반대로 이익식(PC=rate×DA 또는 방법2의 PC=RT+rate×DA)은 그대로 두고, 학습 목적함수의 내용을 바꿨다"

이건 부정확하다 — 방법6.1은 PC=rate×DA를 "그대로" 쓰지 않고 `RT를 shortage 비용에 무조건 추가 + PC엔 I[DA>RT] 지시함수`라는 **수정된 4항 이익식**을 쓴다. 제안하는 정정문:

> 방법 6은 처음에는 "이익식은 그대로 두고 학습 손실만 후회(regret)로 바꾼 것"으로 소개했으나, 검증 결과 이는 부정확했다. `oracle_i`는 β와 무관한 상수이므로 `Σζ_i = Σoracle_i − Σprofit_i(β)`이고, 실제로 코드를 돌려봐도 후회(regret) 최소화와 순수 이익 최대화(`-profit`)는 학습된 β가 소수점까지 완전히 같다(§7.2 참고) — **"SPO+ 손실"이라는 이름표는 결과에 실질적 영향이 없다.** 방법6.1·6.2가 방법1과 다른 결과를 낸 진짜 이유는 학습 손실이 아니라, 밑에 깔린 이익식 자체가 이미 수정돼 있었기 때문이다 — 방법6.1은 shortage 비용에 RT를 무조건 포함시키고 PC엔 `I[DA>RT]` 지시함수를 곱한 4항식을, 방법6.2는 방법2와 동일한 PC=RT+rate×DA를 그대로 재사용한다. 즉 **방법 6은 손실함수 혁신이 아니라, 방법2·3 계열의 이익식 수정에 "SPO+"라는 이름을 붙인 것**에 더 가깝다.

**§5.6 도입부**도 같은 문제가 있어 함께 고쳐야 한다: "방법 6은 이익식은 건드리지 않고, 학습 목적함수의 W1 항을... 후회로 바꾼 것이다" → "방법 6은 방법2·3처럼 shortage 비용에 RT를 반영한 수정 이익식을 쓰되, 학습 손실을 '후회(regret)'라는 이름으로 다시 표현한 것이다(수학적으로는 순수 이익 최대화와 동일 — 3.2.6절 참고)."

**"방법6.2=방법2 정확히 일치"를 설명하는 문장들**(3.3절 등에 있는 "SPO+의 후회(ζ) 기반 목적함수가 방법2의 가중합 목적함수와... 수학적으로 동일한 최적해로 수렴하기 때문")도 인과를 더 정확히 쓸 수 있다 — "수렴한다"가 아니라 **"oracle이 β와 무관한 상수라서 두 목적함수가 애초에 동일한 최적화 문제이기 때문"**이라고 명시하는 게 더 정확하다.

### 7.4 놓친 예외 케이스는 없는가

**방법6(base/plus) 안에서는 예외가 없다** — §7.2의 실측으로 정확한 등식이 확인됐고, MILP 이진변수·보완성 제약도 두 정식화에서 완전히 동일한 구조라 영향이 없다(오라클 상수는 어떤 결정변수와도 결합돼 있지 않으므로 이진변수 선택 `bin_list`에도 전혀 관여하지 않는다).

**다만 이 프로젝트 안에 예외가 하나 있다 — 방법7(Additive CVaR)이다.** `Σζ_i`(합, sum)은 `oracle_i`가 상수든 아니든 상쇄되지만, **CVaR/quantile 같은 비선형(순서통계량 기반) 함수는 이 상쇄가 성립하지 않는다.** `CVaR(a_i − c_i) ≠ CVaR(a_i) − (상수)`는 `c_i`가 관측치마다 다를 때 일반적으로 성립하지 않기 때문이다(oracle_i는 시간대·가격에 따라 관측치마다 다르다). 그래서 방법7의 CVaR 항은 regret이 아니라 순수 profit으로 다시 써도 **결과가 달라질 수 있다** — 방법6과 달리 방법7에서는 "오라클이 상수라 무관하다"는 논리가 적용되지 않는다는 걸 유의해야 한다(시도방법7.md에 별도로 짚어둘 가치가 있다).

---

## 8. §7 결론 기반 후속 제안 — "방법 6"을 독립 방법론으로 다루는 틀 재고 (비판적 검토)

### 8.1 검토 대상 제안

§7의 결론("SPO+ 손실은 결과에 실질적 영향이 없었다")을 근거로 다음 제안이 나왔다:

1. **방법6.2**(PC=RT+rate·DA, 방법2와 완전 동일 이익식) — oracle-상수 증명 때문에 방법2와 처음부터 같은 최적화 문제였고 실측도 β·Fig 전부 일치하니, 독립된 실험으로서 정보가치가 0에 가깝다 → KPI 비교표·순위표의 별도 행에서 빼고 "oracle-상수 sanity check" 각주로 강등해야 하는 것 아닌가?
2. **방법6.1**(RT를 shortage 비용에 무조건 포함 + PC엔 `I[DA>RT]`) — 결과 자체는 유효하지만 "SPO+ 손실 덕분"이라는 설명은 틀렸으니, 방법2·3과 같은 카테고리("shortage 비용 정의를 바꾼 profit function 변형")로 재분류해야 하는 것 아닌가? 3.2.6절 제목·결론 문단도 지금 분석과 반대로 틀렸으니 같이 고쳐야 하지 않나?

**결론부터: 일부는 동의하지만, 두 제안 다 "전부 반영"하면 과하다.**

### 8.2 반박 1 — 방법6.2를 각주로 완전히 강등하는 건 과하다

"정보가치가 0에 가깝다"는 평가에 동의하지 않는다. **정보가치는 0이 아니라 "다른 종류"다.** 6.2가 6.1과 별개로 존재하는 이유는 "새로운 결과를 보여주기 위해서"가 아니라 **"oracle-상수 등식이 코드 수준에서도 성립하는지 확인하는 대조군(control)"** 역할이기 때문이다 — 보고서 §2.7("7종 solver 교차검증")이 "새 결과"가 아니라 "기존 결과의 검증"이었음에도 독립된 절로 남아있는 것과 같은 성격이다. 6.2를 완전히 지우면 "방법2.2를 실제로 독립 코드로 돌려봤더니 정말로 방법2와 소수점까지 같더라"는, §7이 주장하는 등식의 **가장 강력한 실증 증거 하나가 눈에 안 띄게 사라진다.**

**최종 제안**: 표1·표2(KPI 비교표)에는 **남기되**, "평가" 칸 문구를 "방법2와 수치 동일"에서 **"= 방법2 (oracle-상수 등식의 실측 대조군, 독립 결과 아님)"**로 바꿔 역할을 명확히 한다. 반면 **순위표(5.7.2)는 이미 두 방법을 "4위 동률" 한 줄로 합쳐놨으므로** 별도 수정이 필요 없다 — 확인해보니 사용자가 걱정하는 중복 카운트 문제가 이미 해결돼 있었다.

### 8.3 반박 2 — 방법6.1을 "방법2·3류로 재분류"는 절반만 맞다

**동의하는 부분**: "SPO+ 손실 덕분에 좋아졌다"는 인과 설명은 틀렸고, 정정해야 한다.

**반박하는 부분**: 방법6.1의 실제 이익식(`RT 무조건 포함 + PC엔 I[DA>RT] 조건부`)은 방법2(`PC=RT+rate×DA`)도, 방법3(`c×RT`)도 아닌 **제3의 변형**이다. 이 조건부 지시함수(`I[DA>RT]`)는 방법2·3에는 없는 요소이고, **Elmachtoub & Grigas(2022) SPO+ 논문의 shortage-cost 가중치 아이디어에서 실제로 따온 것**이다(오라클-상수 등식으로 "손실 메커니즘"은 무효화됐지만, 이 **특정 조건부 가중치 형태**는 그 논문에서 유래한 게 맞다). "방법2·3류로 재분류"라고만 하면 이 계보(provenance)가 사라진다.

**최종 제안**: 방법6.1은 **번호와 정체성은 유지**하되(방법2/3/4/5와 마찬가지로 "이익식 수정" 대카테고리에 소속시키고), **"SPO+에서 조건부 가중치 아이디어만 차용했다"는 계보는 명시적으로 남긴다.**

### 8.4 반박 3 — 방법7까지 같이 의심하면 안 된다

이건 사용자가 직접 묻지 않았지만 중요하다: **방법7(Additive CVaR)은 이번 비판에서 면제된다.** §7.4에서 확인했듯 CVaR은 quantile 함수라 `oracle_i`가 관측치마다 달라도 상쇄되지 않는다 — 방법7의 "손실 형태(CVaR)가 결과를 바꾼다"는 주장은 **여전히 유효하다.** 재분류 작업 중에 방법6과 방법7을 같은 논리로 묶지 않도록 주의해야 한다.

### 8.5 구체적 수정안 (실험결과_보고서_지원_m1.md에 실제 반영 완료)

#### 3.1절 (방법6 서술 문단 + 요약표)

문단 교체 — 현재("방법 6은 profit function 대신 학습 loss를 Smart SPO+ 형태로 변경한 실험이다. ... 원본 profit function의 구조 자체를 바꾼 것은 아니므로...")는 사실과 반대이므로:

> 방법 6은 처음에는 "학습 loss를 SPO+ 후회(regret)로 바꾼 것"으로 설계했으나, 검증 결과 oracle이 β와 무관한 상수라서 이 손실 변경은 순수 이익 최대화와 수학적으로 동일한 최적화 문제였다(3.2.6절, 부록 `방법1_방법6.1_이익식_비교분석.md` §7 참고). 실제로 결과를 가른 것은 손실이 아니라 **이익식 자체의 변형**이었다 — 방법6.1은 shortage 비용에 RT를 무조건 포함시키고 벌금(PC)엔 DA>RT 조건부 가중치를 곱한 변형(Elmachtoub & Grigas의 SPO+ 논문에서 조건부 가중치 아이디어를 차용)이고, 방법6.2는 방법 2와 완전히 동일한 PC=RT+rate×DA를 재사용한 것으로 결과도 방법2와 소수점까지 일치한다. 그래서 방법6은 방법2~5와 같은 "이익식 수정" 계열로 다시 분류했고, 방법6.2는 독립된 실험이 아니라 이 재분류(oracle-상수 등식)를 실측으로 확인하는 대조군으로 취급한다.

요약표 방법6 행: *"이익식은 그대로 두고, W1 항의 내용을 손실(-profit) 대신 Smart SPO+ 후회(regret)로 바꾼 것"* → *"벌금가에 DA>RT 조건부 가중치를 적용하고 shortage 비용에 RT를 포함시킨 변형(SPO+ 논문에서 조건부 가중치 아이디어 차용 — 단, 손실을 regret으로 표현한 부분은 수학적으로 profit-max와 동일함이 확인됨)"*

#### 3.2.6절 제목 + 결론 문단

제목: "방법 6 — Smart SPO+" → **"방법 6 — 조건부 shortage 비용 (SPO+에서 가중치 아이디어 차용)"**

결론 문단(기존: "구현은 방법 2보다 훨씬 복잡해졌지만, 근본적인 DA-PC>0 문제 자체를 바꾸지는 못했다..." — 방법6.1이 전체 1위인 것과 정면 모순) → :

> **결론(정정)**: 처음엔 "목적함수만 바꿔서 DA-PC>0 문제를 완화했다"고 이해했으나 이는 틀렸다 — oracle이 β와 무관한 상수이므로 후회(regret) 최소화는 순수 이익 최대화와 정확히 같은 최적화 문제였다(학습된 β가 소수점까지 일치 — 부록 참고). 방법6.1이 방법1보다 훨씬 나은 결과(KPI 지점 Δ nRMSE +0.12%p, 전체 방법 중 최상위)를 낸 진짜 이유는, shortage 비용에 RT를 무조건 포함시켜 부족 구간의 이익 기울기를 `DA−RT−PC·I[DA>RT]`로 바꿨기 때문이다 — DA≤RT인 시간대에서는 이 기울기가 항상 0 이하가 되어 방법1의 구조적 결함이 원천 차단된다. 방법2가 PC 재정의로 얻은 효과와 원리상 동일하다. 방법6.2(indicator 없음, PC=RT+rate×DA)는 방법2와 이익식이 완전히 같아 β·Fig·KPI가 소수점까지 일치하며, 이는 위 등식을 실측으로 재확인해주는 대조군이다.

#### 5.6절 도입부

현재: *"방법 6은 이익식은 건드리지 않고, 학습 목적함수의 W1 항을... 후회로 바꾼 것이다"*

→ *"방법 6은 방법2·3처럼 shortage 비용에 RT를 반영한 수정 이익식을 쓰되, 학습 손실을 '후회(regret)'라는 이름으로 다시 표현한 것이다(수학적으로는 순수 이익 최대화와 동일 — 3.2.6절 참고)."*

#### 3.3절 표1·표2 + 설명 문단

방법6.2 행 "평가" 칸: "방법2(Fine Tune)와 수치 동일 (아래 설명 참고)" → **"= 방법2 (oracle-상수 등식의 실측 대조군, 독립 결과 아님)"**

방법6.1 행 "평가" 칸: 현 문구("가장 가까움")는 유지하되, 아래 설명 문단·5.6.2절에서 "SPO+의 후회(ζ) 기반 목적함수가... 수렴하기 때문"이라는 표현을 **"oracle이 β와 무관한 상수라서 애초에 동일한 최적화 문제이기 때문"**으로 인과를 정정.

### 8.6 반영 상태

위 4곳(3.1절, 3.2.6절, 5.6절, 3.3절) 전부 `실험결과_보고서_지원_m1.md`에 실제 반영 완료 — 기존 문장은 빨간 취소선, 새 문장은 파란색으로 표시했다(그 파일만 대상, 다른 사본은 건드리지 않았다).

---

## 9. 방법2 Fine Tune(§5.2.2) 검증 — "W2_BALANCE_WEIGHT=4"는 정말 새로운 장치인가?

### 9.1 배경 — 논문 Eq.(9a)에 정규화가 있는가

보고서 §5.2.2 "문제 2"는 다음과 같이 서술한다: *"기존 목적함수는 논문 Eq.(9a)의 정규화(scale·denom=Σoracle·N)를 그대로 따랐는데..."* — 이 문장이 논문 원문과 맞는지 확인이 필요했다.

논문(Karimi & Kwon, *Applied Energy* 326, 2022) PDF에서 Eq.(9a) 원문을 직접 읽었다:

```
min Z_h = Σ_{t∈U_h} [ W1·((DP_t·S_t) − (DP_t·Ŝ_t + RP_t·y'_t − PC_t·y''_t)) + W2·|S_t−Ŝ_t| ]
```

바로 아래 설명: *"W1 and W2 are regularization parameters that may be used to adjust the trade-off... By increasing the ratio of W1/W2..."* — **여기까지가 전부다.** `Σoracle`로 나누라는 말도, `N`(표본수)으로 나누라는 말도 논문 어디에도 없다. 그냥 raw sum이다.

**결론**: 보고서 §5.2.2 "문제 2"의 "논문 Eq.(9a)의 정규화를 따랐다"는 서술은 **틀렸다.** 정확히는 "기존 구현이 논문에 없는 denom/N 정규화를 **자체적으로 추가**했다가 문제가 생겼다"고 써야 한다.

### 9.2 W2_BALANCE_WEIGHT=4는 "W2=80을 그냥 쓰는 것"과 다른가 — 완전히 같다 (항등식)

방법2 Fine Tune의 목적함수 코드:

```python
W2_eff = W2 * W2_BALANCE_WEIGHT     # W2=20, Weight=4 → W2_eff=80
sc = -W1*RT + W2_eff
yc = W1*PC + W2_eff
obj_x = -W1*DA
```

`W2`와 `Weight`는 목적함수 어디에도 **따로** 등장하지 않는다 — 오직 곱셈 결과 `W2_eff` 하나로만 들어간다. `20×4`든 `80×1`이든 `W2_eff=80`이면 완전히 같은 숫자다.

`temp_debug_weight4_vs_w2_80_equivalence.py`로 실측 확인:

```
(W1=1, W2=20, Weight=4)  vs  (W1=1, W2=80, Weight=1)
W2_eff 값:  A(20*4)=80   B(80*1)=80   동일
max|obj_x_A - obj_x_B| = 0.00e+00
max|sc_A - sc_B|       = 0.00e+00
max|yc_A - yc_B|       = 0.00e+00
bin_list 완전 동일?     = True  (개수: A=0, B=0)

>>> 목적함수 계수·이진변수 판정 전부 비트 단위로 완전히 동일하다.
    풀 필요도 없이 계수 자체가 같다.
```

즉 "Fine Tune"이 한 일은 실질적으로 "논문 벤치마크 지점 W1=1,W2=20을 그대로 두고 알고리즘을 개선"한 게 아니라, **"이 프로젝트의 raw(비정규화) 방식에서는 W1:W2 비율을 1:20이 아니라 1:80으로 바꿔야 논문 결과와 가장 가깝다는 걸 스윕으로 찾아낸 것"**이다 — Fig.3/6이 이미 하는 "W1/W2 비율 스윕"과 **성격상 완전히 같은 작업**을, W1/W2 두 숫자를 직접 바꾸는 대신 "Weight"라는 별도 곱수를 하나 더 얹어서 한 것뿐이다.

### 9.3 이진변수 판정에서 미묘한 차이가 생길 여지는? — 없다

이진변수 판정(`bin_list = [i for i in ... if sc[i]+yc[i]<0]`)도 `sc`, `yc` 값 자체로 판정하는데, 그 값들이 이미 완전히 같으니(`0.00e+00` 차이) 이진변수 집합도 자동으로 같다(위 실측에서 `bin_list 완전 동일 = True`). **정말 100% 동일한 문제**이지 "비슷한 결과를 주는 별개의 문제"가 아니다.

### 9.4 추가 발견 — 이 문제는 KPI 표 한 칸에 그치지 않는다

Fig.3/Fig.6(W1/W2 스윕)과 Fig.5/Fig.8(rate 스윕)의 그림 제목 코드를 대조하면:

```python
fig.suptitle(f"방법2_fig3 — ... rate={FIG36_RATE}, W1/W2 스윕 (dual y축)")                        # ← W2_eff 언급 없음
fig5_fig.suptitle(f"방법2_fig5 — ... W1={FIG58_W1}/W2={FIG58_W2}(W2_eff=W2×4 균형 보정), ...")     # ← 여기만 명시
```

**Fig.5/Fig.8 제목에는 "W2_eff=W2×4 균형 보정"이 명시돼 있는데, Fig.3/Fig.6 제목·3.3절 KPI 표 어디에도 이 disclosure가 없다.** `W2_BALANCE_WEIGHT=4`는 스크립트 전역 상수라 Fig.3/6의 **모든 그리드 점**(1/20, 1/10, 1/5, ..., 1/0 전부)에 똑같이 적용된다 — 즉 Fig.3 x축에 "1/20"이라고 써 있는 점은 실제로는 **1/80**을 푼 결과이고, "1/1"이라고 써 있는 점은 실제로 **1/4**를 푼 결과다. 그래프 라벨과 실제 계산이 전부 어긋나 있는데, 이를 밝히는 곳이 Fig.5/8 제목 한 군데뿐이다.

### 9.5 결론 — Fine Tune은 독립적 기여가 아니라, 이미 하던 W1/W2 스윕의 연장선

**"Fine Tune"이라는 별도 절(§5.2.2)로 다룰 만한 새로운 메커니즘이 아니다.** 실질적으로는 "이 프로젝트의 raw 계수 방식에서는 W1:W2=1:80 근방이 논문의 (1,20) 벤치마크와 가장 가깝게 재현된다"는 **탐색 결과**이고, 이는 Fig.3/6이 원래 하는 일(W1/W2 스윕으로 좋은 지점 찾기)과 방법론적으로 동일하다. 다만 그 탐색 결과를 "W2=20"이라는 논문 벤치마크 라벨 뒤에 숨겨서(Weight라는 곱수로) 보고하고 있다는 게 문제다.

### 9.6 보고서 수정 제안

1. §5.2.2 "문제 2" 서술: "논문 Eq.(9a)의 정규화를 따랐다" → **"논문에는 없는 denom/N 정규화를 자체적으로 추가했다가 문제가 생겼다"**로 정정.
2. 3.3절 KPI 표·본문에 "실제로는 (W1,W2)=(1,80)에 해당" 또는 "W2_eff=W2×4 적용됨"이라는 disclosure를 명시적으로 추가(방법2 Fine Tune, 방법6.2 둘 다 해당 — 둘은 §7에서 확인했듯 동일한 문제이므로 같은 disclosure가 적용된다).
3. Fig.3/Fig.6 제목에도 Fig.5/8처럼 "(W2_eff=W2×4 균형 보정)"을 동일하게 표시(x축 라벨이 실제 값과 다르다는 걸 밝히기 위해).
4. §5.2.2 절 제목 또는 도입부에 "이건 새 알고리즘이 아니라 W1/W2 비율 재탐색"이라는 걸 명시.

(2026-09-13 기준 아직 `실험결과_보고서_지원_m1.md`에는 미반영 — 사용자 확인 후 반영 예정.)

## 10. "regret vs profit-max" 등식이 Fig.3/5/6/8 전 지점에서도 성립하는가 — 전면 재검증

### 10.1 배경 — 왜 다시 검증했나

§7의 등식 검증(§7.2)은 **KPI 지점(W1=1,W2=20,rate=0.5) 하나**에서만 확인됐다. 그런데 이 결과를 근거로 보고서 전체를 재구성하는 안(§8)이 논의 중이었다 — 방법6.1을 "방법6"으로 승격, 방법6.2 삭제, 3.2.6절의 SPO+ 손실·ζ 수식 프레이밍 삭제, 3.1·3.3·5.6절도 6.1/6.2 구분 없이 병합. 이 재구성은 "SPO+ 손실 자체는 결과에 영향이 없다"는 전제 위에 서 있는데, 보고서에는 이 방법의 결과를 **Fig.3(AR W1/W2 10점)·Fig.5(AR rate 11점)·Fig.6(MLR W1/W2 10점)·Fig.8(MLR rate 11점)**, 즉 42개 sweep 지점 전체로 싣고 있다. KPI 지점 하나의 등식 확인만으로 42개 지점 전체를 대표하는 보고서 재구성을 진행하는 건 근거가 부족하므로, **모든 sweep 지점**에서 재검증했다.

### 10.2 방법

`temp_debug_spo_regret_vs_profitmax_full_sweep.py`로, 방법6.1의 실제 이익식(PC=rate·DA·I[DA>RT]+RT 무조건 포함)·정규화(raw+`W2_BALANCE_WEIGHT=4`)·MILP 구조를 고정한 채 두 정식화를 각각 구현했다:

- **(A) profit-max** — `방법6_SPO_base_AR_MLR.py`의 `solve_ar_spo_base`/`solve_mlr_spo_base` 그대로(oracle 없음, `sc`/`yc`에 경제항 직접 반영).
- **(B) regret(literal ζ)** — 보고서 3.2.6절이 서술하는 정식화를 문자 그대로 구현: 관측치별 변수 `ζ_i`를 새로 추가하고, 3후보 오라클 각각에 대해 부등식 제약 `ζ_i + DA_i·x_i + RT_i·y⁺_i − (RT_i+PC_i·I[DA>RT]_i)·y⁻_i ≥ oracle_k,i` (k=0,1,2)를 걸고, 목적함수를 `W1·Σζ_i + W2_eff·Σ|e_i|`로 바꿨다(경제항이 `sc`/`yc`가 아니라 `ζ_i`를 통해서만 들어간다는 점이 핵심적인 차이).

Fig.3/5/6/8에 실제로 쓰인 **42개 지점 전부**(AR: W1/W2 10점 + rate 11점, MLR: 동일)에서 두 버전을 각각 풀어, 학습된 β와 최종 nRMSE·Gap을 비교했다.

### 10.3 결과 — 42/42 지점에서 nRMSE·Gap 완전 일치

| Fig | 지점 수 | nRMSE·Gap 일치 | β까지 완전 일치(`maxΔβ<1e-6`) |
|---|---:|---|---|
| Fig.3 (AR, W1/W2 10점) | 10 | 10/10 | 9/10 |
| Fig.5 (AR, rate 11점) | 11 | 11/11 | 9/11 |
| Fig.6 (MLR, W1/W2 10점) | 10 | 10/10 | 10/10 |
| Fig.8 (MLR, rate 11점) | 11 | 11/11 | 11/11 |
| **합계** | **42** | **42/42** | **39/42** |

**nRMSE·Gap은 42개 지점 전부에서 소수점 4자리까지 완전히 일치했다**(모든 Δ가 `+0.0000`/`-0.0000`). 로그 발췌(대표 지점):

```
[Fig.3] AR 1/20  W1=1,W2=20,rate=0.5  maxΔβ=1.426e-13  nRMSE(pm=35.011,rg=35.011,Δ=+0.0000)  Gap(pm=14.063,rg=14.063,Δ=+0.0000)  [OK]
[Fig.3] AR 1/0   W1=1,W2=0,rate=0.5   maxΔβ=3.291e-13  nRMSE(pm=65.681,rg=65.681,Δ=-0.0000)  Gap(pm=10.959,rg=10.959,Δ=-0.0000)  [OK]
[Fig.5] AR 0%    W1=1,W2=1,rate=0.0   maxΔβ=2.043e-13  nRMSE(pm=46.604,rg=46.604,Δ=-0.0000)  Gap(pm=13.526,rg=13.526,Δ=+0.0000)  [OK]
[Fig.5] AR 100%  W1=1,W2=1,rate=1.0   maxΔβ=2.363e-13  nRMSE(pm=52.599,rg=52.599,Δ=-0.0000)  Gap(pm=12.249,rg=12.249,Δ=+0.0000)  [OK]
[Fig.6] MLR 1/20 W1=1,W2=20,rate=0.5  maxΔβ=5.551e-17  nRMSE(pm=24.017,rg=24.017,Δ=+0.0000)  Gap(pm=11.435,rg=11.435,Δ=+0.0000)  [OK]
[Fig.8] MLR 100% W1=1,W2=1,rate=1.0   maxΔβ=5.551e-17  nRMSE(pm=30.449,rg=30.449,Δ=+0.0000)  Gap(pm=11.608,rg=11.608,Δ=-0.0000)  [OK]
```

### 10.4 3개 지점의 `maxΔβ` — 유의미한 차이가 아니라 alternate optima

39/42는 β까지 1e-13~1e-23 수준(순수 수치오차)으로 일치했지만, 3개 지점만 조금 더 큰 편차가 나왔다:

```
[Fig.3] AR 1/2 W1=1,W2=2,rate=0.5: maxΔβ=5.6013e-06, ΔnRMSE=-0.0000, ΔGap=+0.0000
[Fig.5] AR 30% W1=1,W2=1,rate=0.3: maxΔβ=3.7865e-06, ΔnRMSE=+0.0000, ΔGap=-0.0000
[Fig.5] AR 90% W1=1,W2=1,rate=0.9: maxΔβ=8.2328e-06, ΔnRMSE=-0.0000, ΔGap=-0.0000
```

**이 3곳도 유의미한 차이가 아니다** — 판단 근거:

1. 차이 크기(1e-6대)가 β 자체의 크기(보통 0.01~수십)에 비해 무시할 수준이다.
2. **결정적으로, β가 미세하게 달라졌는데도 그 β로 만든 nRMSE·Gap은 완전히 동일하다.** 이는 두 β가 "테스트셋 예측값에는 영향을 주지 않는 방향"으로만 다르다는 뜻이다.
3. profit-max와 regret(ζ)은 변수·제약 개수가 다른 별개의 LP 완화 구조다(regret 쪽에 `ζ_i` 변수 n개 + 오라클 부등식 3n개가 더 있다). 목적함수 값이 동점(tie)인 지점에서 solver(HiGHS)가 서로 다른 LP 완화 구조 때문에 미세하게 다른 꼭짓점을 고른 것으로 보이며, 이는 **MILP의 동점 최적해(alternate optima)** 특징과 정확히 일치한다.

### 10.5 결론 — 등식이 깨지는 지점은 없다

42개 지점 전부(W1/W2 극단값인 1/0·20/1, rate=0%·100%까지 포함) "oracle이 β와 무관한 상수라 argmin이 같다"는 등식이 유지된다. **KPI 지점 하나의 우연이 아니라, sweep 전 구간에서 성립하는 구조적 등식임이 확인됐다.**

**진행 중인 보고서 재구성(방법6.1→"방법6" 승격, 3.2.6절의 SPO+ 손실·ζ 수식·"목적함수 비교" 표 삭제, 방법6.2 삭제, 3.1·3.3·5.6절 병합)을 지점별 예외 없이 그대로 진행해도 근거가 충분하다.**

---

## 부록 — 검증 스크립트

- `temp_debug_method1_vs_6base_compare.py` — 방법1(`paper_eq_ar_mlr_z03_block18.py`)과 방법6.1(`방법6_SPO_base_AR_MLR.py`)의 실제 목적함수 계수·학습된 β를 같은 데이터·같은 (W1,W2,rate)에서 직접 계산해 비교하는 디버깅 스크립트. §4의 수치는 이 스크립트의 실행 결과를 그대로 옮긴 것이다.
- `temp_debug_spo_denom_ablation.py` — SPO+ 4항식(PC=rate·DA·I[DA>RT])을 고정한 채 정규화 방식(denom / raw / raw+weight4)만 바꿔가며 z03/블록18에서 비교하는 디버깅 스크립트. §6.4의 수치는 이 스크립트의 실행 결과를 그대로 옮긴 것이다.
- `temp_debug_regret_vs_profit_exact_equivalence.py` — 같은 4항 이익식을 고정한 채 손실을 "regret(오라클 포함)" vs "순수 profit-max(오라클 없음)"로 명시적으로 다르게 구성해, 학습된 β가 정확히 동일한지 검증하는 디버깅 스크립트(KPI 지점 1개). §7.2의 수치는 이 스크립트의 실행 결과를 그대로 옮긴 것이다.
- `temp_debug_weight4_vs_w2_80_equivalence.py` — 방법2 Fine Tune의 (W1=1,W2=20,Weight=4)와 (W1=1,W2=80,Weight=1)이 목적함수 계수·이진변수 판정까지 완전히 동일한지 검증하는 디버깅 스크립트. §9.2~9.3의 수치는 이 스크립트의 실행 결과를 그대로 옮긴 것이다.
- `temp_debug_spo_regret_vs_profitmax_full_sweep.py` — §7.2의 KPI 지점 1개 검증을 Fig.3/5/6/8 전 지점(42개)으로 확장한 디버깅 스크립트. §10의 수치는 이 스크립트의 실행 결과(`temp_debug_spo_regret_vs_profitmax_full_sweep.log`)를 그대로 옮긴 것이다.
