# 실험결과 이력 2

`실험결과_이력.md`에 이어지는 작업 기록. 같은 용도(보고서 본문에는 안 넣는 "왜 이렇게 했는지" 히스토리)로, 새 항목은 항상 맨 아래에 날짜와 함께 추가한다.

---

## 2026-09-14 — 방법8(구 "방법2 Fine Tune")의 Gap 오라클 2후보 문제 검증

### 배경

사용자가 방법8(구 방법2 Fine Tune)의 `compute_gap_4term` 함수를 지적했다. 이 함수는 오라클을 `{commit=0, commit=실제}` 2후보만 쓰는데, 그 근거로 코드 주석에 "PC=RT+rate×DA에서는 rate=0이어도 PC=RT>0이라 commit=1.0(풀커밋)이 오라클이 될 수 없다"고 적혀 있다.

사용자의 수학적 반론: rate=0이면 PC=RT가 되어, 부족 구간 기울기(∂π/∂x = DA−PC = DA−RT)와 잉여 구간 기울기(∂π/∂x = DA−RT)가 완전히 같아진다(꺾이는 점이 없어짐). DA>RT인 시간대(이 데이터셋 대부분)에서는 이익함수가 x=0~1 전 구간에서 계속 증가하므로, x=1이 그 시간대의 진짜 오라클이어야 하는데 2후보 정의에서는 빠져 있다.

### 요청

1. 이 수학적 지적이 맞는지 직접 재계산해서 확인.
2. 맞다면 z03/블록18에서 DA>RT 시간대 비율, 3후보 오라클로 다시 계산하면 오라클 값이 얼마나 커지는지, 방법8의 Gap이 얼마나 나빠지는지 실제로 재계산.
3. 같은 2후보 로직이 §5.2.1(방법2 초기)의 baseline Gap 계산에도 쓰였는지 확인.
4. 방법1·3·4·5·6·7 등 다른 방법들의 Gap이 전부 3후보 오라클(표준)을 쓰는지, 방법8/방법2만 예외인지 확인 — 그렇다면 3.3절 KPI 표 전체가 서로 다른 잣대로 채점된 것.

### 검증 방법

`temp_debug_2cand_vs_3cand_oracle_method8.py`를 새로 작성해서: (1) 대수적으로 rate별 두 구간 기울기를 직접 계산, (2) z03/블록18 실제 테스트 데이터에서 DA/RT 비교, (3) `방법2_4term_ar_mlr_z03_block18.py`의 `solve_ar_proposed()`를 그대로 재현해 KPI 지점(rate=0.5, W1=1, W2=20→W2_eff=80)과 rate=0 지점, 그리고 Fig.5/8 rate 스윕 전 구간(0~100%, W1=W2=1→W2_eff=4)에서 실제 예측치를 얻어 2후보 vs 3후보 Gap을 직접 비교했다. 추가로 `integrated_paper_base_AR_MLR.py`(현재 방법1 소스), `방법7_additive_cvar_ar_mlr_z03_block18.py`의 오라클 정의도 코드로 직접 확인했다.

### 검증 결과

**① 수학적 지적 — 맞다.** 대수적으로 확인(DA=50, RT=20 예시): rate=0.0에서 surplus 기울기(DA−RT)=30.0, shortage 기울기(DA−PC)=30.0으로 정확히 같다(꺾임 없음). rate>0이어도 `rate < (DA−RT)/DA`인 동안은 shortage 기울기가 여전히 양수라 commit=1이 유리한 구간이 남는다 — rate=0만의 문제가 아니라 "낮은 rate 전반"의 문제다.

**② 실제 데이터 재계산**

- z03/블록18 테스트셋(360시간) 중 DA>RT: 226/360 = 62.8%.
- 방법8의 실제 예측치로 2후보 vs 3후보 Gap 비교:

| 지점 | 2후보 Gap | 3후보 Gap | 차이 | commit=1이 진짜 오라클인 시간대 |
|---|---:|---:|---:|---:|
| KPI(rate=0.5, W1=1,W2=80) | 14.55% | 14.55% | +0.00%p | 1/360 (0.3%) |
| rate=0.0 (Fig.5/8 왼쪽 끝) | 6.48% | 13.53% | **+7.04%p** | 226/360 (62.8%) |
| rate=0.1 | 7.90% | 11.62% | +3.72%p | 142/360 (39.4%) |
| rate=0.2 | 9.98% | 11.52% | +1.54%p | 77/360 (21.4%) |
| rate=0.3 | 10.65% | 11.02% | +0.37%p | 29/360 (8.1%) |
| rate=0.4 | 11.57% | 11.61% | +0.04%p | 4/360 (1.1%) |
| rate≥0.5 | 동일 | 동일 | ≈0.00%p | 0~1/360 |

**핵심**: 문제는 실재하고, rate=0에서 Gap을 6.48%→13.53%로 절반 이상 과소평가한다. 그런데 정확히 KPI 지점(rate=0.5)에서는 우연히 영향이 거의 0이다 — PC=RT+0.5DA가 충분히 커서 shortage 기울기가 거의 모든 시간대에서 음수가 되기 때문. 즉 **3.3절 KPI 표의 방법8 값(14.55%/11.30%)은 안전**하지만, **5.8절 Fig.5/8의 rate 0~40% 구간 곡선(baseline·proposed 둘 다 — 오라클 값 자체가 예측치와 무관하게 똑같이 왜곡되므로)은 실제보다 Gap이 낮게 그려져 있다.**

**③ §5.2.1(방법2 초기)** — 안전하다. 실제 소스인 `기본코드_(2)_.../model_proposed_ar_profit_change.py`(Gurobi 버전)는 훈련용 오라클과 평가용 Gap 오라클 둘 다 3후보(`max(profit_if_commit_zero, profit_if_commit_actual, profit_if_commit_full)`)를 일관되게 쓴다. 2후보로 줄인 건 그 이후 §5.2.2(=방법8)에서 새로 도입한 변경이다.

**④ 다른 방법들 확인 — 표 전체가 서로 다른 잣대로 채점되고 있다.**

| 방법 | Gap 오라클 | 근거 |
|---|---|---|
| 방법1 (현재, `integrated_paper_base_AR_MLR.py`) | **2후보** | 스크립트 docstring에 "논문 수식(Eq.13 2후보 오라클) 그대로"라고 명시 — 의도적. 훈련용 denom은 3후보인데 평가용 Gap만 2후보로 따로 감 |
| 방법2 (§5.2.1 초기) | 3후보 | 위 ③ 참고 |
| 방법8 (=구 방법2 Fine Tune) | **2후보** | 이번 검증 대상 |
| 방법3 | 3후보 | `compute_zhang_gap`에 `max(p0,pa,p1)` |
| 방법4 | 3후보 | 코드 주석에 "3후보 유지가 맞다"고 명시 |
| 방법5 | 3후보 | 방법4와 동일 근거 |
| 방법6 | 3후보 | `compute_gap_4term`에 `max(p0,pa,p1)` |
| 방법7 (전 조합) | **2후보** | docstring: "방법2와 완전히 동일한 Gap 정의... 방법7과 방법2를 직접 비교하려면 이 지표 정의가 같아야 한다" — 방법7×방법1, 방법7×방법3 조합에도 똑같이 적용됨(그 이익식은 방법2의 "PC≥RT" 구조가 없는데도) |

**추가 실측**: 방법1의 PC=0.5×DA는 rate와 무관하게 shortage 기울기가 항상 `0.5×DA`인데, z03/블록18 테스트셋에서 DA는 360시간 **전부(100%)** 양수(최솟값 18.13)라서, **모든 시간대에서 commit=1이 2후보 오라클에서 빠져 있다.** 방법8은 KPI 지점에서 0.3%만 영향받았지만 방법1은 100% 영향권 — 다만 정확한 Gap 재계산(방법1의 실제 예측치로 2후보 vs 3후보 비교)은 아직 하지 않았다.

### 결론 / 다음 단계

- 지적은 수학·실측 모두 확인됨.
- 3.3절 KPI 표의 방법8 값은 무사(영향 ≈0).
- 5.8절 Fig.5/8의 rate 0~40% 곡선은 실제로 왜곡돼 있음(최대 +7.04%p) — 재생성 필요할 수 있음.
- 방법1은 100% 시간대가 영향권이라 방법8보다 심각할 가능성 있으나 아직 재계산 안 함.
- **사용자에게 반영 여부·범위 확인 요청 중** — 아직 `실험결과_보고서_지원_m1_방법6쉉.md`에는 반영되지 않은 상태(이 문서는 검증 내용만 기록).

### 참고 스크립트

- `temp_debug_2cand_vs_3cand_oracle_method8.py` — 이번 검증에 쓴 스크립트. 대수적 확인 + z03/블록18 DA>RT 비율 + 방법8 실제 solve_ar_proposed 재현으로 KPI/rate=0/rate 스윕 전체의 2후보 vs 3후보 Gap 비교.

---

## 2026-09-14 (계속) — 실제 코드 수정 + 재검증 완료

사용자가 반영을 요청해서 실제로 고쳤다.

1. **`방법2_4term_ar_mlr_z03_block18.py`의 `compute_gap_4term` 수정**: `p1`(commit=1.0) 항을 추가해 `max(p0, pa, p1)` 3후보로 변경. 함수 docstring과 위쪽 설명 주석("2후보로 줄였다"는 부분)도 이번 검증 결과(수학적 근거가 틀렸음, z03/블록18 62.8% 영향)를 반영해 새로 썼다.
2. **재실행 검증**: 수정한 스크립트를 다시 돌려서 KPI 지점(rate=0.5, W1=1, W2=80)을 확인 — **AR 36.32%/14.55%, MLR 23.88%/11.30%로 수정 전과 소수점까지 완전히 동일**. 3.3절 KPI 표는 손댈 필요 없음.
3. **Fig.5(AR)·Fig.8(MLR) 재생성**: `results/방법2_fig5_ar_z03_block18.png`, `results/방법2_fig8_mlr_z03_block18.png`가 새로 저장됐고(Fig.3/6도 스크립트 특성상 같이 재생성되지만 rate=0.5 고정이라 수치 동일함을 확인), `results/방법8_fig{3,5,6,8}_..._z03_block18.png`로 다시 복사했다.
4. **배수(k) 스윕 재검증** (`temp_debug_weight_sweep_3cand_recheck.py`, k=1~15 전체, 3후보 Gap으로 재계산):

   | k | 1 | 2 | 3 | **4** | 5 | 6 | 8 | 10 | 12 | 15 |
   |---|---|---|---|---|---|---|---|---|---|---|
   | SSE(3후보) | 4511.5 | 980.1 | 411.2 | **395.8** | 456.1 | 596.7 | 1009.8 | 1092.9 | 1149.7 | 1233.1 |
   | SSE(2후보, 이전) | 4467 | 941 | 380 | 369 | 431 | 568 | 983 | 1070 | 1134 | 1220 |

   **k=4가 여전히 최적** (2위 k=3과의 격차는 좁아졌지만 — 411.2 vs 395.8 — 순서는 안 바뀜). 3.2.8·5.8절의 "4배가 최적"이라는 서술은 그대로 유지, SSE 표 숫자만 3후보 기준으로 교체.
5. **보고서 반영**: `실험결과_보고서_지원_m1_방법6쉉.md`의 3.2.8절·5.8절에서 "2후보" 서술과 그 틀린 근거를 지우고, 위 검증 결과(수학적 근거·62.8%·+7%p 왜곡·KPI 지점은 무사함)로 다시 썼다. SSE 표도 3후보 값으로 교체.

**발견된 추가 이슈 (아직 미반영)**: 5.9.3 순위표의 방법8 행 "전 지점 평균" 참고열(22.5%p = Fig.3 5.4+Fig.5 7.3+Fig.6 4.7+Fig.8 5.2)에서 Fig.5·Fig.8 성분이 새 CSV 기준으로 약간 바뀐다(Fig.5: 7.3→7.51, Fig.8: 5.2→5.96 — 대략적 재계산, 순위 자체는 안 바뀜). 사용자가 명시적으로 요청한 5가지 범위 밖이라 아직 손대지 않았고, 반영 여부를 물어본 상태.

