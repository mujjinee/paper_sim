# 시도방법 6 — SPO+ (Smart "Predict, then Optimize"+)

## 1. SPO+ 란?

**SPO+** 는 "예측을 잘하는 것"이 아니라 "예측을 바탕으로 내린 **결정을** 잘하는 것"을 가르침 목표로 삼는 방식이다.

기존 시도방법(1~5)이 **이익식(profit formula)** 을 고쳤다면, SPO+ 는 **목적함수(objective function)** 를 바꿨다. 이익식은 그대로 두고, solver 가 최소화하는 것을 "마이너스 이익"에서 "후회(ζ)"로 교체한다.

- **어디서 왔나**: Elmachtoub & Grigas, *Smart "Predict, then Optimize"*, Management Science 68(1), 2022 (arXiv:1710.08005)
- **중심 말**: decision-focused learning (결정 중심 가르침), prediction-then-optimization (예측-후-다듬기)

---

## 2. 기존 방식의 문제 — 왜 SPO+ 가 필요할까?

### 일상살림 예시로 살펴보기

예를 들어, 내일 날씨를 내다보고 우산을 챙길지 정해야 한다고 생각해보자.

**일반 내다봄 방식**은 "예측 비 vs 실제 비"의 차이를 작게 만들려고 한다.
- 예측: 60% 확률 → 실제: 50% → 차이 10%
- 예측: 40% 확률 → 실제: 50% → 차이 10%

두 예측의 틀린 정도는 같지만, **결정은 완전히 달라질 수 있다**.
- 60% 예측 → 우산 챙김 → 비 오지 않아도 우산 하나 지참
- 40% 예측 → 우산 안 챙김 → 비 오면 젖음

**"예측 틀린 정도가 같아도 결정의 결과가 다르다"** — 이 문제를 고치려 든 것이 SPO+ 다.

일반 방식은 목적함수가 "예측 차이"를 작게 만드는 것(MAE, RMSE)이고, SPO+ 는 목적함수를 "후회"를 작게 만드는 것으로 바꾼다.

### 태양광 발전 예시

우리 프로젝트에서도 마찬가지다.

- 예측 발전량 = 0.5 → 하루전에 50% 약정
- 예측 발전량 = 0.6 → 하루전에 60% 약정

예측 틀린 정도가 0.1 차이나는데, 이 0.1 차이가 **돈으로 얼만큼 차이 나는가**는 가격(DA, RT)과 벌금(PC)에 따라 완전히 다르다.

일반 방식은 "예측 차이 0.1"을 모두 똑같이 대접하지만, SPO+ 는 "그 예측 차이가 **돈으로 얼만큼 손해 보는가**"를 직접 가르침에 넣는다.

---

## 3. SPO+ 의 중심 아이디어 — "후회(regret)" 줄이기

### 후회(ζ) 란?

```
후회(ζ) = 가장 잘 번 이익(oracle) − 내 예측으로 번 이익(realized)
```

**말끔히 풀어쓰면:**
- oracle(모든 것을 아는 존재): 내일 발전량을 정확히 알고 있어서 가장 많이 버는 약정량을 고름
- 나(예측 방식): 내일 발전량을 내다보고 그 예측값으로 약정

후회 = "모든 것을 아는 존재보다 얼만큼 덜 버는가"

### 후회의 3가지 후보

oracle 은 세 가지 약정량 중 가장 많이 버는 것을 고른다:

| 후보 | 약정량(x) | 언제 많이 버는가? |
|---|---|---|
| ① commit = 0 | 발전 안 함 | 가격이 매우 낮을 때 |
| ② commit = actual | 실제 발전량만큼 | 대부분 이 경우 |
| ③ commit = 1.0 | 설비 최대로 | 부족분이 나도 벌금 떼고 봐라 더 벌 때 |

각 본 것 i 에 대해, 3가지 후보 중 **가장 큰 이익**이 oracle 이다.

### SPO+ 규칙

```
ζ_i ≥ oracle_profit_①,i − realized_profit_i
ζ_i ≥ oracle_profit_②,i − realized_profit_i
ζ_i ≥ oracle_profit_③,i − realized_profit_i
ζ_i ≥ 0
```

3개의 크고작은식이 있으므로, solver 는 **가장 큰 차이**(= 가장 후회 많은 후보)를 고른다.

**예시:**
```
어떤 시간대 i 에서:
  actual 발전량 = 0.3, DA = 50, RT = 40, PC = 25(=0.5×50)
  예측 = 0.2 라고 하자

  realized(내 이익):
    mismatch = 0.3 − 0.2 = 0.1 (surplus)
    profit = 50×0.2 + 40×0.1 = 14

  oracle 후보 ① (commit=0):  profit = 40×0.3 = 12
  oracle 후보 ② (commit=actual): profit = 50×0.3 = 15
  oracle 후보 ③ (commit=1.0): profit = 50×1.0 + 40×(0.3−1.0) − 25×(1.0−0.3) = 50−28−17.5 = 4.5

  후회:
    ζ ≥ 12 − 14 = −2  (후보 ①: oracle 이 나보다 더 못 함 → 무시)
    ζ ≥ 15 − 14 =  1  (후보 ②: oracle 이 1 더 많이 번다)
    ζ ≥ 4.5 − 14 = −9.5 (후보 ③: 무시)

  → ζ = max(−2, 1, −9.5, 0) = 1

  후회가 1 이다. oracle(후보 ②)이 15 번데 나는 14 만 번다.
  SPO+ 는 이 1 을 작게 만들려고 한다.
```

---

## 4. 시도방법6(SPO+) vs 시도방법2(4항식) 비교

### 두 방식의 셈식 나란히 놓기

#### 시도방법2 (4항식)

```
작게 만들 것  W1/denom × Σ_i [ −(DA·x + RT·surplus − RT·shortage − PC·shortage)_i ]
                 + W2/n × Σ_i (yp_i + ym_i)

규칙:
  x_i = X_i @ β                    (예측값 = 회귀식)
  x_i + yp_i − ym_i = actual_i     (풀어쓰기: surplus/shortage)
  yp_i ≥ 0, ym_i ≥ 0               (마이너스 수 아님)
```

**한 줄 요약**: "이익식을 고쳐서(−RT·shortage 항 추가) 올바른 이익을 최대로 해라"

#### 시도방법6 (SPO+)

```
작게 만들 것  W1/n × Σ_i ζ_i + W2/n × Σ_i |e|_i

규칙:
  x_i = X_i @ β                    (예측값 = 회귀식)
  x_i + yp_i − ym_i = actual_i     (풀어쓰기: surplus/shortage)
  ζ_i ≥ oracle_profit_①,i − realized_profit_i   (후회 상한 ①)
  ζ_i ≥ oracle_profit_②,i − realized_profit_i   (후회 상한 ②)
  ζ_i ≥ oracle_profit_③,i − realized_profit_i   (후회 상한 ③)
  ζ_i ≥ 0                           (후회는 마이너스 수 아님)
```

**한 줄 요약**: "oracle 과의 뒤처짐(후회)을 바로 재어서 작게 해라"

### 중심 차이 — 시도방법2 는 이익식 고침, 시도방법6 은 목적함수 교체

| | 시도방법2(4항식) | 시도방법6(SPO+) |
|---|---|---|
| **바꾸는 대상** | **이익식(profit formula)** | **목적함수(objective function)** |
| **무엇을 작게 하나?** | 마이너스 이익 `−profit` | 후회 상한 `ζ = oracle − realized` |
| **oracle 정보를 쓰나?** | 쓰지 않음 (denom 으로 크기만 맞춤) | **씀** (3후보 oracle 바로 셈) |
| **어떤 성격인가?** | "좋은 예측"을 바로 가르침 | "가장 잘 한 예측과의 차이"를 가르침 |
| **말끔히** | "좋은 점수 받으세요" | "완점자와의 차이 줄이세요" |

### 중심 차이 — 규칙 개수

| | 시도방법2(4항식) | 시도방법6(SPO+) |
|---|---|---|
| 같은식 규칙 | 2×n_obs | 2×n_obs |
| 크고작은식 규칙 | 없음 | **3×n_obs (SPO 후회 상한)** |
| ζ ≥ 0 규칙 | 없음 | **n_obs** |
| 총 규칙 | **2×n_obs** | **5×n_obs** (2.5배 더 많음) |

### W2 항 — 사실 같음

```
|e|_i = |actual − prediction| = yp_i + ym_i
```

surplus(남은 것)와 shortage(모자란 것)를 더하면 바로 틀린 것과 같으므로, **W2 항은 두 방식 모두 MAE(平均절대틀린 것)를 작게 만든다**. 넣는 자리가 다를 뿐 셈으로 보면 같다.

---

## 5. 프로젝트에서 어떻게 썼나?

### 스크립트 파일

| 파일 | 설명 |
|---|---|
| [spo_plus_3term.py](spo_plus_3term.py) | 3항 식 + SPO+ (z03/블록18) |
| [integrated_spo_plus_4term_fig3568_AR_MLR.py](integrated_spo_plus_4term_fig3568_AR_MLR.py) | **4항 식 + SPO+** (z01/블록21, AR+MLR 한데) |
| [spo_plus_ar_mlr_z01_block21.py](spo_plus_ar_mlr_z01_block21.py) | SPO+ AR/MLR (z01/블록21) |

### 들어가는 값 꾸림 (SPO+ MILP)

```
들어가는 값:
  β        — 회귀 곱하는 수 (기본값 + lag 12개 = 13개)
  x_i      — 예측값 (= X_i @ β), i = 1..n_obs
  yp_i     — surplus (나머지 발전), i = 1..n_obs
  ym_i     — shortage (모자란 발전), i = 1..n_obs
  ζ_i      — SPO+ 후회 상한, i = 1..n_obs

작게 할 것: min W1/n × Σζ_i + W2/n × Σ|e|_i

규칙:
  ① x_i = X_i @ β                    (AR 연결)
  ② x_i + yp_i − ym_i = actual_i     (풀어쓰기)
  ③ ζ_i ≥ oracle_①,i − realized_i    (후회 상한)
  ④ ζ_i ≥ oracle_②,i − realized_i    (후회 상한)
  ⑤ ζ_i ≥ oracle_③,i − realized_i    (후회 상한)
  ⑥ ζ_i ≥ 0                          (마이너스 수 아님)
```

### 해 나가는 순서

1. 가르칠 데이터 준비 (90일 × 12시간 = 360 본 것)
2. oracle 셈 (각 본 것별로 3후보 이익 셈 → 가장 큰 값)
3. MILP solver (scipy milp, HiGHS 엔진, mip_rel_gap=1e-9)
4. 회귀 곱하는 수(β) 뽑아내기 → 테스트 데이터 예측
5. nRMSE, Gap打分해보기

---

## 6. 실험 줄어든 것

### 3항 식 + SPO+ (z03/블록18)

| W1/W2 | nRMSE | Gap | 논문값 |
|---|---|---|---|
| 1/20 | 60.35% | 19.41% | 34.89% / 13.91% |

**본 것:**
- Gap 이 기존 3항 MILP 의 **−30% 마이너스에서 양수(+17~21%)로 완전히 달라짐**
  - 이유: SPO+ 가 후회 상한을 가르침 목표에 직접 넣으므로, realized 가 oracle 을 넘는 구조가 깨짐
- 하지만 nRMSE 가 60% 대로 논문보다 **+25.5%p** 벌어짐
- commit=1.0 몰림 줄이기(10~27%) — 기존 3항(100% 몰림)보다 낫지만 근본 고침은 아님

### 4항 식 + SPO+ 한데 (z01/블록21)

| 방식 | nRMSE | Gap |
|---|---|---|
| AR | 48.93% | 16.23% |
| MLR | 43.03% | 14.78% |

**본 것:**
- 4항식과 SPO+ 를 한데해도 nRMSE 가 가장 크게 벌어짐
- z01/블록21 이라 z03/블록18 과 바로 비교하기 어려움

---

## 7. 살펴보기 — SPO+ 의 좋고 아픈 점

### ✅ 좋은 점

1. **Gap 부호 문제 고침**: 기존 3항 식에서 Gap 이 마이너스(−30%)였던 것이 SPO+ 로 양수(+17~21%)로 달라짐
2. **셈으로 확인된 약속**: Elmachtoub & Grigas(2022)가 증명했듯, SPO+ loss 는 결정을 내리며 잃는 것의 상한이므로, 이를 작게 하면 실제 운영 gap 도 줄어듦
3. **W1/W2 크기 문제 돌아넘기**: 두 항의 실제 단위가 달라도, 후회 자체(ζ)가 서로비교 값이라 크기 안 맞음 영향을 덜 받음

### ❌ 아픈 점

1. **이익식 틀이 틀리면 안 됨**: 3항 식의 근본적 빈틈(`DA − PC > 0`)은 SPO+ 만으로 해결 안 됨. 이익식 자체(4항으로)를 먼저 고쳐야 함
2. **넣는 것이 빙글빙글**: 기존 방법2 보다 **2.5배 더 많은 규칙** (3×n_obs 개의 SPO 크고작은식 추가)
3. **nRMSE 가 크게 벌어짐**: 예측 맞게 하는 것과 운영 잘 하는 것 사이 둘 중 하나를 골라야 하는데, 그 사이가 큼. "결정은 낫지만 예측은 엉망"인 상태
4. **셈량**: 규칙이 2.5배 많으니 MILP solver 도 그만큼 오래 걸림

---

## 8. 한눈에 정리 — 시도방법 1~6 비교

| 방법 | 중심 아이디어 | W1 항 | W2 항 | 줄어든 것 |
|---|---|---|---|---|
| **1 (논문 그대로)** | 3항 이익식 글자 그대로 | `−(DA·x + RT·surplus − PC·shortage)` | MAE | ❌ 재현 안 됨 (nRMSE 116% 무너짐) |
| **2 (4항식)** | 3항에 `−RT·shortage` 추가 | `−(DA·x + RT·surplus − RT·shortage − PC·shortage)` | MAE | ✅ 가장 가까움 (nRMSE +0.8%p) |
| **3 (Zhang)** | surplus/shortage 가격 다르게 | surplus = RT, shortage = c×RT (c>1) | MAE | ✅ 방법2 와 비슷 (nRMSE +0.8%p) |
| **4 (PC=max)** | 벌금가를 max(DA,RT) 로 | PC = rate × max(DA,RT) | MAE | △ rate=0.9 에서 괜찮음 |
| **5 (PC=DA+RT)** | 벌금가를 (DA+RT) 로 | PC = rate × (DA+RT) | MAE | △ rate=0.7 에서 괜찮음 |
| **6 (SPO+)** | 후회 상한 가르침 | `ζ ≥ oracle − realized` (3후보) | MAE | △ Gap 부호 고침, nRMSE 큼 |

**방법 1~5** 는 "이익식(profit formula)을 어떻게 짜라" 가 중심.
**방법 6(SPO+)** 은 "목적함수(objective function)를 어떻게 짜라" 가 중심.

→ 서로 다른 층의 고침이며, **이익식이 올바르면(SPO+ 없이도 잘 됨), 이익식이 틀리면(SPO+ 만으로 안 됨)** 이라는 줄에 도착했다.

→ **틀이 올바르면 방식이 중요하지 않다**. 방법2/3 으로 이익식 틀을 고치면, SPO+ 없이도 충분하다.

---

## 9. SPO+ 가 필요한 자리 vs 아닌 자리

### SPO+ 가 쓸모 있는 경우
- 예측 틀린 것(RMSE/MAE)이 작아도 결정 품위가 나쁜 경우 — 예측이 "가운데를 맞추려다" 끝자락 값을 놓칠 때
- 결정 방식이 직선식이 아닌 경우 — threshold 기반 결정(예: "가격이 X 넘으면 약정")
- 한쪽으로 쏠린 비용 — 예측 틀림의 방향에 따라 손실이 고르지 않을 때

### SPO+ 가 필요 없는 경우
- **이익식이 올바르고 MAE 와 결정을 한데에 잘 맞출 때** (우리 프로젝트의 방법2/3 이 바로 이 경우)
- 결정 방식이 간단한 경우 — 예측값을 그대로 약정량으로 쓰는 경우
- 데이터가 모자랄 때 — SPO+ 는 규칙이 많아서 지나치게 맞출 가능성이 높음

---

## 10. 관련 참고 글

| 글 | 쓴 이 | 해 | 내용 |
|---|---|---|---|
| Smart "Predict, then Optimize" | Elmachtoub & Grigas | 2022 (Management Science) | **SPO/SPO+ loss** — 결정 중심 가르침 |
| Oracle Inequalities for Prediction | Elmachtoub & Grigas | 2017 (arXiv:1710.08005) | SPO+ 의 셈 기반 |
| Operational Forecasting | Wang et al. | 2022+ | bilevel KKT 기반 결정 중심 |
| Improving Sequential Market Coordination... | Zhang et al. | 2024 | 프로젝트의 방법3 — 올리는/내리는 맞춤 가격 |

---

## 11. 줄

SPO+ 는 "예측 방식의 가르침 목표 자체를, 예측 틀린 것이 아니라 결정 품위(후회)로 바꾸자"는 아이디어다. 셈으로 보면 아름답지만, 우리 프로젝트에서는 다음과 같은 줄에 도착했다:

> **이익식 틀(4항)이 먼저 올바르면, SPO+ 없이도 방법2/3 로 충분히 잘 다시 뽑는다.**
> **이익식 틀이 틀리면(3항), SPO+ 만으로 해결되지 않는다.**
>
> 시도방법6(SPO+)은 **목적함수**를 바꾼 것이다. 시도방법2/3 은 **이익식 틀**을 고친 것이다.
> 목적함수를 바꿔도 틀이 기울어 있으면 solver 방향이 이미 왜곡되어 있다.
> **틀이 올바르면 방식이 중요하지 않다**는 뜻이다.

### 시도방법3(Zhang) 관점에서 본 시도방법6(SPO+)

| | 방법3 (Zhang) | 방법6 (SPO+) |
|---|---|---|
| **바꾸는 층** | 이익식(profit formula) | 목적함수(objective function) |
| **비유** | "점수 계산 규칙" 고침 | "점수 계산은 그대로, 가르침 방식" 바꿈 |
| **nRMSE** | 35.68% (논문과 +0.8%p) | 60.35% (논문과 +25.5%p) |
| **결론** | ✅ 틀 고치기 → 충분 | ❌ 방식만 바꾸기 → 틀 빈틈 커버 못 함 |

**요약**: 방법3/방법2 로 이익식 틀을 고치면, 목적함수를 건드리지 않아도 solver 가 자연스럽게 최적점을 찾는다. SPO+ 는 틀이 올바르고, 예측과 결정이 맞지 않을 때锦上添花(화려한 곳에 수정동)로 쓸 만한 방식이다.