# 시도방법 7 — Additive CVaR 확장

## 1. 연구 배경

재생에너지 발전사는 실제 발전량과 실시간 전력가격을 알기 전에 하루 전 시장에 발전량을 약정해야 한다. 약정량이 실제 발전량과 정확히 일치하지 않으면 surplus 또는 shortage가 발생하고, 그 경제적 결과는 day-ahead 가격, real-time 가격 및 shortage penalty에 따라 달라진다.

따라서 발전량 예측에서는 단순히 실제 발전량과 예측값의 차이를 줄이는 것뿐 아니라, 그 예측값을 실제 약정량으로 사용했을 때 발생하는 경제적 손실도 함께 고려할 필요가 있다. 예측오차의 크기가 같더라도 가격조건에 따라 경제적 손실은 크게 달라질 수 있기 때문이다.

기존 APEN 모형은 예측오차와 경제적 목적을 동시에 반영하여 예측계수를 학습한다. 그러나 nRMSE와 optimality gap은 전체 테스트 기간의 결과를 하나의 값으로 요약한다. 그러므로 전체적인 성능이 개선되더라도 일부 날짜나 시간대에서 발생하는 큰 손실이 집계값 속에 가려질 수 있다.

본 연구에서는 이러한 한계를 확인하기 위해 시간대별 예측오차와 경제적 regret을 분석하였다. 그 결과 전체 nRMSE 또는 optimality gap이 개선되더라도 일부 시간대에서 성능이 악화되거나, 예측 약정량이 설비 상한에 포화되면서 고가격 시점에 큰 shortage 손실이 발생할 수 있음을 확인하였다.

이에 특정 시간대를 직접 보정하는 대신 전체 학습표본 중 경제적 손실이 큰 꼬리 구간을 관리하기 위해 CVaR 기반 학습을 도입하였다.

---

## 2. 분석에 사용하는 기호

| 기호 | 의미 |
|---|---|
| t | 하나의 관측시점. 날짜와 시간대의 조합을 의미함 |
| d | 날짜 인덱스 |
| h | 시간대 인덱스. 본 실험에서는 9시부터 20시까지 12개 시간대 |
| N | 해당 최적화 문제에 포함된 학습표본 수 |
| Sₜ | 시점 t의 실제 발전량. 설비용량 대비 비율로 정규화된 값 |
| Ŝₜ | 예측모형이 산출한 발전량 예측값 |
| xₜ | day-ahead 시장에 제출하는 약정량 |
| eₜ | 시점 t의 예측오차 |
| yₜ⁺ | 실제 발전량이 약정량보다 많은 surplus |
| yₜ⁻ | 약정량이 실제 발전량보다 많은 shortage |
| DPₜ | 시점 t의 day-ahead 가격 |
| RPₜ | 시점 t의 real-time 가격 |
| PCₜ | 시점 t의 shortage penalty 단가 |
| c | Zhang 수익식에서 shortage 가격의 비대칭 정도를 정하는 계수 |
| K | 설비용량과 시간길이를 곱한 규모계수 |
| πₜ | K를 곱하지 않은 단위 규모의 수익 |
| Pₜ | K를 반영한 실제 화폐 단위의 실현수익 |
| Pₜ* | 실제 발전량과 가격을 사후적으로 알고 선택한 oracle 수익 |
| Rₜ | 화폐 단위의 시점별 economic regret |
| rₜ | 학습 목적함수의 단위에 맞추어 K로 나눈 regret |
| OG | optimality gap |
| α | CVaR 신뢰수준 |
| ζ | CVaR 선형화에서 VaR 역할을 하는 손실 문턱값 |
| uₜ | 시점 t의 regret이 ζ를 초과하는 정도 |
| λ | CVaR 항의 가중치 |
| W₁ | 경제적 손실항의 가중치 |
| W₂ | 예측오차항의 가중치 |

본 모형에서 예측값은 그대로 시장 약정량으로 사용되므로 다음 관계가 성립한다: **xₜ ≡ Ŝₜ**

예측오차는 다음과 같이 정의한다: **eₜ = Sₜ − Ŝₜ = Sₜ − xₜ**

---

## 3. 발전량 불일치와 surplus·shortage의 정의

실제 발전량 Sₜ와 약정량 xₜ의 차이는 surplus와 shortage로 분해된다.

```
yₜ⁺ = max(Sₜ − xₜ, 0)
yₜ⁻ = max(xₜ − Sₜ, 0)
xₜ + yₜ⁺ − yₜ⁻ = Sₜ
```

경우를 나누어 보면 다음과 같다:
- Sₜ > xₜ이면 surplus가 발생. yₜ⁺ = Sₜ − xₜ, yₜ⁻ = 0
- Sₜ < xₜ이면 shortage가 발생. yₜ⁺ = 0, yₜ⁻ = xₜ − Sₜ
- Sₜ = xₜ이면 surplus와 shortage가 모두 0

모형에는 surplus와 shortage가 같은 시점에 동시에 양수가 되지 않도록 이진변수와 제약조건을 추가하였다. 코드에서는 zₜ=0일 때 surplus만 허용하고, zₜ=1일 때 shortage만 허용한다.

```
yₜ⁺ + zₜ ≤ 1
yₜ⁻ − zₜ ≤ 0
zₜ ∈ {0, 1}
```

Sₜ와 xₜ가 0과 1 사이로 정규화되어 있기 때문에 yₜ⁺와 yₜ⁻의 상한도 1로 둘 수 있다.

이 제약이 적용되면 절대예측오차는 다음과 같이 나타낼 수 있다:

```
|Sₜ − xₜ| = yₜ⁺ + yₜ⁻
```

---

## 4. 세 가지 수익식

### 4.1 공통적인 수익 구조

```
πₜ(xₜ) = DPₜ·xₜ + RPₜ·yₜ⁺ − Cₜ⁻·yₜ⁻
Pₜ(xₜ) = K·πₜ(xₜ)
K = CAPACITY_MW × DURATION_HOURS (현재 30MW × 1시간 = 30)
```

### 4.2 원본 3항 수익식

```
πₜ⁽³⁾(xₜ) = DPₜ·xₜ + RPₜ·yₜ⁺ − PCₜ·yₜ⁻
PCₜ = PENALTY_RATE × DPₜ
Cₜ⁻ = PCₜ
```

### 4.3 수정 4항 수익식

```
πₜ⁽⁴⁾(xₜ) = DPₜ·xₜ + RPₜ·yₜ⁺ − (PCₜ + RPₜ)·yₜ⁻
Cₜ⁻ = PCₜ + RPₜ
```

RPₜ가 급등하면 shortage 비용도 함께 증가한다. 고가격 시간대의 과다약정에 더 강한 경제적 불이익이 부과된다.

### 4.4 수정 3항 비대칭 수익식 (Zhang)

```
πₜ⁽ᶻ⁾(xₜ) = DPₜ·xₜ + RPₜ·yₜ⁺ − c·RPₜ·yₜ⁻
Cₜ⁻ = c·RPₜ
```

c가 클수록 shortage에 대한 비대칭적 비용이 강해진다.

---

## 5. Oracle profit, 시점별 regret 및 optimality gap

### 5.1 Oracle profit

```
Pₜ* = max{Pₜ(0), Pₜ(Sₜ), Pₜ(1)}
```

약정량 후보를 세 가지로 둔다:
- xₜ = 0: day-ahead 시장에는 약정하지 않고 실제 발전량을 real-time 시장에 판매
- xₜ = Sₜ: 실제 발전량과 정확히 같은 양을 약정
- xₜ = 1: 설비용량 전체를 약정

### 5.2 시점별 economic regret

```
Rₜ = Pₜ* − Pₜ(xₜ)
```

Rₜ = 0이면 예측모형의 약정량이 해당 시점에서 oracle과 같은 수익을 얻었다. Rₜ > 0이면 예측모형이 oracle보다 Rₜ만큼 적은 수익을 얻었다.

### 5.3 Total regret

```
R_total = Σ_{t∈T_test} Rₜ
```

### 5.4 Optimality gap

```
OG = 100 × (Σ_{t∈T_test} Rₜ) / (Σ_{t∈T_test} Pₜ*)
```

### 5.5 nRMSE

```
nRMSE = 100 × RMSE / S̄
```

### 5.6 다섯 지표의 의미 (쉽게 풀어서)

| 지표 | 측정하는 것 | 일상 언어 |
|---|---|---|
| nRMSE | 예측 정확도 | "내일 발전량을 얼만큼 맞게 내다보나?" |
| OG (Optimality Gap) | 전체 이익 손실 비율 | "전지전능한 존재 대비 이익을 얼만큼 덜 버나?" |
| Total Regret | 전체 이익 손실 금액 | "전체적으로 얼만큼의 돈이 손해를 보나?" |
| CVaR90 | 꼬리위험 (상위 10% 평균) | "가장 나쁜 날들 평균 얼만큼 손해 보나?" |
| Maximum Regret | 최악单日 손실 | "가장 나쁜 하루에 얼만큼 손해 보나?" |

**nRMSE** — 예측값과 실제 발전량의 차이를 실제 발전량 평균으로 나눈 백분율.
- 예시: 평균 발전량이 0.5 인데, 예측이 평균 0.06 정도 틀렸다면: `0.06 / 0.5 × 100 = 12%`
- **작을수록 좋음** — 예측이 실제 발전량에 가까움

**Optimality Gap** — oracle 이익 대비 실현 이익 손실의 비율.
- 예시: oracle 이 벌 수 있는 이익 1,000원, 우리 모형이 본 이익 870원 → `OG = (1000 − 870) / 1000 × 100 = 13%`
- **작을수록 좋음** — oracle 에 가까운 결정 내림

**Total Regret** — 전체 테스트 기간의 regret 합계 (화폐 단위).
- 예시: 시점1 regret 20 + 시점2 regret 10 + 시점3 regret 20 = `Total Regret = 50`
- **작을수록 좋음** — 전체 손실 금액이 적음

**CVaR90** — regret 이 큰 상위 10% 표본의 평균.
- 예시: 360 시점 중 regret 이 큰 상위 36개를 고르고, 그 평균을 냄
- **작을수록 좋음** — 꼬리위험 (극단적 손실) 이 통제됨

**Maximum Regret** — 테스트 기간에서 가장 큰 단일 regret 값.
- 예시: 시점별 regret: 20, 10, 5, 8, ..., 300, ..., 15 → `Maximum Regret = 300`
- **작을수록 좋음** — 최악单日 손실이 작음

**CVaR 의 목표** — OG 와 Total Regret 은 유지하면서 CVaR90 과 Maximum Regret 을 줄이는 것. 평균 손실은 그대로 두고, **꼬리만 자르는** 게 목적이다.

---

## 6. 기존 APEN 목적함수

```
min W₂ Σ|Sₜ − xₜ| + W₁ Σ rₜ
```

여기서 rₜ는 학습 목적함수의 단위에 맞게 정규화된 regret이다.

```
rₜ = Rₜ / K = πₜ* − πₜ(xₜ)
```

---

## 7. CVaR 도입 전 시간대별 진단

### 7.1 최초 진단 대상

| 항목 | 조건 |
|---|---|
| 데이터 | Zone 3 |
| 블록 | 블록 18 |
| 학습기간 | 2013-08-25~2013-11-22, 90일 |
| 테스트기간 | 2013-11-23~2013-12-22, 30일 |
| 예측모형 | Baseline AR, Proposed AR |

### 7.2 시간대별 optimality gap 변화

| 시간 | 원본 3항 | 수정 4항 | Zhang |
|---|---|---|---|
| 9시 | −11.05 | −7.23 | −8.84 |
| 10시 | +0.16 | −0.64 | −0.33 |
| 11시 | +1.88 | +2.54 | +3.25 |
| 12시 | −4.02 | +0.93 | +0.37 |
| 13시 | −0.06 | −0.55 | −0.67 |
| 14시 | +0.18 | −0.41 | −0.45 |
| 15시 | −0.95 | −0.77 | −0.58 |
| 16시 | −0.92 | +1.52 | +0.18 |
| 17시 | −5.53 | −3.64 | −1.86 |
| 18시 | +0.94 | −0.37 | −0.37 |
| 19시 | −2.83 | −1.19 | −1.19 |
| 20시 | −0.13 | −0.53 | −0.58 |

11시는 세 수익식 모두에서 공통으로 악화된 시간대였다.

### 7.3 11시 약정량의 상한 포화

| 수익식 | Proposed AR의 x=1 발생 | Baseline AR의 x=1 발생 |
|---|---|---|
| 원본 3항 | 3/30일 | 3/30일 |
| 수정 4항 | 14/30일 | 3/30일 |
| Zhang | 17/30일 | 3/30일 |

### 7.4 Mean regret과 CVaR90의 차이

| 수익식 | Mean regret 변화 | CVaR90 regret 변화 | CVaR/Mean 배율 |
|---|---|---|---|
| 원본 3항 | +22.7 | +63.2 | 2.8배 |
| 수정 4항 | +26.8 | +121.9 | 4.5배 |
| Zhang | +34.3 | +206.0 | 6.0배 |

평균손실에서도 11시의 악화가 나타났지만, 최악의 10%를 보는 CVaR90에서는 그 크기가 훨씬 크게 나타났다.

---

## 8. 특정 시간대 보정 대신 CVaR를 선택한 이유

다른 계절의 블록에서도 "전체 지표는 개선되지만 일부 구간은 악화된다"는 현상이 반복되었으나 문제가 발생하는 정확한 시간대는 데이터 블록에 따라 달랐다. 따라서 특정 시간대를 지정하지 않고, 어느 날짜와 시간대에서 발생하든 큰 regret에 추가적인 가중치를 주는 CVaR가 더 적절한 보완방법으로 판단되었다.

---

## 9. CVaR의 정의

### 9.1 손실 분포의 모양

손실 값(리그렛) 을 막대로 그리면 왼쪽에 많이 모여 있고 오른쪽으로 드물지만 매우 큰 값이 꼬리처럼 늘어진다:

```
손실 크기 →
0    20    40    60    80   100   150   200
████████████████▇▇▇▅▅▃▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▅
            ↑                      ↑
          VaR90=60              CVaR90=101
         (90번째 값)          (상위 10개 평균)
```

### 9.2 VaR와 CVaR

숫자로 보면 이해가 쉽다. 학습 표본이 100개 있고, 손실 값을 작은 순으로 정리하면:

```
1~90번째:  손실이 작다 (10~50 사이)
91번째:    60
92번째:    65
93번째:    70
94번째:    75
95번째:    80
96번째:    90
97번째:    100
98번째:    120
99번째:    150
100번째:   200  ← 최악
```

**VaR90 = 60** — 손실을 작은 순으로 90% 지점의 값. "90% 는 손실이 60 이하다" 만 알려줌. 문턱을 넘은 것이 200 인지도 모름.

**CVaR90 = (60+65+70+75+80+90+100+120+150+200) / 10 = 101** — 상위 10%(10개) 의 평균. **꼬리가 얼마나 큰지** 알려줌.

### 9.3 CVaR 수식

```
CVaR_α(r) = ζ + (1 / ((1−α)N)) Σ (rₜ − ζ)₊
```

α=0.9일 때 CVaR90은 직관적으로 손실이 큰 상위 10% 표본의 평균이다.

### 9.4 ζ가 VaR 역할을 하는 이유

ζ를 너무 낮게 설정하면 많은 표본의 rₜ−ζ가 양수가 되어 초과손실 합계가 커진다. 반대로 ζ를 너무 높이면 초과손실은 줄어들지만 ζ 자체가 커진다. 최적화 과정에서 이 두 효과가 균형을 이루는 지점이 손실분포의 α분위수에 대응한다.

### 9.5 선형화를 위한 초과손실 변수

`max(rₜ − ζ, 0)` 는 비선형이므로 MILP solver 가 풀 수 없다. 보조 변수 uₜ 로 선형화한다:

```
uₜ ≥ rₜ − ζ    (uₜ 는 rₜ − ζ 보다 같거나 크다)
uₜ ≥ 0          (uₜ 는 음수가 아니다)
```

최소화 문제이므로 solver 가 `uₜ = max(rₜ − ζ, 0)` 를 자동으로 고름.

---

## 10. 최초 CVaR 정식화

### 10.1 초기 가중평균 방식

```
min W₂ Σ|Sₜ−xₜ| + W₁ [(1−λ) Σ rₜ + λN·CVaR_α(r)]
```

---

## 11. 초기 CVaR 구현에서 정비한 사항

- 수익식별 shortage 단가 일치
- 30MW 규모계수 제거
- 평가 지표 추가

---

## 12. 초기 가중평균 방식에서 발견된 문제

초기 식의 핵심 문제는 λ가 증가할 때 CVaR 항만 커지는 것이 아니라 기존 total regret 항의 가중치가 함께 감소한다는 점이다.

| λ | 기존 total regret 계수 (1−λ) | CVaR 계수 λ |
|---|---|---|
| 0 | 1.000 | 0 |
| 0.025 | 0.975 | 0.025 |
| 0.05 | 0.950 | 0.050 |
| 0.075 | 0.925 | 0.075 |
| 0.10 | 0.900 | 0.100 |

λ=0.10 이면 기존 total regret 의 중요도가 **10% 낮아진다**.

**왜 문제가 되나?**

```
CVaR90 ↓ (꼬리 10%는 줄었다)
그런데 일반 90%의 regret ↑ (오히려 늘었다)
→ total regret ↑ (전체 손실이 커짐)
```

최악 10개 표본의 regret 이 100→50 으로 줄었더라도, 나머지 90개 표본의 regret 이 1→2 로 늘면 전체 손실은 오히려 커진다.

**실험 결과:**

| λ | nRMSE 변화 | Gap 변화 | Total regret 변화 | CVaR90 변화 |
|---|---|---|---|---|
| 0.025 | +3.93%p | +0.46%p | **+2.66%** ❌ | +1.02% |
| 0.050 | +8.37%p | +0.51%p | **+2.94%** ❌ | −0.75% |
| 0.100 | +11.34%p | +0.78%p | **+4.47%** ❌ | −1.14% |

CVaR90 은 줄었지만 total regret 이 **커졌다**. 꼬리손실을 줄이려다 일반 손실이 더 늘어난 것이다.

---

## 13. Additive CVaR 방식으로의 수정

기존 total regret 의 계수를 1.0 그대로 두고, CVaR 만 **추가**한다:

```
min  W₂ Σ|Sₜ − xₜ|
     + W₁ Σ rₜ                          ← 기존 리그렛 (계수 1.0 유지)
     + W₁λ [Nζ + (1/(1−α)) Σ uₜ]        ← CVaR 추가 패널티

조건:
  uₜ ≥ rₜ − ζ,  uₜ ≥ 0
```

| 구분 | Total regret 항 | CVaR 항 | λ 증가의 의미 |
|---|---|---|---|
| 초기 가중평균 | (1−λ)Σrₜ | λN·CVaR | CVaR 강화와 동시에 total regret 약화 |
| 최종 additive | **Σrₜ** | λN·CVaR | total regret **유지**하고 CVaR만 추가 |

α=0.9, λ=0.05, W₁=1, W₂=20 를 넣으면:

```
min  20 Σ|Sₜ − xₜ| + Σ rₜ + 0.05N·ζ + 0.5 Σ uₜ
```

**작동 원리**: 100개 표본 중 손실이 큰 10개(91~100번째) 가 있다고 하자.

```
r₅₀=30,  r₉₁=60,  r₉₅=80,  r₁₀₀=200
ζ가 VaR90=60 에 수렴하면:
  u₅₀ = max(30−60, 0) = 0     (문턱 아래 → 패널티 없음)
  u₉₁ = max(60−60, 0) = 0     (문턱 위 → 패널티 없음)
  u₉₅ = max(80−60, 0) = 20    (문턱 위 → 추가 패널티 20)
  u₁₀₀= max(200−60, 0) = 140  (문턱 위 → 추가 패널티 140)
```

일반 표본은 uₜ = 0 이므로 CVaR 패널티를 거의 받지 않는다. 꼬리 표본은 uₜ 가 크므로 큰 패널티를 받는다. → solver 가 "꼬리 손실을 줄이자" 는 방향으로 계수를 조정하게 된다.

**실험 결과**:

| λ | nRMSE 변화 | Gap 변화 | Total regret 변화 | CVaR90 변화 |
|---|---|---|---|---|
| 0.025 | −0.79%p | −0.55%p | −3.80% | −3.37% |
| **0.05** | **−1.19%p** | **−1.50%p** | **−10.41%** | **−10.06%** |
| 0.075 | +0.82%p | −1.37%p | −9.53% | −9.69% |

모든 지표가 함께 개선되었다. 초기 방식의 문제 (total regret 증가) 가 해결되었다.

---

## 14. 최종 파라미터 설정

```
α = 0.9, λ = 0.05, W₁ = 1, W₂ = 20
```

λ 스윕 결과:

| λ | nRMSE 변화 | Gap 변화 | Total regret 변화 | CVaR90 변화 | Maximum regret 변화 |
|---|---|---|---|---|---|
| 0.025 | −0.79%p | −0.55%p | −3.80% | −3.37% | −3.75% |
| **0.05** | **−1.19%p** | **−1.50%p** | **−10.41%** | **−10.06%** | **−15.47%** |
| 0.075 | +0.82%p | −1.37%p | −9.53% | −9.69% | −12.88% |
| 0.10 | +3.47%p | −1.37%p | −9.53% | −11.28% | −12.18% |

λ=0.05가 가장 균형적인 설정이었다.

---

## 15. 실험 단계별 모델 범위

| 분석 단계 | 예측모형 | 수익식 | 데이터 범위 |
|---|---|---|---|
| 최초 문제 발견 | Baseline AR, Proposed AR | 원본 3항 | z03 블록 18 |
| 시간대별 진단 확장 | Baseline AR, Proposed AR | 원본 3항·수정 4항·Zhang | z03 블록 18 |
| 72개 블록 검증 | Proposed AR | 수정 4항만 | Z01·Z02·Z03, 총 72개 블록 |
| 6개 조합 비교 | Proposed AR, Proposed MLR | 원본 3항·수정 4항·Zhang | z03 블록 18만 |

---

## 16. λ=0 코드 등가성 검증

최대 예측값 차이: 1.11×10⁻⁴
최대 optimality gap 차이: 1.9×10⁻⁵%p

---

## 17. Proposed AR·수정 4항의 72개 블록 검증

### 17.1 Zone별 결과

| Zone | Total regret 개선 블록 | λ=0 pooled OG | λ=0.05 pooled OG | Total regret 변화 |
|---|---|---|---|---|
| Z01 | 16/24 | 21.11% | 20.84% | −1.28% |
| Z02 | 18/24 | 20.81% | 20.91% | +0.52% |
| Z03 | 17/24 | 21.34% | 21.00% | −1.57% |
| 전체 | **51/72** | 21.09% | 20.92% | **−0.80%** |

### 17.2 전체 통합지표

| 지표 | λ=0 | λ=0.05 | 변화 |
|---|---|---|---|
| Total regret | 2,185,796 | 2,168,231 | −0.80% |
| Pooled optimality gap | 21.09% | 20.92% | −0.17%p |
| Pooled CVaR90 | 551.59 | 545.90 | −1.03% |
| Maximum regret | 22,671.76 | 21,990.32 | −3.01% |
| Pooled nRMSE | 67.28% | 68.75% | **+1.47%p** |

---

## 18. Z02 블록 4의 극단적 실패

```
Total regret 변화: +55.13%
CVaR90 변화: +92.90%
Optimality gap 변화: +9.70%p (17.60% → 27.30%)
```

2012년 10월 8일 10시:
- 실제 발전량 Sₜ = 0.3041
- Real-time 가격 RPₜ = 169.06
- λ=0 예측·약정량 = 0
- λ=0.05 예측·약정량 = 1
- λ=0.05 regret = 4,445.83

---

## 19. z03 블록 18의 6개 모델·수익식 조합 비교

### 19.1 Proposed AR 결과

| 수익식 | 지표 | λ=0 | λ=0.05 | 변화 |
|---|---|---|---|---|
| 원본 3항 | nRMSE | 42.29% | 41.45% | −0.83%p |
| 원본 3항 | OG | 23.51% | 24.04% | +0.53%p |
| 원본 3항 | Total regret | 67,808 | 69,339 | +2.26% |
| **수정 4항** | **nRMSE** | **37.85%** | **36.66%** | **−1.19%p** |
| **수정 4항** | **OG** | **14.39%** | **12.89%** | **−1.50%p** |
| **수정 4항** | **Total regret** | **30,726** | **27,529** | **−10.41%** |
| 수정 3항 | nRMSE | 37.88% | 39.75% | +1.86%p |
| 수정 3항 | OG | 13.82% | 13.33% | −0.49%p |
| 수정 3항 | Total regret | 29,593 | 28,554 | −3.51% |

### 19.2 Proposed MLR 결과

| 수익식 | nRMSE 변화 | OG 변화 | Total regret 변화 | CVaR90 변화 |
|---|---|---|---|---|
| 원본 3항 | +1.06%p | −0.24%p | −1.00% | −0.51% |
| 수정 4항 | −0.17%p | −0.07%p | −0.65% | −1.78% |
| 수정 3항 | −0.20%p | −0.02%p | −0.23% | −2.34% |

### 19.3 여섯 조합 종합

| 모형 | 원본 3항 | 수정 4항 | 수정 3항 |
|---|---|---|---|
| Proposed AR | 정확도 개선, total regret 악화 | **모든 지표 개선** | 경제성 개선, nRMSE 악화 |
| Proposed MLR | 경제성 개선, nRMSE 악화 | **모든 지표 개선** | 모든 지표 개선 |

---

## 20. 민감도 분석

### 20.1 공통 패턴

12개 figure를 종합하면, CVaR 적용에 따른 가장 일관된 변화는 optimality gap의 개선이 아니라 **nRMSE의 악화**였다.

### 20.2 W₁/W₂ 스윕에서 nRMSE가 악화된 이유

```
min W₂ Σ|eₜ| + W₁[Σ Rₜ + λNζ + (1/(1−α)) Σ uₜ]
```

W₁/W₂가 증가할수록 모형은 실제 발전량을 정확하게 맞히는 것보다 경제적 regret, 특히 손실분포의 꼬리부분을 줄이는 방향을 더 강하게 선택한다.

W₂=0인 1/0 지점에서는 예측오차항이 목적함수에서 완전히 사라진다.

### 20.3 기존 3항 수익식에서 급격한 붕괴가 나타난 이유

```
πₜ(xₜ) = DPₜ·xₜ − PCₜ(xₜ − Sₜ) = (DPₜ − PCₜ)·xₜ + PCₜ·Sₜ

r < 1 ⟹ DPₜ − PCₜ > 0  → 약정량을 늘릴수록 수익 증가 (구조적 문제)
r = 1 ⟹ DPₜ − PCₜ = 0  → 구조적 유인 사라짐
```

### 20.4 대표 설정 W₁=1, W₂=20의 의미

민감도 분석을 통해 확인된 것은 그 결과가 넓은 가중치 범위에서 유지되는 강건한 결과가 아니라, CVaR의 부작용이 비교적 작게 나타난 제한적인 가중치 영역의 결과라는 점이다.

---

## 21. 최종 평가

**확인된 결과:**
- CVaR 적용에 따른 nRMSE 악화가 여러 모형과 수익식에서 반복
- CVaR 적용 전후의 optimality gap 차이는 nRMSE 변화에 비해 작음
- W₂가 작아지면 예측정확도를 직접 통제하는 기능이 약화
- W₁이 증가하면 전체 regret과 CVaR가 동시에 강화

**결론:**

> Additive CVaR는 기존 total regret 항을 유지하면서 꼬리손실을 추가로 관리할 수 있으며, W₁=1, W₂=20, λ=0.05의 대표 조건에서는 일부 경제적 개선이 확인되었다. 그러나 CVaR 항이 W₁에 함께 비례하기 때문에 W₁/W₂ 또는 shortage penalty의 영향력이 커질수록 예측오차항을 압도할 수 있다. 따라서 본 CVaR 확장은 **보편적으로 우수한 대체모형이 아니라, 제한된 가중치 영역에서 사용할 수 있는 위험회피적 보완방법**으로 해석해야 한다.

---

## 22. 이 프로젝트(z03/블록18, scipy/HiGHS)로의 통합 구현

위 1~21절은 `Additive_CVaR_확장.pdf`(`D:\03_JiWon\APEN_new\APEN_new\추가방법론_5_보고서_Additive_CVaR_확장.pdf`와 동일 문서)와, 그 근거가 된 원본 코드 —

- `추가방법론_5_(0)_기존모델들의_시간별지표_CVaR도입에_대한_근거`
- `추가방법론_5_(1)_수정전_CVaR_도입`
- `추가방법론_5_(2)_수정전_CVaR_도입_시간별지표`
- `추가방법론_5_(3)_수정전_CVaR_도입_시간별지표_table3` / `_람다스윕`
- `추가방법론_5_(4)_수정된_CVaR_시간별지표` ← **최종 additive CVaR 정식화(§13~14)의 실제 구현체**
- `추가방법론_5_(5)_수정된_CVaR_시간별지표_figure코드`

를 분석해 정리한 내용이다. 이 절에서는 그 중 §13~14의 **최종 additive CVaR 정식화**(4항 수익식, 즉 이 프로젝트의 방법2와 같은 PC=RT+rate×DA)를, 이 프로젝트가 실제로 쓰는 scipy(HiGHS) MILP 방식(`방법2_4term_ar_mlr_z03_block18.py`)에 통합한 내용을 기록한다.

### 22.1 원본(Gurobi)과 이 프로젝트(scipy/HiGHS)의 차이

원본 코드(`추가방법론_5_(4).../proposed_ar_4term_additive_cvar_z03_block18.py`)는 Gurobi(`gurobipy`)로 시간대별 MILP를 푼다. 이 프로젝트는 Gurobi 라이선스가 없어 방법1~6 전부 scipy `optimize.milp`(HiGHS)로 짰기 때문에, CVaR 항도 같은 방식(연속변수 `zeta`·`s_i` + `LinearConstraint` 하나 추가)으로 옮겨야 한다. 수식 자체(목적함수·오라클·CVaR 선형화)는 원본과 동일하고, 달라지는 것은 solver API뿐이다.

### 22.2 구현 위치

**`방법7_additive_cvar_ar_mlr_z03_block18.py`** — `방법2_4term_ar_mlr_z03_block18.py`를 베이스로, 데이터 로딩·baseline AR/MLR·`compute_gap_4term`(오라클 2후보)·`auto_ylim`은 그대로 재사용하고, 제안모형 MILP만 CVaR 항을 추가해 확장했다.

**MILP 변수 순서** (AR 기준, 시간대 하나당):

```
[ beta(13), x(n_obs), y+(n_obs), y-(n_obs), z_bin(n_bin), zeta(1), s(n_obs) ]
```

`beta`~`z_bin`까지는 방법2와 완전히 동일하고, `zeta`(CVaR 문턱값 ζ, 스칼라)와 `s`(초과손실 uₜ, n_obs개)만 새로 추가된다.

**목적함수** (§13의 수식을 그대로 반영):

```
obj[x]      = -W1 * DA_h                      # 방법2와 동일
obj[y+]     = -W1 * RT_h + W2_eff              # 방법2와 동일 (surplus cost)
obj[y-]     =  W1 * PC_h + W2_eff              # 방법2와 동일 (shortage cost), PC_h = RT_h + rate*DA_h
obj[zeta]   =  W1 * λ * n_obs                  # ← CVaR 추가항 1
obj[s_i]    =  W1 * λ / (1 - α)   (모든 i)      # ← CVaR 추가항 2
```

W2_eff = W2 × `W2_BALANCE_WEIGHT`(=4)는 방법2의 raw+weight 보정을 그대로 물려받는다(§10.3/§11 참고) — CVaR 항은 이 보정에 관여하지 않고 W1에만 곱해진다(§13 수식 그대로).

**새로 추가된 선형 제약** (CVaR 선형화, §9.5·§13):

```
s_i + zeta + DA_i·x_i + RT_i·y+_i - PC_i·y-_i  ≥  oracle_i     (모든 학습표본 i)
```

좌변을 `oracle_i - profit_i(x_i,y+_i,y-_i) ≤ zeta + s_i`로 정리하면 `s_i ≥ regret_i - zeta`(§9.5)와 같은 식이다. `oracle_i`는 학습용 raw(30MW 배율 없는) 3후보 오라클 `max(RT_i·S_i, DA_i·S_i, DA_i·1+RT_i·surplus_full-PC_i·shortage_full)`로, 원본 코드 §5-1~5-3(oracle_profit_train_unscaled)과 동일하게 계산한다(`오라클_3cand_raw()` 함수).

MLR도 시간대 구분 없이 pooled 하나로 같은 구조를 그대로 쓴다(`solve_mlr_proposed_cvar`).

### 22.3 λ=0일 때 방법2와 등가 (§16 검증을 이 프로젝트에서도 유지)

`obj[zeta]`, `obj[s_i]`가 전부 `W1·λ`에 비례하므로 λ=0이면 두 항의 목적함수 계수가 0이 되고, `zeta`·`s_i`는 목적함수에 아무 영향을 못 주는 자유변수가 된다 — 나머지 변수(`beta`, `x`, `y+`, `y-`, `z`)의 최적해는 방법2의 `solve_ar_proposed`/`solve_mlr_proposed`와 정확히 같아야 한다. 이 프로젝트에서도 원본 §16과 같은 방식으로 등가성을 확인하도록, 스크립트 실행 시 KPI 지점(W1=1,W2=20,rate=50%)에서 λ=0 결과를 출력하고 방법2의 같은 지점 출력과 직접 대조하도록 안내 문구를 넣었다(§22.5의 "미실행" 참고).

### 22.4 평가지표 — 두 가지 오라클을 용도별로 나눠 씀

이 프로젝트는 방법2와의 직접 비교가 중요하므로, nRMSE·Optimality Gap은 방법2가 이미 검증해 둔 **오라클 2후보**(`compute_gap_4term`, {commit=0, commit=actual})를 그대로 재사용한다 — 방법2·방법7 KPI 표를 나란히 놓고 비교할 수 있게 하기 위해서다. 반면 CVaR 고유 지표(Total regret·Mean regret·CVaR90·Maximum regret)는 시도방법7.md 자신의 정의(§5.1, §7.4)대로 **오라클 3후보**({0, S, 1})로 별도 계산한다(`compute_regret_diagnostics`) — 두 오라클이 다른 이유는 §8.1(방법2의 2후보 단순화는 PC=RT+rate×DA일 때만 유효)과 5.1(원 정의는 항상 3후보)이 서로 다른 목적으로 만들어졌기 때문이며, 이 절에서는 그 구분을 유지했다.

### 22.5 실행 및 산출물 (미실행 — 사용자 실행 대기)

스크립트 실행은 하지 않았다(코드 작성만 완료). 실행하면 다음을 생성한다:

- **λ 스윕 표** (`results/simulation_output/방법7_lambda_sweep_ar_mlr_z03_block18.csv`) — KPI 지점(W1=1,W2=20,rate=50%)에서 λ∈{0, 0.025, 0.05, 0.075, 0.10}별 nRMSE·Gap·Total regret·Mean regret·CVaR90·Max regret. §14의 표와 같은 실험을 이 프로젝트 데이터·solver로 재현한 것이다.
- **Fig.3/6** (`results/방법7_fig3_ar_z03_block18.png`, `방법7_fig6_mlr_z03_block18.png`) — W1/W2 10개점 스윕(rate=50%), 논문·λ=0(=방법2)·λ=0.05(제안) 세 곡선 비교.
- **Fig.5/8** (`results/방법7_fig5_ar_z03_block18.png`, `방법7_fig8_mlr_z03_block18.png`) — 벌금율 0~100% 스윕(W1=W2=1), 같은 세 곡선 비교.
- 각 Fig의 원시 수치 CSV(`results/simulation_output/방법7_fig{3,5,6,8}_*.csv`).

**주의**: CVaR 항 때문에 시간대별 MILP마다 연속변수(zeta 1개 + s_i n_obs개)와 제약(n_obs개)이 추가되어 방법2보다 문제 크기가 커진다 — 실행 시간이 방법2보다 길어질 수 있다(특히 Fig.3/6/5/8은 λ=0·0.05 두 번씩 풀어야 해서 방법2 대비 대략 2배 분량의 MILP를 푼다).

---

## 23. 실행 결과 — λ=0.05 효과가 예상보다 훨씬 약했던 이유 (디버깅)

### 23.1 실행 결과 요약 (KPI 지점, W1=1·W2=20·rate=50%)

`방법7_additive_cvar_ar_mlr_z03_block18.py`를 실제로 돌려보니, λ=0(등가성 검증)은 정확히 방법2와 일치했다 — AR nRMSE=36.32%/Gap=14.55%, MLR nRMSE=23.88%/Gap=11.30%로 `방법2_4term_ar_mlr_z03_block18.py`의 KPI 지점 출력과 소수점 수준까지 같다(§22.3의 등가성 검증 통과).

문제는 λ=0→0.05로 올렸을 때다. 시도방법7.md §14가 보고한 변화(AR: ΔnRMSE −1.19%p, ΔGap −1.50%p, ΔTotalRegret −10.41%, ΔCVaR90 −10.06%)와 달리, 이 프로젝트에 통합한 버전은 같은 지점에서:

```
λ=0.05: ΔnRMSE=+0.01%p  ΔGap=-0.02%p  ΔTotalRegret=-0.13%  ΔCVaR90=-0.23%
```

거의 아무 효과가 없었다. λ를 0.025~0.10로 스윕해도 전부 비슷하게 미미했다(`방법7_lambda_sweep_ar_mlr_z03_block18.csv` 참고). 게다가 Fig.3/6(W1/W2 스윕)·Fig.5/8(rate 스윕)에서는 KPI 지점을 벗어나면 λ=0.05가 오히려 nRMSE를 **더 나쁘게** 만드는 경우가 많았다(예: Fig.5 AR, rate=0%에서 nRMSE 46.60%→58.10%로 +11.5%p 악화, W1/W2=1/1에서 44.64%→50.64%로 +6.0%p 악화).

### 23.2 원인 진단 — `temp_debug_cvar_weight_dilution.py`

방법2의 `W2_BALANCE_WEIGHT=4`(W2_eff=W2×4, §10.3/§11 참고) 보정이 additive CVaR 항(W1에만 비례하고 W2와는 무관함, §13)의 상대적 영향력을 약화시킨다는 가설을 세우고, 같은 KPI 지점에서 세 가지를 비교했다(전체 로그: `temp_debug_cvar_weight_dilution.log`):

| 케이스 | 목적함수 | λ=0→0.05 ΔnRMSE | ΔGap | ΔTotalRegret | ΔCVaR90 |
|---|---|---:|---:|---:|---:|
| **(A) 방법7 그대로** | W2_eff = W2×4 (가중치 보정 있음) | +0.01%p | −0.02%p | −0.13% | −0.23% |
| **(B) 원본 참고코드처럼** | W2_eff = W2 (보정 없음, §10.2 단계) | **−1.19%p** | **−1.50%p** | **−10.41%** | **−10.06%** |

**(B)의 수치가 시도방법7.md §14/§19가 보고한 값과 소수점 둘째 자리까지 정확히 일치한다.** 이것으로 두 가지가 확인됐다:

1. **CVaR 구현 자체(오라클·선형화 제약·목적함수)는 맞다** — 원본과 같은 조건(가중치 보정 없음)으로 돌리면 원본이 보고한 개선 효과가 그대로 재현된다.
2. **시도방법7.md가 보고한 CVaR 개선 효과는, 방법2의 W2_BALANCE_WEIGHT=4 보정이 적용되지 않은 상태에서 얻은 결과다.** 원본 Gurobi 코드(`추가방법론_5_(4).../proposed_ar_4term_additive_cvar_z03_block18.py`)의 §5-4를 다시 보면 `surplus_cost = -W1*rt + W2`, `shortage_cost = W1*shortage_price + W2`로, 실제로 W2를 그대로 쓰고 있어 이 진단과 일치한다.

목적함수 계수로 왜 이렇게 되는지도 확인했다(대표로 hour=0):

```
(A) 가중치 보정 있음: sc∈[-19.1, 60.9], yc∈[112.5, 195.3]   (W2_eff=80)
(B) 가중치 보정 없음: sc∈[-79.1,  0.9], yc∈[ 52.5, 135.3]   (W2_eff=20)
CVaR 계수(둘 다 동일): obj_zeta=4.5, obj_s(표본당)=0.5
```

(A)에서는 정확도항(yc, sc)이 이미 100~200 수준으로 크게 부풀려져 있어서, CVaR 계수(4.5, 0.5)가 상대적으로 너무 작아 β를 거의 못 움직인다. (B)에서는 yc·sc가 CVaR 계수와 비슷한 자릿수(20~80 대) 안에 있어서 CVaR가 실제로 β 선택에 영향을 줄 수 있다.

### 23.3 λ를 키우면 보정과 CVaR를 양립시킬 수 있는가? — 아니다

혹시나 해서 (A) 상태에서 λ를 0.5, 2, 5, 10까지 올려봤다:

| λ (보정 있음) | ΔnRMSE | ΔGap | ΔTotalRegret | ΔCVaR90 |
|---|---:|---:|---:|---:|
| 0.05 | +0.01%p | −0.02%p | −0.13% | −0.23% |
| 0.5 | +2.56%p | −1.10%p | −7.53% | −9.63% |
| 2.0 | +17.84%p | −2.54%p | −17.45% | −30.03% |
| 5.0 | +24.31%p | −2.24%p | −15.38% | −26.17% |
| **10.0** | **+25.92%p** | −1.52%p | **−10.42%** | −23.91% |

λ=10(원래보다 200배)이 돼야 (B)의 λ=0.05와 비슷한 크기의 TotalRegret 감소(−10.42% vs −10.41%)가 나오는데, 그 대가로 nRMSE가 25.92%p나 나빠진다 — (B)에서는 같은 정도의 regret 개선을 nRMSE **개선**(−1.19%p)과 함께 얻었던 것과 대조적이다. **즉 W2_BALANCE_WEIGHT 보정과 additive CVaR는 "누가 β를 차지하느냐"를 두고 경쟁하는 관계라, λ만 키워서는 원본만큼 효율적인 절충점을 못 찾는다** — 같은 목적함수 안에 두 가지 서로 다른 정규화(W2 밸런스 보정 + CVaR)를 얹었을 때 생기는 합성 부작용으로 보인다.

### 23.4 결론 및 남은 선택지

- **방법7(=방법2+CVaR, 가중치 보정 유지)은 λ=0.05에서는 유의미한 개선을 못 준다** — §22의 구현 자체는 정확하지만(등가성 검증·원본 재현 둘 다 통과), 방법2의 다른 수정(W2_BALANCE_WEIGHT=4)과 결합하면 CVaR 항이 사실상 무력화된다.
- 다음 절(§24)에서 가중치 보정이 있는 상태에 맞는 새 λ를 재보정한다.

---

## 24. 가중치 보정 상태에 맞는 λ 재보정 (`temp_debug_cvar_lambda_resweep.py`)

### 24.1 방법

§23에서 확인한 대로, 가중치 보정(W2_eff=W2×4)이 있는 상태에서는 λ=0.05가 사실상 무효과다. 방법2가 `W2_BALANCE_WEIGHT=4`를 찾을 때 논문 Fig.5 판독값과의 SSE를 기준으로 삼았던 것과 달리, 여기서는 CVaR 고유의 목표(§5.6: "OG와 Total Regret은 유지하면서 CVaR90과 Maximum Regret을 줄이는 것 — 평균 손실은 그대로 두고 꼬리만 자르는 게 목적")를 그대로 기준으로 삼아, KPI 지점(W1=1,W2=20,rate=50%)에서 λ를 0(=방법2)부터 1.5까지 촘촘히 스윕했다.

### 24.2 결과 (전체: `results/simulation_output/temp_cvar_lambda_resweep_weighted.csv`)

| λ | AR nRMSE(Δ) | AR Gap(Δ) | AR ΔTotalRegret | AR ΔCVaR90 | AR ΔMaxRegret | MLR nRMSE(Δ) | MLR ΔTotalRegret | MLR ΔCVaR90 |
|---|---|---|---:|---:|---:|---|---:|---:|
| 0.05 | 36.33%(+0.01) | 14.53%(-0.02) | -0.12% | -0.23% | -0.69% | 23.77%(-0.10) | -0.38% | -0.48% |
| 0.10 | 36.15%(-0.17) | 14.45%(-0.09) | -0.63% | -0.31% | +0.07% | 23.69%(-0.19) | -0.75% | -0.90% |
| **0.15** | 36.63%(+0.31) | 14.41%(-0.14) | -0.93% | -1.28% | **-11.27%** | 23.59%(-0.28) | -1.08% | -1.38% |
| **0.20** | 36.54%(+0.22) | 14.41%(-0.14) | -0.94% | -1.19% | **-11.27%** | 23.53%(-0.35) | -1.47% | -1.79% |
| 0.30 | 36.87%(+0.55) | 14.54%(-0.01) | -0.05% | -0.65% | -11.52% | 23.29%(-0.59) | -2.07% | -3.13% |
| 0.40 | 38.41%(+2.09) | 13.98%(-0.57) | -3.91% | -7.47% | -27.93% | 23.15%(-0.73) | -2.55% | -4.15% |
| **0.50** | 38.88%(+2.56) | 13.45%(-1.10) | -7.53% | **-9.63%** | -29.11% | 23.03%(-0.85) | -3.05% | -5.59% |
| 0.75 | 42.91%(+6.59) | 13.27%(-1.27) | -8.76% | -12.57% | -22.93% | 23.05%(-0.83) | -3.26% | -7.98% |
| 1.00 | 43.92%(+7.60) | 12.04%(-2.51) | -17.25% | -20.09% | -26.39% | 23.73%(-0.14) | -3.61% | -12.32% |

(λ=0.3 지점의 ΔTotalRegret이 λ=0.2보다 오히려 작게 나오는 등 완전히 매끄러운 단조 곡선은 아니다 — 시간대별 MILP의 이진변수(z_t, surplus/shortage 배타 조건)가 λ에 따라 다르게 스위칭되면서 생기는 조합적 불연속으로 보인다. 큰 흐름 자체(λ↑ → 대체로 regret 계열 감소, nRMSE 증가)는 일관된다.)

### 24.3 두 가지 후보와 최종 선택

- **λ=0.2 (채택)**: nRMSE·Gap·Total regret은 λ=0 대비 거의 그대로인데(ΔnRMSE 0.22%p, ΔTotalRegret −0.94%), **Maximum regret만 11.27% 크게 줄어든다.** "평균은 그대로, 꼬리(그 중에서도 최악값)만 자른다"는 CVaR의 원래 취지(§5.6)에 가장 정확히 들어맞는 지점이다. MLR도 부작용 없이(오히려 nRMSE가 0.35%p 개선) 같은 방향으로 움직인다.
- **λ=0.5 (참고)**: CVaR90 감소폭(−9.63%)이 원본(가중치 보정 없음, λ=0.05)의 −10.06%에 근접해 "원 논문이 보여준 것과 비슷한 크기의 꼬리 개선"을 원한다면 이 지점이 후보다. 다만 대가로 AR nRMSE가 +2.56%p 나빠지고 Gap도 1.10%p 줄어(OG 자체가 변함) §5.6이 말한 "OG는 유지"라는 조건에서는 벗어난다.
- **최종 채택**: `방법7_additive_cvar_ar_mlr_z03_block18.py`의 `CVAR_LAMBDA_MAIN`을 0.05→**0.2**로, `LAMBDA_SWEEP`을 `[0, 0.05, 0.1, 0.15, 0.2, 0.3, 0.4, 0.5]`로 갱신했다. Fig.3/5/6/8 비교 플롯(λ=0 vs λ=0.2)과 λ 스윕 표는 이 값으로 다시 실행해야 최신 결과가 반영된다(미실행 — §22.5와 동일하게 사용자 실행 대기).

### 24.4 남는 해석 — 이 재보정이 말해주는 것

이 재보정 과정 자체가 흥미로운 결론을 하나 더 준다: **"CVaR로 꼬리를 얼마나 자를 수 있는가"는 λ 하나만의 문제가 아니라, 그 CVaR가 얹히는 목적함수의 나머지 항들이 이미 얼마나 강하게 정규화돼 있는지에 좌우된다.** 방법2의 W2_BALANCE_WEIGHT=4 보정처럼 다른 목적으로 만든 정규화가 있으면, CVaR의 λ도 그 정규화 강도에 맞게 별도로 재보정해야 한다 — 원본(추가방법론_5_(4))에서 찾은 λ=0.05를 그대로 다른 프로젝트/다른 정규화 위에 이식하면 안 된다는 뜻이다.

---

## 25. λ=0.2를 Fig.5/8(W1=W2=1)에 그대로 쓰면 안 되는 이유 — 지점별 재보정

### 25.1 문제 재발견

`CVAR_LAMBDA_MAIN=0.2`로 방법7 전체(Fig.3/5/6/8)를 다시 돌려보니, **Fig.3/6의 KPI 지점(1/20)에서는 §24에서 확인한 대로 작동했지만, Fig.5/8(W1=W2=1 고정, rate 스윕)과 Fig.3/6의 W1/W2가 1에 가까워지는 구간에서는 nRMSE가 심각하게 무너졌다:**

| 지점 | λ=0 nRMSE | λ=0.2 nRMSE | Δ |
|---|---:|---:|---:|
| Fig.3 AR, 1/20(KPI) | 36.32% | 36.54% | +0.22%p |
| Fig.3 AR, 1/2 | 41.16% | 56.69% | **+15.53%p** |
| Fig.3 AR, 2/1 | 53.22% | 72.12% | **+18.90%p** |
| Fig.5 AR, rate=0(W1=W2=1) | 46.60% | 82.89% | **+36.29%p** |
| Fig.6 MLR, 5/1 | 39.05% | 63.48% | **+24.43%p** |
| Fig.8 MLR, rate=0.6(W1=W2=1) | 28.65% | 47.03% | **+18.38%p** |

**원인**: §23~24의 메커니즘과 정확히 같다. CVaR 항은 W1에만 비례하고 이를 상쇄하는 정확도항은 W2_eff=W2×4에 비례하는데, KPI 지점은 W2=20(W2_eff=80)인 반면 Fig.5/8은 애초에 **W1=W2=1(W2_eff=4)** 에서 rate만 바꾸는 그림이다. W2_eff가 KPI 지점의 1/20이니 같은 λ=0.2가 그만큼 더 강하게 작동한 것이고, Fig.3/6도 W1/W2가 1/1·2/1·5/1처럼 W2가 작아지는 쪽으로 갈수록 같은 이유로 나빠진다.

### 25.2 Fig.5/8 전용 λ 재보정 (`temp_debug_cvar_lambda_resweep_fig58.py`)

Fig.5/8의 기준점(W1=W2=1, rate=0.5)에서 λ를 0~0.1로 다시 스윕했다(전체: `results/simulation_output/temp_cvar_lambda_resweep_fig58.csv`):

| λ | nRMSE(Δ) | Gap(Δ) | ΔTotalRegret | ΔCVaR90 | ΔMaxRegret |
|---|---|---|---:|---:|---:|
| 0.0025 | 45.07%(+0.43) | 12.21%(+0.42) | +3.53% | +3.83% | 0.00% |
| 0.005 | 44.95%(+0.30) | 11.77%(-0.03) | -0.24% | -1.10% | 0.00% |
| 0.0075 | 45.02%(+0.37) | 11.71%(-0.09) | -0.75% | -1.75% | 0.00% |
| **0.01** | **45.85%(+1.20)** | 11.59%(-0.21) | -1.75% | -3.04% | **-0.21%** |
| 0.02 | 47.99%(+3.35) | 11.59%(-0.21) | -1.75% | -4.72% | -1.11% |
| 0.05 | 50.64%(+6.00) | 11.54%(-0.26) | -2.16% | -9.14% | -18.13% |
| 0.10 | 58.28%(+13.64) | 11.79%(-0.01) | -0.05% | -10.68% | -18.39% |

**KPI 지점(λ=0.2에서 ΔnRMSE +0.22%p로 ΔMaxRegret -11.27%를 얻었던 것)과 비교하면, 이 지점(W1=W2=1)의 트레이드오프가 훨씬 나쁘다.** ΔnRMSE를 비슷한 수준(~1%p 이내)으로 제한하면 λ=0.0075~0.01 정도인데, 이때 얻는 tail 개선은 사실상 0(ΔMaxRegret 0%~-0.21%)에 가깝다 — KPI 지점처럼 "부작용 없이 꼬리만 자르는" 효과를 이 지점에서는 얻을 수 없다는 뜻이다. tail이 눈에 띄게 줄려면(λ=0.05, ΔMaxRegret -18%) nRMSE를 6%p나 희생해야 한다.

### 25.3 최종 채택 — 지점별로 다른 λ

이 결과를 반영해 `방법7_additive_cvar_ar_mlr_z03_block18.py`에 **λ 상수를 두 개로 분리**했다:

```
CVAR_LAMBDA_MAIN  = 0.2    # Fig.3/6 (KPI 지점 W1=1,W2=20 기준 W1/W2 스윕)
CVAR_LAMBDA_FIG58 = 0.01   # Fig.5/8 (W1=W2=1 고정, rate 스윕) — 부작용을 최소화한 절충값
```

Fig.5/8은 λ=0.01로도 tail 개선이 미미하지만(§25.2), 그 이상(λ=0.05 이상)으로 올리면 nRMSE 희생이 너무 커서(+6%p 이상) 이 지점만 놓고 보면 "그래도 최소한의 절충값"으로 0.01을 택했다. 그림 제목·범례에도 "Fig.5/8 전용 재보정값"이라고 표시해 KPI 지점의 λ=0.2와 다른 값임을 명시했다.

### 25.4 결론 — CVaR의 효과는 W1/W2 지점마다 다르다

방법2(그리고 다른 방법들)의 Fig.3 곡선도 원래 W1/W2가 1/1~1/0로 갈수록(W2가 작아질수록) nRMSE가 오르는 경향이 있지만(raw 계수 구조 자체의 특성, 3.2.2절), **CVaR를 얹으면 이 경향이 훨씬 가파르게 증폭된다** — CVaR 항이 W1에만 비례해서, W2가 작아질수록(=W1/W2가 커질수록) 상대적으로 더 강하게 작동하기 때문이다. 따라서 **이 프로젝트에 통합한 additive CVaR는 "W2가 W1보다 충분히 큰"(=정확도 항이 충분히 강한) 영역에서만 안전하게 쓸 수 있고, 그 영역을 벗어나면(W1≈W2 이상) λ를 그 지점에 맞게 다시 낮춰야 한다** — 이번 §24~25에서 두 지점(KPI 지점, Fig.5/8 지점)을 각각 재보정한 것이 그 실례다.