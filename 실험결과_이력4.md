# 실험결과 이력 4

`실험결과_이력.md`·`실험결과_이력2.md`·`실험결과_이력3.md`에 이어지는 작업 기록. 같은 용도(보고서 본문에는 안 넣는 "왜 이렇게 했는지" 히스토리)로, 새 항목은 항상 맨 아래에 날짜와 함께 추가한다.

---

## 2026-09-14 — 4개 SciPy 스크립트를 Gurobi로 변환 + 검증 완료 보고

### 배경 / 요청

사용자가 다음 4개 scipy 기반 스크립트를 gurobipy 기반으로 변환해달라고 요청했다.

1. `방법8_w2x4_ar_mlr_z03_block18.py` → 실제로는 존재하지 않는 이름이라 확인 요청 → 사용자가 `방법2_4term_ar_mlr_z03_block18.py`(방법8, PC=RT+rate×DA + W2×4)로 확정.
2. `방법6_pc_conditional_ar_mlr.py`
3. `방법4_pc_max_rt_dt_AR_MLR.py`
4. `방법5_pc_da_plus_rt_AR_MLR.py`

출력 파일명은 각각 `_gurobi.py` 접미사를 붙여 원본과 같은 위치에 새로 생성, 원본은 수정하지 않는 것이 조건이었다. `기본코드_(2)_AR_MLR_baseline_proposed_3가지_profit_figure코드/` 폴더의 방법1·2·3 Gurobi 구현을 스타일 기준으로 삼되, 방법론·파라미터·정규화·이익식·bounds·제약·스윕 범위·파일 경로는 절대 바꾸지 말라는 조건이 있었다. Gurobi가 실패/라이선스 한도에 걸리면 scipy로 자동 fallback하지 말고 명확한 오류로 표시할 것도 조건이었다.

검증 순서: ① 4개 파일 완성 → ② 문법·정적 검증 → ③ AR 기본 설정 실행 검증 → ④ AR 전체 스윕 실행 검증. MLR은 라이선스 한도로 실행이 안 될 수 있으니, 안 되면 억지로 돌리지 말고 정적 검증까지만 했다고 명확히 밝히라는 지시였다.

### 작업 내용

**참고 스타일 확인**: `model_proposed_ar_profit_change.py`(방법2 Gurobi), `model_baseline_ar_profit_change.py`(baseline bounded-LAD Gurobi), MLR 대응 파일들을 읽고 Gurobi 변환 패턴을 확립 — `gp.Model` → `addMVar`(beta/x/y_plus/y_minus, 필요시 이진 z) → `addConstr`(commitment_eq, mismatch_eq, complementarity) → `setObjective` → `optimize()` → `.X`로 계수 추출, 상태 확인은 `gmodel.Status == GRB.OPTIMAL`.

**변환 범위**: 각 파일에서 정확히 4곳만 수정 — AR baseline 회귀(bounded LAD, 시간대별 12개), MLR baseline 회귀(pooled), `solve_ar_proposed`/`solve_ar` 함수 본문(MILP), `solve_mlr_proposed`/`solve_mlr` 함수 본문(MILP). 함수로 감싸져 있어 호출부(그리드 스윕, KPI 계산, Fig3/5/6/8 생성, CSV 저장)는 전혀 손댈 필요가 없었다.

**작업 순서**: `cp`로 4개 원본을 `_gurobi.py` 이름으로 복사한 뒤, Edit으로 위 4곳만 교체. import문(`from scipy import sparse` / `from scipy.optimize import ...` → `import gurobipy as gp` / `from gurobipy import GRB`)과 파일 헤더에 변환 메모(무엇을 바꿨는지, 원본 미수정 확인)를 추가했다.

### 정적 검증

- 4개 파일 전부 `python -m py_compile` 통과.
- `diff` 원본 vs 변환본 — 변경된 hunk 라인 범위가 전부 위 4곳(+헤더/import)에만 위치, Gap 함수·스윕 루프·Figure 코드·CSV 저장은 한 글자도 다르지 않음을 확인.
- `grep`으로 `sparse.`, `linprog`, `milp(`, `LinearConstraint`, `Bounds(` 잔존 여부 확인 — 전부 제거됨(주석 언급만 남음).

### 실행 검증 (AR)

`temp_verify_gurobi_ar_kpi.py`, `temp_verify_gurobi_ar_sweep.py`를 작성해서, 원본/변환본 파일의 "MLR baseline 섹션을 건너뛴 AR 부분만" `exec`로 잘라 실행하고 직접 비교했다(원본 파일은 건드리지 않음, 별도 스크립트로 비교만 함).

**KPI 지점(rate=0.5, W1=1, W2=20) 비교**:

| 파일 | nRMSE diff | Gap diff |
|---|---:|---:|
| 방법8 | 3.55e-14 | 0.00e+00 |
| 방법6 | 3.11e-07 | 2.56e-07 |
| 방법4 | 2.03e-10 | 6.60e-11 |
| 방법5 | 4.08e-07 | 2.25e-07 |

4개 파일 모두 KPI 값이 보고서 3.3절 값(방법8 36.32%/14.55%, 방법6 35.01%/14.06%, 방법4 43.64%/22.04%, 방법5 37.63%/10.33%)과 정확히 일치.

**전체 AR 스윕(Fig3류 W1/W2 10점 @rate=0.5, Fig5류 rate 11점 @W1=W2=1) 비교**: 4개 파일 전부 max diff가 1e-4~1e-7 범위(nRMSE·Gap·예측값 전부) — `MIPGap=1e-9` 오차 범위 안.

**solver objective 직접 비교**(방법8, KPI지점 hour=0 단일 MILP): scipy `-703.5003974336613` vs gurobi `-703.5003974336619`, 차이 5.68e-13 — 사실상 동일.

결론: **목적함수·제약식 변환 오류 없음** — 남은 차이는 전부 solver 알고리즘/부동소수점 수준.

### MLR 검증 — 정적 대조까지만, 실행은 라이선스 한도로 실패

방법8 스크립트를 실제로 끝까지 돌려봤고, **MLR baseline(pooled bounded-LAD, ~1084개 변수) 단계에서** 다음 오류로 멈췄다:

```
gurobipy._exception.GurobiError: Model too large for size-limited license;
visit https://gurobi.com/unrestricted for more information
```

proposed-model MILP(변수가 더 많음)까지 갈 필요도 없이 baseline LP에서부터 막혔다. 4개 파일 모두 같은 z03/블록18 데이터(train 1080행)를 쓰므로 나머지 3개도 같은 지점에서 같은 이유로 실패할 것으로 판단, 개별 실행은 하지 않았다. **MLR은 문법 검사와 수식·제약식 정적 대조까지만 했고, 실제 실행/수치 비교는 하지 못했다** — 사용자에게도 이렇게 명확히 구분해서 보고했다.

기존 결과 파일(`results/방법2_fig*.png`, CSV 등)은 MLR 단계에서 먼저 멈춰서 전혀 덮어써지지 않았다(확인함).

### 충돌/미해결 사항

없음 — 요청한 방법론·파라미터(이익식, W1/W2/rate, 정규화 없음, bounds, oracle 3후보, train/test 기간, clipping, 스윕 범위, 파일 경로)는 전부 원본 그대로이고, scipy↔논문/보고서 수식 간 불일치도 이번 변환 범위에서는 발견되지 않았다.

### 참고 스크립트

- `temp_verify_gurobi_ar_kpi.py` — KPI 지점(rate=0.5,W1=1,W2=20) 1개에서 scipy vs gurobi AR 결과(baseline nRMSE, 예측값, nRMSE, Gap) 비교.
- `temp_verify_gurobi_ar_sweep.py` — Fig3류(W1/W2 10점)·Fig5류(rate 11점) 전체 AR 스윕 비교.
- `temp_run_방법8_gurobi_full.log` — 방법8 Gurobi 스크립트 전체 실행 로그(MLR baseline에서 라이선스 오류로 중단된 기록).
