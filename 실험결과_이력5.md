# 실험결과 이력 5

`실험결과_이력.md`~`실험결과_이력4.md`에 이어지는 작업 기록. 같은 용도(보고서 본문에는 안 넣는 "왜 이렇게 했는지" 히스토리)로, 새 항목은 항상 맨 아래에 날짜와 함께 추가한다.

---

## 2026-09-15 — 4개 Gurobi 변환본에 Figure 생성·저장 코드 추가(`_gurobi_figure.py`)

### 배경 / 요청

`실험결과_이력4.md`에서 검증 완료한 4개 `_gurobi.py`(방법8·6·4·5)에 Figure 생성·저장 코드를 추가하되, 기존 `_gurobi.py`는 수정하지 말고 파일명 끝에 `_gurobi_figure`를 붙인 새 파일로 저장해달라는 요청이었다.

핵심 조건:
- SciPy 원본에 Figure 코드가 들어있다고 가정하지 말고, 방법4·5·6·8의 실제 Figure 생성 코드가 별도로 있는지 프로젝트 전체를 먼저 확인.
- 있으면 그것을 최우선 기준, 없으면 `기본코드_(2)_.../` 방법1·2·3 Gurobi Figure 구성을 기준으로 구현, 찾았다고 추측/꾸며내지 말 것.
- Figure는 이번 Gurobi 실행에서 새로 계산된 결과 배열로 그려야 함(기존 scipy 상수/CSV를 Gurobi 결과인 것처럼 표시 금지).
- 저장 경로: `results/방법N_figX_..._z03_block18.png` → `..._gurobi.png`, CSV/NPZ도 새로 저장할 경우 `_gurobi` 접미사.
- 검증된 최적화(Gurobi) 부분은 리팩터링/재작성 금지 — 복사 후 Figure에 필요한 부분만 추가·연결.
- MLR은 무료 라이선스로 전체 실행 불가하므로 억지로 실행하거나 문제 크기를 줄이지 말 것 — 사용자가 학교 academic license로 직접 실행 예정.
- 검증: ① 문법 검사, ② 변수 정의/shape 확인, ③ 스윕 결과가 올바른 배열에 저장되는지 정적 확인, ④ 모든 출력에 `_gurobi` 확인, ⑤ 기존 파일 덮어쓰기 경로 없는지 확인, ⑥ 가능하면 AR만 실제 실행해 Figure 생성 여부 확인 — 실행 못 한 MLR Figure는 검증했다고 말하지 말 것.

### 핵심 발견 — 별도 Figure 스크립트는 없었다

프로젝트를 확인한 결과 **방법4·5·6·8의 Figure 생성 코드는 별도 파일이 아니라, 4개 SciPy 원본 스크립트 자기 자신 안에 이미 들어있었다.** 각 파일이 `savefig()`로 쓰는 경로(`p3 = os.path.join(RESULTS_DIR, "방법4_fig3_ar_z03_block18.png")` 등)가 `results/`에 실제로 있는 파일명과 정확히 일치 — 즉 이 스크립트들 자체가 현재 결과 이미지를 만든 스크립트였다. 이미 `_gurobi.py`로 복사할 때(이력4) 솔버 블록 4곳만 바꾸고 나머지는 손대지 않았으므로, **Figure 코드는 이미 `_gurobi.py` 안에 완전한 채로 들어있었다** — `기본코드_(2)_.../` 방법1·2·3 참고는 불필요했다.

### 작업 내용

4개 `_gurobi.py`를 `_gurobi_figure.py`로 복사한 뒤, **저장 경로 문자열만** 수정했다 — Figure 로직은 한 줄도 새로 안 썼다.

- `p3`/`p5`/`p6`/`p8`(PNG 4개): 확장자 앞에 `_gurobi` 삽입.
- `grid_csv`(방법8·6만 해당): `_gurobi` 삽입.
- `fig3/5/6/8_....csv`(4개 파일 전부, 각 4개씩): `_gurobi` 삽입.
- 파일 헤더에 "이 파일은 `_gurobi.py`를 복사해 저장 경로만 바꿨고, Figure 코드는 원본 scipy 파일에서 그대로 물려받은 것"이라는 변환 메모 추가.

`diff`로 원본(`_gurobi.py`) vs 신규(`_gurobi_figure.py`) 확인 — 4개 파일 전부 바뀐 줄이 8~9줄(경로 문자열)뿐이고, 그 외에는 완전히 동일함을 확인했다.

### 정적 검증

- ① 4개 파일 전부 `py_compile` 통과.
- ②③ Figure 코드가 참조하는 배열(`fig3_ar_n` 등)은 이력4에서 이미 AR 전체 스윕으로 실측 검증된 `get_ar`/`solve_ar_proposed` 등이 채우는 것과 동일 코드이므로(diff로 확인, 경로 문자열 외 변경 없음), shape·순서 문제 없음을 정적으로 확인.
- ④⑤ `grep`으로 모든 출력 경로(PNG·CSV·grid_csv)를 훑어 `_gurobi`가 안 붙은 경로가 있는지 확인 — 없음. 기존 `_gurobi.py` 4개, 원본 scipy 결과물(`results/방법N_fig*.png`, CSV)을 덮어쓰는 경로도 없음.

### 실행 검증 — Figure는 하나도 생성되지 못함(중요한 구조적 제약 발견)

`방법8_w2x4_ar_mlr_z03_block18_gurobi_figure.py`를 실제로 돌려봤다. 결과: MLR baseline(`gmodel_mlr.optimize()`)에서 이전과 동일한 라이선스 오류로 중단됐다.

```
gurobipy._exception.GurobiError: Model too large for size-limited license
```

**중요한 점**: 이건 "MLR 그림만 못 그린다"는 뜻이 아니다. 4개 파일 전부 그리드/스윕 루프(Fig.3~8 데이터를 만드는 단계)가 AR과 MLR을 같은 반복문 안에서 같이 계산하는 구조라(`get_ar(...)`, `get_mlr(...)`를 매 지점마다 나란히 호출), MLR baseline이 막히면 그 뒤에 오는 Fig.3(AR)·Fig.5(AR) plotting 코드까지 전부 도달이 안 된다. **AR 전용 Figure만 따로 뽑아내는 것도 지금 이 환경에서는 안 된다** — 이건 이번에 새로 생긴 제약이 아니라 원본 scipy 코드부터 있던 구조라, 리팩터링 금지 지시에 따라 그대로 뒀다. `results/*_gurobi.png`, `*_gurobi.csv` 파일은 하나도 생성되지 않았음을 확인(부작용 없음).

**즉 Figure 생성 여부는 실행으로 확인하지 못했고, 정적 검토(코드가 이미 검증된 것과 동일함)로만 판단했다.** 사용자에게도 이렇게 명확히 구분해서 보고했다.

### 학교에서 academic license로 실행할 때 확인 사항 (사용자에게 전달)

1. MLR baseline이 통과하는지부터 확인 — 막히면 AR 포함 4개 Figure 전부 안 나옴(위 구조적 이유).
2. 완주하면 `results/`에 `_gurobi.png` 4개, `simulation_output/`에 `_gurobi.csv`(방법8·6은 grid 포함 5개, 방법4·5는 4개) 생성됨 — 기존 scipy 결과 파일은 안전.
3. AR 수치는 이미 검증 끝났으니, 새 KPI·Fig3·Fig5 숫자가 scipy 버전과 거의 같은지(1e-4 이하 오차)만 가볍게 확인하면 충분.
4. MLR 결과가 이상하면: `gmodel.Status`가 `GRB.OPTIMAL`인지(방법5는 `TimeLimit=300` 있어 `TIME_LIMIT` 상태 가능성 있음 — 라이선스 문제와 구분할 것), Fig.6/8의 MLR nRMSE·Gap이 보고서 3.3절 값과 맞는지.
5. Gurobi 버전은 이 환경에서 13.0.3 — 버전 차이로 미세하게 달라지면 solver 알고리즘 차이(이전에도 1e-6~1e-13 수준)로 보고 크게 걱정 안 해도 됨.

### 참고

- `temp_run_방법8_gurobi_figure_full.log` — 방법8 Gurobi Figure 스크립트 전체 실행 로그(MLR baseline에서 라이선스 오류로 중단된 기록).
