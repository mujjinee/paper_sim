# 회귀계수 분석 — 수렴/발산 관찰 가이드

---

## 1. 예측 구조 (AR 예시)

$$\hat{y}_t = α + \sum_{h=1}^{12} β_h · y_{t-1+h}$$

| 계수 | 의미 | 역할 |
|------|------|------|
| α | 절편 | 기본 발전 규모 (전일 데이터와 무관한 baseline) |
| β_h (h=1~12) | 전일 h시간 계수 | 직전 하루 각 시간대의 영향도 |

→ 최적화의 대상은 **α, β** 13개 계수. 이 값이 어떻게 결정되느냐가 예측의 전부.

---

## 2. 최적화 목적함수 — 전체 연쇄 수식

### 2-1. 연쇄 구조

$$\underbrace{α, β}_{\text{회귀계수}}
\xrightarrow{\text{1. 예측}}
\underbrace{\hat{y}_i}_{\text{예측 발전량}}
\xrightarrow{\text{2. mismatch}}
\underbrace{\text{surplus}_i, \text{shortage}_i}_{\text{과소/과대 예측}}
\xrightarrow{\text{3. 이익 계산}}
\underbrace{\pi_i}_{\text{샘플별 이익}}
\xrightarrow{\text{4. 합산}}
\underbrace{f(α, β)}_{\text{목적함수}}$$

---

### 2-2. 단계별 수식

#### Step 1: 예측 (회귀계수 → 예측값)

$$\hat{y}_i(α, β) = α + \sum_{h=1}^{12} β_h · y_{i-1, h}$$

→ α, β가 변하면 ŷ_i가 변한다. ŷ_i는 0~1로 clip된다 (정규화 발전량).

#### Step 2: Mismatch (예측값 → 과소/과대 분류)

$$\text{mismatch}_i = a_i - \hat{y}_i(α, β)$$

$$\text{surplus}_i = \max(a_i - \hat{y}_i, 0) \ \ \ (\text{실제 > 예측, 과소 예측})$$

$$\text{shortage}_i = \max(\hat{y}_i - a_i, 0) \ \ \ (\text{예측 > 실제, 과대 예측})$$

→ 두 값 중 하나만 0이 아님. ŷ_i가 a_i보다 작으면 surplus↑, 크면 shortage↑

**직관적 예시:**
- 실제 발전 0.6, 예측 0.4 → surplus = 0.2 (예측보다 많이 났음 → 남은 전기를 RT에 팔면 됨)
- 실제 발전 0.4, 예측 0.6 → shortage = 0.2 (예측보다 적게 났음 → RT에서 전기를 사와야 함)

#### Step 3: 이익 (surplus/shortage → 샘플별 이익)

**① 3항 이익식 (논문 원본 그대로 — 시도방법1)**

$$\pi_i(α, β) = DA_i · \hat{y}_i + RP_i · \text{surplus}_i - PC_i · \text{shortage}_i$$

- **PC_i** = 0.5 × DA_i (논문 Eq.1a 그대로)

→ shortage일 때 벌금만 있음. RT에서 사와야 하는 비용은 반영 안 됨.
→ DA − PC = DA − 0.5×DA = 0.5×DA > 0 이므로, 약정량(ŷ)을 늘릴수록 이익이 무조건 커지는 구조적 허점 존재.
→ 이 때문에 계수가 발산 (α→1, β→0, nRMSE→116%)

**② 4항 이익식 (논문 재현에 성공한 방식 — 시도방법2)**

$$\pi_i(α, β) = \underbrace{DA_i · \hat{y}_i}_{\text{DA 판매 수입}} + \underbrace{RP_i · \text{surplus}_i}_{\text{남은 전기 RT 매도}} - \underbrace{RP_i · \text{shortage}_i}_{\text{모자란 전기 RT 매수}} - \underbrace{PC_i · \text{shortage}_i}_{\text{과대 예측 벌금}}$$

- **PC_i** = rate × DA_i  (rate = 0.5, 벌금비용률 × DA 가격)

→ surplus일 때는 RP로 팔면 되므로 + RP·surplus
→ shortage일 때는 RP로 사야 하고 벌금도 내야 하므로 - RP·shortage - PC·shortage
→ shortage 비용이 RP + PC = RT + 0.5×DA로 커지므로, 무작정 약정을 늘리는 게 더 이상 유리하지 않음.
→ ŷ이 실제 발전량에 가까울수록 π_i가 커짐

**③ 3항 이익식 — 시도방법4 (PC = rate × max(DA, RT))**

$$\pi_i(α, β) = DA_i · \hat{y}_i + RP_i · \text{surplus}_i - PC_i · \text{shortage}_i$$

- **PC_i** = rate × max(DA_i, RT_i)

→ RT가 DA보다 비싼 시간대에서는 벌금이 세져서 과대 예측이 막힘
→ 하지만 RT < DA인 시간대에서는 PC < DA가 되어 여전히 구조적 허점 잔존

#### Step 4: 목적함수 (합산 → 최적화 대상)

**올바른 gap 형태 (min 문제):**

$$f(α, β) = W_1 · \frac{\sum_i \pi_i^{oracle} - \sum_i \pi_i^{realized}(α, β)}{\sum_i \pi_i^{oracle}} + W_2 · \frac{1}{N}\sum_i |\hat{y}_i(α, β) - a_i|$$

→ **gap** = (최대 가능 이익 - 실제 이익) / 최대 가능 이익

**각 항이 줄어드는 조건:**

| 항 | 무엇을 나타내는가 | 어떻게 줄어들까 |
|----|-------------------|-----------------|
| 첫 번째 항 (W1) | 이익 차이 (oracle vs realized) | π_realized가 π_oracle에 가까워지면 ↓ |
| 두 번째 항 (W2) | 예측 오차 (MAE) | ŷ이 실제 a에 가까워지면 ↓ |

→ 두 항 모두 **ŷ이 정확해야 줄어듦**. 하지만 "정확"의 의미는 조금 다름:
- W1 항: "이익이 최대인 예측" (정확해야 하지만, 살짝 과소 예측해도 RT로 팔면 이익이 됨)
- W2 항: "수치적으로 정확한 예측" (MAE를 최소화하는 것)

→ **W1/W2 비율**이 이 두 목표를 어떻게 균형 잡을지 결정

---

### 2-3. Oracle (3후보 중 최대 이익, α, β와 무관)

$$\pi_i^{oracle} = \max\Big( \pi_i(x=0), \ \pi_i(x=a_i), \ \pi_i(x=1.0) \Big)$$

| 후보 | 예측값 | 전략 |
|------|--------|------|
| x = 0 | 0 | "전혀 팔지 말자" → surplus = a_i, RT에 전기를 다 팔면 됨 |
| x = a_i | 실제값 | "정확히 예측하자" → mismatch = 0, surplus = shortage = 0 |
| x = 1.0 | 최대 | "최대로 팔자" → 부족하면 RT에서 사와야 함 (벌금 포함) |

→ 이 세 전략 중 그날 그 시간대에 가장 이익이 큰 전략을 고른 값.
→ α, β와 완전히 무관한 상수. 데이터(DA, RT, actual)만으로 결정됨.
→ 최적화의 "완벽한 기준선" 역할. realized가 oracle에 가까울수록 gap이 작아짐.

---

### 2-4. α, β가 이익에 미치는 영향 (직관적 설명)

**α가 커지면 (예측값 전체가 올라감):**
- ŷ↑ → DA 판매↑ (수익 좋음)
- 하지만 실제 발전이 낮을 때 → shortage↑ → RT 매수 비용 + 벌금 발생
- → α가 너무 크면 과대 예측으로 벌금 부담

**β가 0으로 가깝게 되면 (전일 데이터 영향이 줄어듦):**
- ŷ ≈ α로 평평한 예측이 됨
- 전일 태양광 패턴을 전혀 활용하지 못함
- → 예측 정확도↓ → MAE↑

**α→1, β→0 조합 (계수 붕괴 현상 — 시도방법1에서 관측):**
- 모든 예측을 1.0(최대)으로 고정
- DA 판매는 최대지만, 실제 발전이 낮을 때 shortage가 막심
- → 시도방법1(3항, PC=0.5×DA)에서는 shortage 비용이 PC만 있어서, DA − PC = 0.5×DA > 0. 즉 shortage가 나더라도 순이익이 양수이므로, 목적함수가 "약정을 최대한 늘리라"고 유도. 그 결과 α→1, β→0으로 붕괴.
- → 시도방법2(4항)에서는 shortage 비용이 RP + PC이므로, RP로 사오는 비용이 커져서 무작정 늘리는 게 더 이상 유리하지 않음. 계수가 안정적으로 수렴.

---

## 3. L-BFGS-B iteration 과정

각 iteration k에서 관찰할 3가지:

### ① α (절편) 궤적

$$\{α^0, α^1, α^2, ..., α^K\}$$

| 현상 | 의미 |
|------|------|
| 중간값에서 안정 (예: α → 0.3) | **수렴** — 합리적인 예측 수준 |
| 1.0으로 폭주 | **발산** — "모든 예측을 최대값으로" = 계수 붕괴 |
| 0으로 수렴 | **발산** — "예측을 0으로" = 발전 포기 |

### ② Σβ (계수 합) 궤적

$$S^k = \sum_{h=1}^{12} |β_h^k|$$

| 현상 | 의미 |
|------|------|
| 0이 아닌 값 유지 (예: S → 0.8) | **수렴** — 전일 데이터 활용 |
| 0으로 소멸 | **발산** — 전일 데이터 무시 = "α만으로 예측" |

### ③ 목적함수값 궤적

$$\{f^0, f^1, f^2, ..., f^K\}$$

| 현상 | 의미 |
|------|------|
| 감소 후 평탄 | **수렴** — local minimum 도달 |
| 계속 감소 (멈출 기미 없음) | **발산** — 목적함수 구조 문제 (시도방법1 3항식에서 관측) |
| 급격히 떨어짐 후 불안정 | **발산** — 수치적 불안정 |

---

## 4. 수렴 vs 발산 패턴 비교

```
수렴 (정상)                    발산 (시도방법1, 3항 이익식에서 관측)

α: 0.2 ──────────●            α: 0.5 ──────────┐
   → 0.3 ──────●─ (안정)                  ↓
   → 0.31 ──●─ (안정)              1.0 ───────────────● (폭주)

Σβ: 0.9 ──────●                Σβ: 0.8 ────────┐
   → 0.8 ────●─ (유지)                       ↓
   → 0.82 ──●─ (유지)              0.0 ───────────────● (소멸)

obj: -2.1 ──●                   obj: -2.1 ────┐
   → -2.3 ──●─ (평탄)                          ↓
   → -2.31 ─●─ (평탄)              -5.0 ───────────● (계속 감소)
```

---

## 5. 관찰 방법 (실제 구현)

L-BFGS-B의 `callback` 함수에 iteration별 기록 추가:

```python
history = []

def callback(x):
    alpha = x[0]          # 절편
    sigma_beta = np.sum(np.abs(x[1:]))  # 계수 합
    obj = objective(x)    # 목적함수값
    history.append({
        'alpha': alpha,
        'sigma_beta': sigma_beta,
        'obj': obj
    })

res = optimize.minimize(objective, x0, method='L-BFGS-B',
                        callback=callback,
                        options={'maxiter': 2000, 'ftol': 1e-14})
```

---

## 6. 결론

**α와 Σβ를 iteration 단위로 추적하면 됨.**

| 관찰 조합 | 진단 |
|-----------|------|
| α 안정 + Σβ 유지 | ✅ 정상 수렴 — 목적함수 구조 문제없음 |
| α → 1.0 + Σβ → 0.0 | ❌ 계수 붕괴 — 목적함수 구조적 문제 |
| α → 0.0 + Σβ → 0.0 | ⚠️ 예측 붕괴 — 발전 포기 |
| obj 계속 감소 | ⚠️ 수렴 안됨 — maxiter 늘리거나 초기값 변경 |

---

## 7. 관련 파일

- [iter_convergence.py](iter_convergence.py) — AR/MLR × solver별 계수 궤적 추적
- [iter_convergence_lbfgs.py](iter_convergence_lbfgs.py) — L-BFGS-B iteration별 α, Σβ, obj 기록