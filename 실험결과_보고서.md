# APEN 실험결과 보고서

논문 제안 모형 재현 실험 정리 + 원본 결과 문서(APEN 결과, z03 block 18) 전체 첨부

---

## 1. 서론

이 글은 APEN 논문(Karimi & Kwon, *Applied Energy* 326, 2022)을 보고, 그 안의 제안 모형을 직접 코드로 짜서 따라 해본 과정을 정리한 글이다. 태양광 발전량을 다음 날 예측해서 하루 전 시장에 얼마나 팔지 정하는 문제인데, 그냥 예측만 잘하는 게 아니라 "예측이 틀렸을 때 돈이 얼마나 새는지"까지 같이 고려해서 회귀 계수를 정하는 게 이 논문의 핵심 아이디어다.

---

## 2. 논문 분석 및 고려 사항

논문을 재현하는 과정에서 실제로 부딪혔던 구현 이슈들을 정리한다. (2.1~2.4절은 원본 결과 문서 "APEN 결과 (z03 block 18)"의 1~4절 내용을 그대로 옮겼고, 2.5~2.6절은 그 이후 세션에서 추가로 파고든 내용이다.)

### 2.1 Optimality gap이 음수로 나오던 문제

기존 구현에서는 평가용 오라클을 max{DP_t, RP_t}·S_t (약정량이 0인 경우와 실제 발전량만큼인 경우, 이 두 가지만 비교)로 계산하고 있었다. 그런데 코드를 다시 살펴보던 중, 이 모델의 이익함수가 x_t = S_t 를 기준으로 꺾이는 구간별 선형함수이고 약정범위가 [0,1] 로 정해져 있다 보니, 벌금율이 100%보다 낮을 때는 실제 발전량보다 더 많이 약정하는 쪽( x_t > S_t )이 더 유리해지는 구간이 존재할 수 있다는 걸 발견했다.

즉 기존에 쓰던 2가지 후보만으로는 x_t=1 (설비 최대 약정)인 경우를 놓치고 있었고, 그래서 계산된 오라클 값이 실제로 가능한 최댓값보다 작을 수 있었던 것으로 보인다. 이 때문에 모델이 초과약정을 하는 경우, 실현이익이 이 오라클 값을 넘어서면서 gap이 음수로 계산되는 현상이 나타난 것 같다.

이 부분은, 이익함수가 [0, S_t, 1] 구간에서 구간별 선형이라는 성질을 이용해서, 최댓값이 나올 수 있는 지점(양 끝점과 꺾이는 점) 세 곳을 전부 후보로 넣는 방식으로 보완했다. 학습용과 평가용 오라클 모두 {0, S_t, 1} 세 후보 중 최댓값을 쓰도록 수정했고, 이렇게 하면 모델의 약정량도 항상 [0,1] 안에 있으니 오라클이 실현이익보다 항상 같거나 크게 되어(오라클 ≥ 실현이익), 그 이후로는 gap이 음수로 나오는 경우가 없어진 것을 확인했다.

> **실제로 찾은 사례**: 2013년 12월 9일 오전 9시 데이터를 보면, 실제 발전량은 설비의 13%(S=0.13)였고, 하루전가격은 54.98, 실시간가격은 36.53, 벌금단가는 그 절반인 27.49였다.
>
> - 기존: "실제 발전량(13%)만큼만 약정" → 대략 215원 정도 버는 게 최선이라고 계산했다.
> - 그런데 실제로 "설비 최대(100%)까지 약정"해버리면 → 부족분(87%)에 대한 벌금을 다 물어도 대략 **932원**을 번다.

### 2.2 Gurobi solver 사용

이 작업에서는 기존에 쓰던 scipy(HiGHS) 기반 코드를 Gurobi(gurobipy)로 바꿔서 다시 구현했다. (이 변환 작업은 AI 도구의 도움을 받아 진행했다.)

바꾸는 과정에서, AR 모형은 무료 라이선스로도 문제 없이 잘 돌아갔지만, MLR 모형은 학습 관측치 수가 약 1,080개로 많아, Gurobi의 무료(size-limited) 라이선스 한도(변수·제약 약 2,000개)를 초과해서 (`Model too large for size-limited license` 오류) 실행되지 않았다. 이에 따라, scipy(HiGHS)가 Gurobi와 수학적으로 동일한 해를 낸다는 점에 근거하여, MLR 결과는 scipy(HiGHS)로 산출한 값을 사용했다. 추후 학교의 정식 Gurobi 라이선스를 발급받으면 동일 코드로 재실행하여 재확인할 계획이다.

### 2.3 Optimality gap이 논문과 안 맞던 문제

오라클을 위와 같이 고치고 나서도, gap 값이 논문에 보고된 값(대략 10~15% 수준)보다 훨씬 크게(25~35% 수준) 나오는 현상이 계속 있었다. 그리고 W1/W2 비율을 바꿔가며 Fig.3·Fig.6 형태로 그려보면, 특정 지점을 넘어가면 값이 갑자기 크게 튀어버리고 그 뒤로는 W1을 아무리 더 키워도 똑같은 값에 멈춰 있는(더 이상 안 움직이는) 모습이 계속 나타났다.

이걸 살펴본 결과, 논문 Eq.(1a)에 나온 그대로의 이익함수( DA·x + RT·surplus − PC·shortage , PC=0.5·DA )에 원인이 있는 것 같았다. 부족한 만큼 약정했을 때의 이익 기울기가 DA−PC = DA·(1−벌금율) 인데, 벌금율이 100%보다 낮으면 이 값이 항상 양수라서, 약정을 설비 최대( x=1 )까지 늘릴수록 계산상 이익이 계속 커지는 구조였다. 이 부분 때문에 학습이 "일단 최대한 많이 약정하자"는 쪽으로 쏠리면서, nRMSE가 100%를 넘는 수준까지 튀고 gap도 같이 왜곡되는 현상이 나타났던 것 같다.

이 부분을 보완해보기 위해, 부족비용에 실시간시장 가격을 함께 반영하는 두 가지 방식을 시도했고, 각각 생각한 근거는 다음과 같다.

- **(1) 4항 방식 (부족비용 = PC + RT)**: 실제 전력시장에서는 하루전 약정을 못 지켰을 때, 계약상 벌금( PC )을 무는 것과는 별개로 부족한 만큼을 실시간시장에서 그 가격( RT )에 직접 사와야 하는 경우가 일반적인 정산 구조로 알고 있다. 벌금과 매입비용이 서로 다른 성격의 항목이라 둘 다 반영하는 게 맞지 않을까 싶어서 추가해본 것으로, 특정 논문에서 그대로 가져온 건 아니고 시장 정산 구조를 참고해서 자체적으로 시도해본 부분이다.
- **(2) Zhang et al.(2024)**("Improving Sequential Market Coordination via Value-oriented Renewable Energy Forecasting") **방식 (ρ₊=C·RT, ρ₋=RT)**: 이 논문에서, 상향조정가격(ρ₊)과 하향조정가격(ρ₋)을 하루전가격(ρ)과 다르게 두면서 ρ₊ > ρ > ρ₋ 가 성립해야 한다는 조건을 제시하고 있었다. 다만 논문에서는 이 세 가격을 각각 독립된 값으로 직접 지정하고 있었다(예시로 든 Table II에서도 발전기별 입찰가를 그렇게 따로따로 정해뒀다). 이 조건을 반영해보고 싶었으나 MISO 데이터의 하루전가격(DA)과 실시간가격(RT)에서는, ρ₊ 에 해당할 만한 별도 데이터가 없었다. 그래서 이미 잉여 가격으로 쓰고 있던 RT가 자연스럽게 ρ₋ 역할을 하고 있다고 보고, ρ₊ 만 새로 만들어야 하는 상황이었는데, 가장 간단한 방법으로 RT에 배수 C를 곱해서(ρ₊=C·RT) 만들었다. 이렇게 하면 ρ₊ > ρ > ρ₋ 라는 논문의 조건을 만족시킬 수 있고, C 하나만 조절해가며 차익거래가 남아있는 정도를 스윕해볼 수 있었다.

두 방식 모두 적용해본 결과, 초과약정 쪽으로 쏠리던 경향이 완화되면서 W1/W2 전 구간에서 비교적 안정적인 곡선을 얻을 수 있었고, gap 값도 원래보다 절반 이하 수준으로 줄어들어 논문에 보고된 값에 좀 더 가까워지는 것을 확인했다.

### 2.4 Zone 3, Block 18을 대표 사례로 선정

GEFCom2014의 3개 zone(z01, z02, z03) 각각에 대해 90일 학습/30일 테스트로 구성된 120일 롤링 블록 24개를 전수 평가했다. 그 중 기본(baseline) 모형의 nRMSE가 논문이 보고한 값에 가장 가깝게 나온 조합이 zone 3의 블록18(학습 2013-08-25~2013-11-22, 테스트 2013-11-23~2013-12-22)이었으며, 이후 모든 MLR 비교(Fig.6, Table 4 재현 등)는 이 블록을 대표 사례로 사용했다.

### 2.5 학습용 오라클과 평가용 오라클을 다르게 쓰는 이유

제안 모형(Eq.9)의 목적함수는 W1 항(달러 단위 경제손실)과 W2 항(0~1 비율 단위 예측오차, MAE)을 정수 비율(1,2,5,10,20 등)로 그냥 더한다. 두 항의 물리적 단위가 완전히 다르므로, W1 항을 "그 시간대에서 이론상 가능한 최대 이익"(`denom_h`)으로 나눠서 0~1 근처 크기로 맞춰주는 정규화가 필요했다. 이 `denom_h`를 구하는 오라클(학습용)과, nRMSE·gap을 최종 채점하는 오라클(평가용)은 서로 다른 후보 집합을 쓴다.

| | 학습용 오라클(`denom_h` 계산) | 평가용 오라클(Eq.13, 최종 gap 계산) |
|---|---|---|
| 후보 | {0, 실제발전량, 설비최대(1.0)} 3개 | {0, 실제발전량} 2개 |
| 근거 | 논문에 없음 — 외부 참고 구현(`operational_corrected`)에서 차용 | 논문 Eq.(13) + 5.1절 본문 그대로 |
| 후보를 줄이면(③ 제외) | 논문의 정상 비교점(W1=1,W2=20)에서조차 계수가 발산 — AR nRMSE가 38.6%→103.5%로 붕괴 | (해당 없음, 원래 이 정의를 씀) |

두 오라클을 똑같이 맞추고 싶은 게 자연스러운 직관이지만, 직접 실험해보니 **평가용 정의(2후보)를 학습에도 그대로 쓰면 W1=1,W2=20이라는, 논문이 실제로 검증한 "정상" 지점에서도 회귀계수가 발산**했다 — 시간대 11(오후 8시, 일몰 직전)에서는 학습표본 300개 중 286개가 극단(이진변수 필요) 상태로 몰렸다. 반대로 설비최대(③) 후보를 학습에만 남겨두면 같은 지점에서 안정적으로 수렴한다. 그래서 부득이하게 "학습은 3후보, 평가는 2후보"라는 비대칭 구성을 유지하고 있다.

### 2.6 W1/W2 정규화(denom) 자체가 논문 원문에 없다

PDF 원문(6페이지)의 Eq.(9a)를 다시 정밀 대조해봤는데, `denom_h` 정규화도 `scale = CAPACITY_MW × DURATION_HOURS`도 원문 어디에도 없었다. 논문은 그냥 "W1×(달러 단위 손실) + W2×(비율 단위 MAE)"를 정수 비율로 더하라고만 적어놨을 뿐, 두 항의 스케일을 어떻게 맞추는지는 서술하지 않는다. 즉 `denom_h` 정규화는 **논문을 그대로 구현하려다 발산해서, 외부 프로젝트의 임시방편을 빌려온 패치**였다는 걸 이번에 확인했다.

같은 분야("가치지향/decision-focused 재생에너지 예측")의 후속 연구들(예: arXiv:2310.00571 등)을 찾아봤는데, 이들은 대체로 W1/W2 같은 임의 가중치 없이, 이층 최적화(bilevel program)의 KKT 조건에서 손실함수를 직접 유도하는 방식을 쓴다 — 정확히 우리가 겪은 스케일 불일치 문제를 구조적으로 피해가는 방향이다. Eq.(9a)의 임의 가중합 방식이 사후적으로 보면 다소 임시방편적이었을 가능성을 뒷받침하는 정황이라고 볼 수 있다.

---

## 3. 논문 재현 및 개선 방법

### 3.1 5가지 profit 식 비교 결과 종합

다섯 가지 방법을 한번에 놓고 보면, **방법 1(논문 그대로)은 W1/W2를 어떻게 돌려도 특정 지점을 넘는 순간 nRMSE·Gap이 극단값으로 튀어 고정되는 포화 현상이 예외 없이 나타난다.** 이건 3항 이익식 자체에 "약정을 무조건 늘리는 게 유리해지는" 구조적인 허점이 있기 때문으로, 2.3절에서 짚은 문제와 같은 이야기다.

반면 **방법 2(4항)와 방법 3(Zhang)은 그 포화 없이 완만한 곡선을 그리면서**, 논문 Table 3 지점(W1=1, W2=20)에서 각각 nRMSE +0.8%p, Gap +0.3~0.6%p 수준까지 좁혀졌다 — 다섯 방법 중 논문에 가장 가깝다. 방법 2는 논문 이익식(Eq.1a)에 빠져 있던 항을 그대로 살린 가장 직접적인 고침이고, 방법 3은 전력시장의 상향/하향 조정가격 구조를 반영한, 이론적으로 더 그럴듯한 설명이라는 차이가 있다.

**방법 4, 5는 벌금율(rate)이라는 손잡이가 하나 더 있어서**, rate=0.5로 고정한 지점만 보면 방법 1급으로 벌어지지만, 각자 잘 맞는 rate(방법4는 0.9, 방법5는 0.7)를 따로 찾으면 방법 2·3과 비슷한 수준까지 가까워진다. 다만 이건 손잡이가 하나 늘어난 만큼 맞춰야 할 게 하나 더 생긴다는 뜻이라, 방법 2·3보다 손이 더 간다.

**정리하면**: 3항 이익식을 문자 그대로 쓰는 한 재현은 어렵고, 부족분에 대한 값을 이익식에 제대로 반영해야(4항이든, Zhang 비대칭이든, 혹은 벌금가 정의를 바꾸든) 논문 결과에 가까워진다는 게 다섯 방법을 통틀어 공통적으로 확인된 점이다.

### 3.2 3가지 profit 식 비교 결과 종합

*(원본 결과 문서 "APEN 결과 (z03 block 18)"의 5절 내용을 그대로 옮겼다. 3.1절이 다섯 가지 방법을 간략히 정리한 것이라면, 이 절은 그 중 세 가지(원본 3항·4항 수정·Zhang 비대칭)를 AR·MLR 두 모형, Fig.3/5/6/8 12개 그래프로 나눠 더 자세히 뜯어본 내용이다.)*

세 가지 이익함수(① 논문 원본 3항, ② 4항 수정, ③ Zhang 비대칭)에 대해 W1/W2 비율을 바꾼 경우(Fig.3, Fig.6)와 벌금율(또는 C값)을 바꾼 경우(Fig.5, Fig.8)를 각각 AR·MLR 두 모형에 대해 그려봤고, 논문에 보고된 원곡선도 같은 그래프에 옅게 겹쳐서 비교했다.

**(가) AR — Fig.3 (W1/W2 비율 스윕)**

- ① **원본(3항)**: W1 비중이 커질수록 nRMSE가 36%(AR)에서 76%(1/10)를 거쳐 1/5 지점부터 116% 안팎으로 튀어 오른 뒤 그 이후(1/2~1/0)로는 전혀 움직이지 않고 고정된다. gap은 반대로 25%(AR)에서 시작해 1/5 지점부터 3.4%까지 뚝 떨어진 뒤 역시 고정된다.
- ② **4항 수정**: 포화 없이 36%에서 68%까지 W1 비중에 따라 완만하게 상승하고, gap은 15%에서 10.7% 선까지 완만하게 하락한 뒤 거의 평평하게 유지된다. 논문 곡선(옅은 점선)과 비교하면 초반(AR~1/5 구간)에는 nRMSE가 논문보다 낮게 나오다가, W1 비중이 커지는 후반부로 갈수록 논문보다 높게 벌어지는 모습이다.
- ③ **Zhang 비대칭**: 마찬가지로 포화 없이 36%에서 69% 선까지 완만하게 상승하고, gap은 14.5%에서 11% 선까지 내려간 뒤 2/1 부근에서 살짝 반등해 11~11.5% 사이에서 유지된다. 전체적인 모양은 4항 방식과 거의 비슷하다.

**(나) AR — Fig.5 (벌금율 / Zhang C 스윕, W1=W2=1)**

- ① **원본(3항)**: nRMSE는 벌금율 0~70% 구간 내내 115% 안팎에서 고정돼 있다가, 80~90%에서 잠깐 120%까지 더 올라간 뒤 100%에서야 68%로 뚝 떨어진다. gap은 기본 AR(36%→7%로 선형 감소)과 제안 모형(2%에서 시작해 7%까지 서서히 증가)이 서로 반대 방향으로 움직이다가 100% 지점에서 둘 다 7% 근처로 만나는 모양이다.
- ② **4항 수정**: nRMSE가 0%에서 95%로 시작해 10%만 돼도 65%까지 급락하고, 이후 완만하게 내려가 60% 부근에서 53%로 최저점을 찍은 뒤 다시 68%까지 서서히 올라가는 U자형이다. 기본 AR은 여전히 36%로 평평하다.
- ③ **Zhang 비대칭**(x축은 C=0~3.0, 논문 곡선은 벌금율 0~100%에 대응시켜 겹침): nRMSE는 C=0~0.9까지 116% 안팎에 머물다가 C=1.2에서 65%로 급락하고 이후 C=3.0까지 62~77% 사이를 오르내린다. gap은 기본 AR이 13%에서 시작해 C=0.6~0.9 부근에서 11%까지 살짝 내려갔다가 C=3.0까지 23%로 꾸준히 올라가고, 제안 모형은 2%에서 시작해 C=0.9~1.2 부근에서 12%까지 오른 뒤 11~14% 사이에서 평평하게 유지된다 — AR·MLR 전체를 통틀어 기본 모형과 제안 모형의 gap 곡선이 가장 뚜렷하게 갈라지면서 논문이 말하려는 방향(제안 모형이 완만함)과 잘 맞아떨어지는 그래프였다.

**(다) MLR — Fig.6 (W1/W2 비율 스윕)**

- ① **원본(3항)**: nRMSE가 MLR(22%)에서 1/20(31%)까지 오르다가 1/10 지점부터 115% 안팎으로 튀어 고정된다. gap은 반대로 29% 안팎에서 시작해 1/10 지점부터 3~4% 선으로 떨어진 뒤 고정된다.
- ② **4항 수정**: nRMSE가 24%(MLR)~23%(1/20~1/10) 선에서 거의 안 움직이다가 1/5 지점부터 서서히 올라 1/0에서 53%까지 상승한다. gap은 11%대에서 시작해 1/2 부근에서 10% 선으로 최저점을 찍고, 이후 10.2~10.3% 선에서 거의 평평하게 유지된다.
- ③ **Zhang 비대칭**: nRMSE는 4항과 거의 비슷하게 24%→23%로 시작해 1/2부터 오르기 시작하지만, 4항보다 더 가파르게 올라 1/0에서 58.6%까지 도달한다. gap은 11%대에서 시작해 1/1~2/1 부근에서 10.2% 선으로 최저점을 찍고 이후 10.5% 선까지 완만히 올라간다.

**(라) MLR — Fig.8 (벌금율 / C 스윕, W1=W2=1)**

- ① **원본(3항)**: nRMSE는 MLR 24% 고정, 제안 모형은 90%까지 115% 안팎에 머물다가 100%에서 102%로 소폭 하락한다. gap은 MLR이 41%에서 7.7%까지 거의 선형으로 감소하고, 제안 모형은 2%에서 시작해 100%에서 12%까지 서서히 올라 두 선이 끝에서 서로 가까워지는, AR Fig.5와 같은 방향이 반대로 움직이는 모양이다.
- ② **4항 수정**: nRMSE가 0%에서 51%로 높게 시작했다가 10%에 24%로 급락하고, 이후 서서히 올라 100%에서 54%에 도달한다(U자형에 가깝지만 저점이 아주 초반에 있는 모양). gap은 MLR이 14%에서 시작해 30% 부근에서 10%로 최저점을 찍은 뒤 다시 16%까지 올라가는 U자형이고, 제안 모형은 13%에서 시작해 같은 30~40% 부근에서 9.8~10% 선으로 내려간 뒤 10.5% 선까지만 완만하게 오른다 — MLR은 U자로 크게 출렁이는데 제안 모형은 그보다 훨씬 좁은 범위 안에서 움직인다.
- ③ **Zhang 비대칭**(x축 C=1.0~2.0): nRMSE는 C=1.0에서 51%로 시작해 1.1~1.2에서 24%로 급락한 뒤, 1.6~1.7 부근에서 39~41%로 잠깐 주춤했다가 2.0에서 58.5%까지 다시 올라간다. gap은 MLR이 14%에서 시작해 1.3 부근에서 10.7%로 최저점을 찍고 2.0까지 15%로 올라가고, 제안 모형은 13%에서 시작해 1.4~1.5 부근에서 10.2%로 내려간 뒤 2.0까지 10.4% 선에서 거의 안 움직인다.

### 3.3 시도 방법

| 방법 | 한 줄 요약 |
|---|---|
| 방법 1 | 논문을 문자 그대로 짠 것 (3항 이익식, PC = 0.5×DA, 보정 없음) |
| 방법 2 | 이익식에 −RT×부족 한 항을 더 넣은 것 (4항 이익식) |
| 방법 3 | Zhang 방식 — 부족할 때 가격과 남을 때 가격을 다르게 둠 (부족가 = c×RT) |
| 방법 4 | 벌금가를 PC = rate×max(DA, RT)로 바꾼 것 |
| 방법 5 | 벌금가를 PC = rate×(DA + RT)로 바꾼 것 |
| 방법 6 | 벌금가는 그대로 두고, W1/W2 혼합 대신 Smart SPO+ loss로 바꾼 것 |

**● 방법 1 — 논문 그대로**

논문 Eq. 1a를 문자 그대로 3항으로 짜고, 벌금가도 PC = 0.5×DA로 그대로 뒀다. 이렇게 짜면 부족분(shortage)이 나는 시간대에서 이익 = DA×x − 0.5×DA×(x−S)가 되는데, 이 식은 x(약정량)를 키울수록 이익이 커지는 구조라 아무 제약이 없다.

**● 방법 2 — 4항식 넣기**

방법 1의 이익식에서 빠졌던 −RT×부족 항을 더해서 4항으로 만들었다. 이익 = DA×약정 + RT×잉여 − RT×부족 − 벌금×부족. 부족분을 실제로 RT 시장에서 사 와야 하는 돈이 반영되니까, 약정을 무작정 늘리는 게 더 이상 공짜 이득이 아니게 된다.

**● 방법 3 — Zhang 방식 (비대칭 가격)**

Zhang 등(2024)의 방식을 빌려서, 남을 때 파는 가격(RT)과 부족할 때 사와야 하는 가격(c×RT, c>1)을 다르게 뒀다. 전력시장에서는 부족분을 살 때가 남는 걸 팔 때보다 원래 비싸므로, 이게 방법 2보다 이론적으로 더 그럴듯한 설명이다.

**● 방법 4 — PC = rate × max(DA, RT)**

벌금가를 DA와 RT 중 더 큰 쪽에 비례하게 정했다. RT가 DA보다 비싼 시간대라면 벌금이 더 세지니까, 방법 1의 허점(무조건 약정을 늘리는 게 유리한 구조)이 그 시간대만큼은 없어진다.

**● 방법 5 — PC = rate × (DA + RT)**

벌금가를 DA와 RT를 더한 값에 비례하게 정했다.

**● 방법 6 — Smart SPO+ loss (Elmachtoub & Grigas 2022)**

방법 1~5는 전부 벌금가(PC) 정의나 이익식 자체를 고치는 접근이었는데, 이 방법은 반대로 이익식은 그대로 두고 **학습 목적함수를 W1/W2 혼합 대신 SPO+ loss로 통째로 바꿔봤다.** Piecewise-linear 구조로 shortage/surplus 방향을 구분해서 다르게 페널티를 주고, |oracle − realized| 대신 **dual-based decision error**를 직접 측정하는 방식이다.

**결론**: 구현이 복잡한 것에 비해, 근본적인 DA − PC > 0 문제 자체를 바꾸지는 못했다. 다만 Gap의 부호(음수→양수)는 고쳐져서, 다른 방법들과 같이 쓸 보조적 접근으로는 가능성이 있다. 자세한 결과는 4.6절, 그리고 이후 버그 수정 결과는 4.7절 참고.

### 3.4 KPI 비교 (W1=1, W2=20, 벌금율 50%)

논문 Table 3이 비교 기준으로 삼는 지점(W1=1, W2=20, 벌금율 50%)에서 여섯 방법을 나란히 놓아봤다.

| 방법 | nRMSE | Gap | 논문 값과 차이 |
|---|---:|---:|---|
| 논문(Table 3, 참고) | 34.89% | 13.91% | — |
| 방법 1 (논문 그대로) | 42.29% | 23.51% | nRMSE +7.4%p, Gap +9.6%p — 많이 벌어짐 |
| 방법 2 (4항식) | 35.69% | 14.54% | nRMSE +0.8%p, Gap +0.6%p — 가장 가까움 |
| 방법 3 (Zhang, c=1.5) | 35.68% | 14.19% | nRMSE +0.8%p, Gap +0.3%p — 방법2와 비슷하게 가까움 |
| 방법 4 (PC=rate·max(DA,RT), rate=0.5) | 36.35% | 24.75% | nRMSE +1.5%p, Gap +10.8%p — 많이 벌어짐 |
| 방법 5 (PC=rate·(DA+RT), rate=0.5) | 36.09% | 10.70% | nRMSE +1.2%p, Gap −3.2%p — Gap이 논문보다 작음 |
| 방법 6 (Smart SPO+ loss) | 60.35% | 19.41% | nRMSE +25.5%p, Gap +5.5%p — Gap 부호는 고쳐졌지만 nRMSE가 가장 크게 벌어짐 |

> 방법 4는 이 rate=0.5 지점에서만 보면 안 좋아 보이지만, rate를 0.9~1.2쪽으로 옮기면 훨씬 나아진다 — 4절에서 그래프로 보여준다. 방법 6의 수치는 3.5절에서 밝혀진 버그가 수정되기 전 값이며, 수정 후 결과는 4.7절에 갱신해뒀다.

### 3.5 LinearConstraint 등식 제약이 부등식으로 새던 버그

3.1~3.4절의 분석과는 별개로, 통합 스크립트(AR·MLR을 한 파일에서 같이 돌리는 방법1·2·6 버전)를 다시 감사하는 과정에서 코드 버그를 하나 더 찾았다. scipy의 `LinearConstraint(A, rhs)`를 등식 제약(약정량 = 회귀식 예측값, 예측값+잉여−부족 = 실제발전량)으로 쓰려고 했는데, 이 함수의 실제 시그니처는 `LinearConstraint(A, lb=-inf, ub=inf)`라서 **인자를 2개만 넘기면 `lb`만 채워지고 `ub`는 기본값 `+inf`로 남는다.** 즉 의도한 등식이 아니라 `A@x ≥ rhs`라는 부등식이 되어 있었던 것이다 — 약정량(x)을 아무리 키워도 부족분에 대한 페널티를 물지 않는 구멍이 생긴 셈이다.

그 결과 MILP는 W1/W2를 어떻게 바꾸든 `β = [1, 0, ..., 0]`(피처는 전부 무시하고 그냥 상시 최대발전 1.0을 약정)이라는 자명해로 수렴했다. 증상은 뚜렷했다:

- W1/W2 10개 점으로 Fig.3를 그려도 **전 구간이 완전히 평평한 직선**이 된다.
- nRMSE가 100%를 훌쩍 넘는다(z01 기준 247.06%, z03 기준 116.45%).
- 구조가 전혀 다른 AR과 MLR 모형이 **정확히 같은 nRMSE 값**으로 수렴한다 — 둘 다 "무조건 최대 커밋"이라는 같은 자명해로 붕괴했으니 예측값 자체가 사실상 동일해진 것이다.
- 실제로 hour=0 시간대의 MILP 해를 직접 뜯어보니 `β=[1,0,0,...,0]`, 약정량이 학습표본 90개 전부에서 정확히 1.0, 부족분(y⁻)은 전부 0으로 나왔다 — 실제발전량이 0.02~0.64로 제각각인데도 부족 페널티가 전혀 안 잡힌다는 건 등식이 깨져 있다는 직접적인 증거였다.

수정은 간단했다: `LinearConstraint(A, rhs)` → `LinearConstraint(A, rhs, rhs)`로 세 번째 인자(상한)를 명시해서 진짜 등식으로 만들었다. 영향받은 통합 스크립트 5개(방법1의 z01·z03 버전, 방법5(4.5절과는 별개로 통합판을 새로 만든 버전), 논문 원식 그대로 재현한 버전, 방법6(SPO+))를 전부 고치고 재실행해서 확인했다. 반대로 애초에 3항짜리 `LinearConstraint(A, lb, ub)` 형태로 세 인자를 다 넘겨쓰던 4항식·Zhang 스크립트는 처음부터 이 버그가 없었다.

흥미로운 점은, 이 버그를 걷어내고 나니 W1/W2가 낮은 구간(1/20~1/2)은 정상적으로 반응하고, W1/W2가 일정 임계값을 넘는 지점부터만 다시 평평해지는 패턴이 나타났다는 것이다. 즉 **버그가 있을 때는 전 구간이 가짜로 평평했던 것이고, 버그를 고치고 나니 논문이 검증하지 않은 고-W1 영역에서만 진짜로 포화되는 것**으로 정리됐다 — 이건 2.3·3.1절에서 짚은 "이익식 자체의 구조적 한계(DA−PC>0이면 약정을 늘릴수록 유리)"와 정확히 같은 메커니즘이 이 임계값 이상에서 실제로 작동한 것이지, 코드가 잘못돼서가 아니다. 코드 버그와 수식의 구조적 한계는 서로 다른 현상이었고, 이번에 둘을 분리해낼 수 있었다.

---

## 4. 실험 결과

방법별로 W1/W2 스윕(Fig.3 계열)과 벌금율 스윕(Fig.5 계열) 그래프를 논문값과 나란히 놓고 비교한 결과다.

### 4.1 시도방법 1 결과 — 논문 그대로

- W1/W2를 1/20에서 1/0까지 늘려가며 봤더니, nRMSE가 1/5 지점부터 이미 116%까지 치솟고 그 뒤로는 더 안 움직인다 (완전히 극단에 붙어버린 상태).
- 벌금율을 0%~100%로 바꿔가며 봐도 똑같이 90%대까지는 계속 무너진 상태로 있다가, 100%에 가까워져서야 조금 풀린다.

![방법1 Fig.5 — 벌금율 0~100% 스윕](CodefromJiWon/results/model_proposed_ar_ver2_fig5.png)

*(방법 1은 Fig.3 그림은 따로 안 남겨 뒀고, 3.4절 표의 수치와 위 Fig.5만 있다. 이후 통합 스크립트로 다시 그린 버전은 4.7절 참고 — 3.5절 버그가 섞여 있던 결과라는 점을 감안하고 봐야 한다.)*

### 4.2 시도방법 2 결과 — 4항식 넣기

W1/W2를 1/20부터 1/0까지 쭉 스윕해도 논문 곡선처럼 nRMSE는 완만히 오르고 Gap은 완만히 내려가는 모양이 그대로 나왔다. AR 기준선 Gap도 14.94%로 논문(15.04%)과 거의 맞았다.

| 방법2 Fig.3 — 벌금율 50%, W1/W2 스윕 (dual y축) | 방법2 Fig.5 — W1/W2=1/1, 벌금율 0~100% 스윕 |
|---|---|
| ![방법2 Fig.3](CodefromJiWon/results/model_proposed_ar_profit_change_v3_fig3.png) | ![방법2 Fig.5](CodefromJiWon/results/model_proposed_ar_profit_change_v3_fig5.png) |

다른 블록에서도 같은 경향이 나오는지 확인하려고, zone z01의 block21(z03·block18과는 다른 롤링 블록)에서도 4항식을 AR·MLR 양쪽으로 다시 돌려봤다. 곡선 모양(포화 없이 완만하게 움직이는 트레이드오프)이 z03·block18과 마찬가지로 그대로 나왔다.

| 방법2 Fig.3 (z01,block21) — AR, rate=0.5, W1/W2 스윕 | 방법2 Fig.5 (z01,block21) — AR, W1/W2=1/1, 벌금율 스윕 |
|---|---|
| ![방법2 Fig.3 z01](results/fig3_ar_4term_z01_block21.png) | ![방법2 Fig.5 z01](results/fig5_ar_4term_z01_block21.png) |

| 방법2 Fig.6 (z01,block21) — MLR, rate=0.5, W1/W2 스윕 | 방법2 Fig.8 (z01,block21) — MLR, W1/W2=1/1, 벌금율 스윕 |
|---|---|
| ![방법2 Fig.6 z01](results/fig6_mlr_4term_z01_block21.png) | ![방법2 Fig.8 z01](results/fig8_mlr_4term_z01_block21.png) |

### 4.3 시도방법 3 결과 — Zhang 방식 (비대칭 가격)

c값을 1.0, 1.2, 1.5, 2.0, 3.0으로 바꿔가며 봤는데, **c=1.5에서 논문과 가장 잘 맞았다** (W1/W2=1/20에서 nRMSE 35.68%, Gap 14.19%). c가 2.0, 3.0처럼 너무 커지면 부족 비용이 지나치게 커져서 오히려 Gap이 20%, 30%대로 벌어졌다.

| 방법3 Fig.3 — c=1.5, 벌금율 50%, W1/W2 스윕 (dual y축) | 방법3 Fig.5 — W1/W2=1/1, c 스윕(1.0~3.0) |
|---|---|
| ![방법3 Fig.3](results/results_archive/fig3_zhang_asymmetric_3term_v2.png) | ![방법3 Fig.5](results/results_archive/fig5_zhang_asymmetric_3term_v2.png) |

*(방법3 Fig.5의 옅은 점선은 논문 Fig.5(벌금율 0~100%) 참고선 — x축 의미가 달라 정확한 대응은 아님)*

*(방법3의 Fig.5는 논문에 "c값 스윕" 자체가 없어서, x축을 c로 두고 참고선만 얹었다.)*

### 4.4 시도방법 4 결과 — PC = rate × max(DA, RT)

rate를 0.3부터 1.5까지 통째로 스윕해서 논문값과 가장 가까운 조합을 자동으로 찾아봤더니 **rate=0.9** 근처가 제일 좋았다. rate=0.5(3.4절 표의 비교 지점)에서는 오히려 안 좋게 나오는데, 이건 이 방법이 rate=0.5에서 최적이 아니라 그런 것뿐이다.

| 방법4 Fig.3 — best rate 자동탐색, W1/W2 스윕 (dual y축) | 방법4 Fig.5 — W1/W2=1/1, 벌금율 스윕 |
|---|---|
| ![방법4 Fig.3](results/results_archive/fig3_method1_pc_max_da_rt_ar_v3.png) | ![방법4 Fig.5](results/results_archive/fig5_method1_pc_max_da_rt_ar_v3.png) |

이 방법은 MLR(외생적 예측, 기상 정보를 쓰는 쪽)에도 똑같이 적용해봤다. 논문 Fig.6/Fig.8에 해당하는 자리다.

| 방법4 MLR Fig.6 — best rate 자동탐색, W1/W2 스윕 (dual y축) | 방법4 MLR Fig.8 — W1/W2=1/1, 벌금율 스윕 (논문 참고선 포함) |
|---|---|
| ![방법4 Fig.6](results/results_archive/fig6_method1_pc_max_da_rt_mlr_v2.png) | ![방법4 Fig.8](results/results_archive/fig8_method1_pc_max_da_rt_mlr_v2.png) |

### 4.5 시도방법 5 결과 — PC = rate × (DA + RT)

rate를 0.1부터 1.0까지 바꿔가며 봤을 때 **rate=0.7** 근처가 논문과 가장 가까웠다 (W1/W2=1/20에서 nRMSE 35.59%, Gap 13.45%). rate=0.5(3.4절 표의 비교 지점)에서는 Gap이 10.70%로 논문(13.91%)보다 좀 작게 나온다 — 이 rate에서는 벌금이 살짝 약한 셈이다.

아래는 통합 스크립트(`integrated_pc_da_plus_rt_fig3568_AR_MLR.py`, z03/블록18, rate=50% 고정)로 W1/W2·penalty rate 전 구간을 다시 그린 버전이다 (4.7·4.8절과 같은 스크립트).

| 방법5 Fig.3 — AR, W1/W2 스윕 (rate=50%) | 방법5 Fig.5 — AR, penalty rate 스윕 (W1=W2=1) |
|---|---|
| ![방법5 Fig.3](results/fig3_pc_da_plus_rt_W1W2_AR.png) | ![방법5 Fig.5](results/fig5_pc_da_plus_rt_rate_AR.png) |

| 방법5 Fig.6 — MLR, W1/W2 스윕 (rate=50%) | 방법5 Fig.8 — MLR, penalty rate 스윕 (W1=W2=1) |
|---|---|
| ![방법5 Fig.6](results/fig6_pc_da_plus_rt_W1W2_MLR.png) | ![방법5 Fig.8](results/fig8_pc_da_plus_rt_rate_MLR.png) |

### 4.6 시도방법 6 결과 — Smart SPO+ loss

W1/W2=1/20에서 시작해 1/0까지 스윕했을 때, **Gap은 방법1과 정반대로 전부 양수(17~21%대)로 나왔다** — 기존 3항 MILP가 −30%대 음수였던 것과 비교하면 완전히 반전된 셈이다. SPO+ loss가 의사결정 손실(oracle과의 차이) 상한을 학습 목표에 직접 넣기 때문으로 보인다.

다만 **nRMSE가 53~69%대로, 여섯 방법 중 가장 크게 벌어졌다**(1/20에서 60.35%, 논문 34.89% 대비 +25.5%p). commit=1.0에 몰리는 비율도 기존 3항(100% 몰림)보다는 줄었지만 여전히 10~27% 수준이라, 3항 구조의 근본 문제(DA−PC>0)를 완전히 없애지는 못했다.

| 방법6 Fig.3 (z01) — SPO+, AR, rate=0.5, W1/W2 스윕 | 방법6 Fig.5 (z01) — SPO+, AR, W1/W2=1/1, 벌금율 스윕 |
|---|---|
| ![방법6 Fig.3](results/fig3_spo_z01.png) | ![방법6 Fig.5](results/fig5_spo_z01.png) |

| 방법6 Fig.6 (z01) — SPO+, MLR, rate=0.5, W1/W2 스윕 | 방법6 Fig.8 (z01) — SPO+, MLR, W1/W2=1/1, 벌금율 스윕 |
|---|---|
| ![방법6 Fig.6](results/fig6_spo_z01.png) | ![방법6 Fig.8](results/fig8_spo_z01.png) |

> **주의(작성 당시, 지금은 해결됨)**: 처음 이 절을 쓸 때 첨부했던 z01 그래프는 nRMSE가 245~250%대에서 완전히 평평하게 고정돼 있고 AR 기준선과 제안모형 선이 거의 겹쳐 있었다 — z03·block18 기준으로 확인한 위 수치(53~69%대)와 스케일이 맞지 않았다. 원인은 3.5절의 LinearConstraint 버그였고, 4.7절에서 재검증을 마쳤다. **위에 삽입된 그림은 버그를 고친 스크립트로 다시 뽑은 최신 버전**이라 더 이상 이 문제가 없다.

위는 SPO+ loss만 단독으로 적용한 버전이고, 아래는 여기에 4항식(방법2)까지 같이 결합한 버전(`integrated_spo_plus_4term_fig3568_AR_MLR.py`, z01/블록21)이다.

| 방법6+2 Fig.3 — SPO+×4항식, AR, W1/W2 스윕 (rate=50%) | 방법6+2 Fig.5 — SPO+×4항식, AR, penalty rate 스윕 (W1=W2=1) |
|---|---|
| ![방법6+2 Fig.3](results/fig3_spo_plus_4term_W1W2_AR.png) | ![방법6+2 Fig.5](results/fig5_spo_plus_4term_rate_AR.png) |

| 방법6+2 Fig.6 — SPO+×4항식, MLR, W1/W2 스윕 (rate=50%) | 방법6+2 Fig.8 — SPO+×4항식, MLR, penalty rate 스윕 (W1=W2=1) |
|---|---|
| ![방법6+2 Fig.6](results/fig6_spo_plus_4term_W1W2_MLR.png) | ![방법6+2 Fig.8](results/fig8_spo_plus_4term_rate_MLR.png) |

### 4.7 버그 수정 후 재검증 — 5가지 시도방법 재실행 결과

3.5절에서 찾은 LinearConstraint 버그를 고친 뒤, 영향받은 통합 스크립트를 전부 다시 돌려서 Fig.3(W1/W2 스윕) 데이터를 새로 뽑았다. 아래는 W1/W2=1/20 지점(논문 Table 3 비교 지점) 기준 수정 전/후 비교다.

| 방법 | 수정 전 (버그 있음) | 수정 후 |
|---|---|---|
| 방법1 통합판 (z01) | 전 구간 nRMSE 247.06% / Gap 4.05%로 고정 | 1/20: 48.55%/44.88% → 정상 반응. 다만 W1 비중이 2/1을 넘어가면 다시 246.8%/4.99%로 포화 — 3.5절에서 설명한 "이론적으로 예상된 고-W1 영역"과 일치 |
| 방법1 통합판 (z03) | 전 구간 116.45%/4.62%로 고정 | 1/20: 36.35%/24.75% → 정상 반응. 5/1부터 116.45%/4.62%로 포화 |
| 방법5 통합판 (z03, PC=rate·(DA+RT)) | 전 구간 116.45%/10.23%로 고정 | 1/20~1/1: 36.09~37.98%/10.46~10.70%로 매끄럽게 변화, 2/1 이후에도 79.62%→100.29%로 계속 움직이며(=완전히 평평해지지 않음) 방법1과 달리 극단 포화가 나타나지 않았다 |
| 방법6 SPO+ (z01) | 전 구간 247.06%/92.72%로 고정 (4.6절의 "스케일 안 맞음" 경고 원인) | 1/20: 48.61%/16.34% → 논문(34.89%/13.91%)과 비슷한 수준까지 근접. W1 비중이 커질수록 67~116%/13~16% 선까지 완만히 벌어지는, 방법1보다는 온건한 곡선 |

**4.6절의 경고를 여기서 해소한다**: 당시 "z01 재구현 과정에서 문제가 있었을 가능성"이라고만 적어뒀던 방법6(SPO+)의 스케일 불일치는, LinearConstraint 부등식 버그가 원인이었다. 수정 후 방법6은 nRMSE·Gap 모두 논문에 훨씬 가까워졌고 Gap 부호도 여전히 양수로 유지된다 — 3.3절 결론("구현이 복잡한 것에 비해 근본 문제를 못 바꿨다")은 유지되지만, 그 판단의 근거였던 수치 자체는 이번 수정으로 갱신됐다.

한 가지 새로 확인된 점은 **방법1과 방법5의 차이**다. 방법1(PC=rate·max(DA,RT))은 버그를 고쳐도 W1 비중이 커지면 다시 극단으로 포화되는데, 방법5(PC=rate·(DA+RT))는 같은 구간에서도 계속 값이 움직인다. 이유는 2.3절 논리를 그대로 적용하면 된다 — 방법1은 RT가 DA보다 낮은 시간대에서는 여전히 PC<DA(벌금이 DA가격보다 작음)가 성립해 "약정을 늘릴수록 유리한" 구간이 남아있지만, 방법5는 PC=rate·(DA+RT)가 RT가 충분히 크면 DA를 넘어설 수 있어 이 구멍이 메워지기 때문으로 보인다.

### 4.8 2세대 통합 스크립트 — 6가지 방법을 AR+MLR 한 파일에서 Fig.3/5/6/8까지 직접 생성

4.1~4.7절의 그림·수치는 방법별로 따로 짠 스크립트(주로 AR 단독, 일부는 CodefromJiWon 폴더의 구버전)에서 나온 것이었다. 이후 6가지 방법 전부를 "AR+MLR 통합, W1/W2·penalty rate 스윕, Fig.3/5/6/8을 한 파일에서 바로 생성"하는 동일한 틀로 다시 짠 2세대 스크립트를 만들어 교차 검증했다. 아래 표는 각 스크립트를 그대로 실행해서 얻은 KPI 지점(W1=1, W2=20, penalty rate=50%; Zhang은 c=1.5) 결과다.

| 방법 | 스크립트 | 데이터 | AR nRMSE | AR Gap | MLR nRMSE | MLR Gap |
|---|---|---|---:|---:|---:|---:|
| 1 (논문 원식 3항) | `integrated_paper_base_AR_MLR.py` | z03/블록18 | 36.29% | -1.09% | 24.25% | 3.02% |
| 2 (4항식) | `integrated_4term_ar_mlr_z03_block18.py` | z03/블록18 | 35.69% | 14.54% | 24.07% | 11.49% |
| 3 (Zhang 비대칭, c=1.5) | `integrated_zhang_asym_fig3568_AR_MLR.py` | z03/블록18 | 35.68% | 14.19% | 24.07% | 11.36% |
| 4 (PC=rate·max(DA,RT)) | `integrated_pc_max_rt_dt_fig3568_AR_MLR.py` | z03/블록18 | 43.64% | 22.04% | 29.03% | 24.92% |
| 5 (PC=rate·(DA+RT)) | `integrated_pc_da_plus_rt_fig3568_AR_MLR.py` | z03/블록18 | 37.63% | 10.33% | 24.25% | 10.94% |
| 6+2 (SPO+ × 4항식 결합) | `integrated_spo_plus_4term_fig3568_AR_MLR.py` | z01/블록21 | 48.93% | 16.23% | 43.03% | 14.78% |
| 논문(Table 3/4, 참고) | — | — | 34.89% | 13.91% | 21.92% | 11.91% |
| baseline(같은 스크립트의 일반 회귀, z03/블록18) | — | — | 36.11% | 10.7~25.0%\* | 24.43% | 10.9~27.7%\* | 

\* baseline(AR/MLR을 그냥 bounded LAD로만 학습한 값)의 nRMSE는 PC 정의와 무관하게 항상 36.11%/24.43%로 같지만, Gap은 각 방법의 PC 정의로 채점하기 때문에 방법마다 다르게 나온다.

**방법1~6을 한 틀에서 다시 비교해도 3.1절 결론이 그대로 재확인됐다**: 4항식(방법2)과 Zhang 비대칭(방법3)이 논문에 가장 가깝고(nRMSE +0.8%p 안팎), PC=max(DA,RT)(방법4)가 nRMSE·Gap 둘 다 가장 크게 벌어졌다(+8.8%p, +8.1%p). 방법5(PC=DA+RT)는 nRMSE는 3위권이지만 Gap이 논문보다 오히려 3.6%p 작게 나왔다. 방법6+2(SPO+와 4항식을 합친 조합, z01/블록21 데이터라 직접 비교는 조심스럽다)는 nRMSE가 가장 크게 벌어졌는데, 이는 SPO+ loss 자체의 특성(4.6절)과 z01이라는 다른 블록의 조합 효과가 섞여 있어 보인다.

**AR — Fig.3 (W1/W2 스윕)**

| 방법1 (논문 원식) | 방법2 (4항식) |
|---|---|
| ![Fig.3 방법1](results/simulation_output/fig3_paper_W1W2_AR.png) | ![Fig.3 방법2](results/fig3_ar_4term_z03_block18.png) |

| 방법3 (Zhang) | 방법4 (PC=max) |
|---|---|
| ![Fig.3 방법3](results/fig3_zhang_asym_W1W2_AR.png) | ![Fig.3 방법4](results/fig3_pc_max_rt_dt_W1W2_AR.png) |

| 방법5 (PC=DA+RT) | 방법6+2 (SPO+×4항식) |
|---|---|
| ![Fig.3 방법5](results/fig3_pc_da_plus_rt_W1W2_AR.png) | ![Fig.3 방법6](results/fig3_spo_plus_4term_W1W2_AR.png) |

**AR — Fig.5 (penalty rate 스윕, W1=W2=1)**

| 방법1 | 방법2 |
|---|---|
| ![Fig.5 방법1](results/simulation_output/fig5_paper_rate_AR.png) | ![Fig.5 방법2](results/fig5_ar_4term_z03_block18.png) |

| 방법3 | 방법4 |
|---|---|
| ![Fig.5 방법3](results/fig5_zhang_asym_c_AR.png) | ![Fig.5 방법4](results/fig5_pc_max_rt_dt_rate_AR.png) |

| 방법5 | 방법6+2 |
|---|---|
| ![Fig.5 방법5](results/fig5_pc_da_plus_rt_rate_AR.png) | ![Fig.5 방법6](results/fig5_spo_plus_4term_rate_AR.png) |

**MLR — Fig.6 (W1/W2 스윕)**

| 방법1 | 방법2 |
|---|---|
| ![Fig.6 방법1](results/simulation_output/fig6_paper_W1W2_MLR.png) | ![Fig.6 방법2](results/fig6_mlr_4term_z03_block18.png) |

| 방법3 | 방법4 |
|---|---|
| ![Fig.6 방법3](results/fig6_zhang_asym_W1W2_MLR.png) | ![Fig.6 방법4](results/fig6_pc_max_rt_dt_W1W2_MLR.png) |

| 방법5 | 방법6+2 |
|---|---|
| ![Fig.6 방법5](results/fig6_pc_da_plus_rt_W1W2_MLR.png) | ![Fig.6 방법6](results/fig6_spo_plus_4term_W1W2_MLR.png) |

**MLR — Fig.8 (penalty rate 스윕, W1=W2=1)**

| 방법1 | 방법2 |
|---|---|
| ![Fig.8 방법1](results/simulation_output/fig8_paper_rate_MLR.png) | ![Fig.8 방법2](results/fig8_mlr_4term_z03_block18.png) |

| 방법3 | 방법4 |
|---|---|
| ![Fig.8 방법3](results/fig8_zhang_asym_c_MLR.png) | ![Fig.8 방법4](results/fig8_pc_max_rt_dt_rate_MLR.png) |

| 방법5 | 방법6+2 |
|---|---|
| ![Fig.8 방법5](results/fig8_pc_da_plus_rt_rate_MLR.png) | ![Fig.8 방법6](results/fig8_spo_plus_4term_rate_MLR.png) |

*(그림 범례의 한글이 네모(□)로 깨져 보이는 건 이 실행 환경에 한글 폰트가 없어서다 — 축 제목 등 영문은 정상 표시되고, 데이터 자체에는 영향 없다.)*

---

## 5. 결론

> **종합**: AR·MLR, W1/W2 스윕·벌금율(C) 스윕을 다 합쳐 12개 그래프를 놓고 보면, 원본(3항)은 예외 없이 어느 지점부터 값이 튀어 고정되는 포화가 나타나고 기본·제안 모형의 gap이 서로 반대 방향으로 움직이거나 거의 안 갈라지는 반면, 4항·Zhang 두 방식은 포화 없이 완만한 곡선을 그리면서 기본 모형의 gap이 계속 벌어지는(또는 U자로 크게 출렁이는) 동안 제안 모형의 gap은 상대적으로 좁은 범위 안에서 안정적으로 유지되는 모습이 공통적으로 확인됐다. 다만 여전히 nRMSE·gap의 절대적인 수치는 논문보다 다소 높게 나오고 있어서, 형태(트레이드오프 방향)는 재현했지만 수치까지 완전히 일치시키지는 못한 상태다.

- **방법 1(논문 그대로)은 재현이 안 된다.** 3항 이익식 자체에 "약정을 무조건 늘리는 게 유리해지는" 구조적인 허점이 있어서, W1/W2를 어떻게 돌려도 논문 같은 완만한 곡선이 안 나온다. 이건 우리 코드가 잘못된 게 아니라, 논문 Eq. 1a를 문자 그대로 옮기면 생기는 문제로 보인다.
- **가장 잘 맞은 건 방법 2(4항식)와 방법 3(Zhang, c=1.5)이다.** 둘 다 W1=1,W2=20 지점에서 nRMSE는 논문보다 0.8%p, Gap은 0.3~0.6%p 정도밖에 안 벌어졌고, W1/W2를 전체로 스윕해도 곡선 모양이 논문과 잘 맞았다.
- **방법 4, 5는 벌금율(rate)을 잘 골라야 한다.** rate=0.5 한 지점만 보면 둘 다 논문과 차이가 크지만, 각자 잘 맞는 rate(방법4는 0.9, 방법5는 0.7)를 따로 찾으면 방법 2, 3과 비슷한 수준까지 가까워진다.
- **방법 6(SPO+)은 Gap의 부호 문제(음수→양수)는 고쳤지만 nRMSE를 더 벌려놓는 절충이었다.** 다만 4.7절에서 밝혔듯, 이 판단의 근거가 됐던 초기 수치에는 LinearConstraint 버그가 섞여 있었고, 수정 후에는 논문에 훨씬 가까운 수준으로 나온다.

**정리하면**: 3항 이익식을 문자 그대로 쓰는 한 재현은 어렵고, 부족분에 대한 값을 이익식에 제대로 반영해야(4항이든, Zhang 비대칭이든) 논문 결과가 재현된다.

> **추가로 확인한 것(3.5·4.7절)**: 위 분석과는 별개로, 통합 스크립트(방법1·5·6)에서 scipy `LinearConstraint`를 등식이 아니라 부등식으로 새게 만드는 코드 버그를 찾아 고쳤다. 이 버그는 Fig.3 전 구간을 가짜로 평평하게 만들었던 것으로, 수정 후에는 W1/W2가 낮은 구간에서 정상적으로 반응하고, 방법1처럼 이론이 실제로 예측한 고-W1 구간에서만 진짜로 포화된다는 걸 확인했다. "3항 이익식은 재현이 안 된다"는 결론 자체는 그대로 유지되지만, 그 원인이 순전히 수식의 구조적 한계였는지 코드 버그가 섞여 있었는지를 이번에 더 명확하게 분리해낼 수 있었고, 특히 방법6(SPO+)에 대한 평가는 이번 수정으로 실질적으로 바뀌었다.

### 원본 결과 문서(APEN 결과, z03 block 18)의 결론

부록에 그대로 첨부한 원본 문서는 같은 재현 작업을 오라클 보정(음수 Gap 문제 해결), Gurobi solver 전환, 3가지 이익식(원본 3항·4항·Zhang)을 AR·MLR 양쪽에서 Fig.3/5/6/8 12개 그래프로 비교하는 방식으로 정리했다. 그 결론을 요약하면:

- 오라클을 {0, 실제발전량} 2후보에서 {0, 실제발전량, 설비최대} 3후보로 고쳐서, 실현이익이 오라클을 넘어서면서 Gap이 음수로 나오던 문제를 해결했다.
- 벌금율이 100%보다 낮을 때는 이익식 구조상 약정을 계속 늘리는 쪽이 항상 유리해서, W1 비중이 커질수록 학습이 한쪽 극단(코너해)으로 쏠리는 현상을 확인했다 — 방법 1에서 본 문제와 같은 이야기다.
- 4항 방식과 Zhang 비대칭 방식 둘 다 이 문제를 완화했고, AR·MLR 두 모형·Fig.3/5/6/8 전 구간에서 "기본 모형은 벌금이 강해질수록 계속 나빠지고 제안 모형은 그 영향을 흡수하며 완만하게 유지된다"는 논문과 같은 방향의 트레이드오프를 재현했다.
- 다만 nRMSE·Gap의 절대적인 수치는 논문과 완전히 일치하지는 않았다 — 형태(트레이드오프 방향)는 재현했지만 수치까지 맞추지는 못했다는 게 원본 문서의 결론이다.

---

## 6. 감상평

논문 하나를 그대로 따라서 코드로 옮기는 게 생각보다 훨씬 손이 많이 갔다. 논문 본문의 수식(Eq. 9a, Eq. 13)만 보고 짰을 때는 당연히 논문 결과가 나올 줄 알았는데, 실제로 돌려보니 계수가 한쪽으로 완전히 쏠려버리는 걸 보고 처음엔 내 코드가 어디 잘못됐나 한참 찾아봤다. 나중에 보니 논문 수식 자체에 안 적혀 있는 부분(W1/W2 스케일을 어떻게 맞추는지, 오라클을 몇 개로 볼지, 부족분 비용을 어떻게 계산하는지)이 여러 군데 있었고, 그 빈틈을 어떻게 메우느냐에 따라 결과가 완전히 달라졌다.

여러 방법을 하나씩 시도하면서, "논문이 짧게 써놓은 한 줄짜리 식" 뒤에 얼마나 많은 선택지가 숨어 있는지를 체감했다. 4항식이나 Zhang 방식처럼 이익식 자체를 고쳐야 풀리는 문제였다는 걸 알기까지 여러 번 돌아갔는데, 돌아갈 때마다 "왜 안 되는지"를 하나씩 좁혀나가는 과정 자체가 재밌기도 하고 답답하기도 했다. 결과적으로 논문과 가까운 값을 찾아낸 방법(방법2, 방법3)을 봤을 때는 그동안의 시행착오가 헛되지 않았다는 느낌이 들었다.

한 가지 더 배운 건, "곡선이 그럴듯하게 보인다"는 것과 "코드가 맞다"는 건 별개라는 점이다. 방법1·5·6의 통합 스크립트에서 발견한 LinearConstraint 버그(3.5절)는 몇 달째 "3항 이익식의 구조적 한계"라고만 설명해오던 현상 중 일부가 사실은 scipy API를 잘못 쓴 아주 단순한 실수였다는 걸 보여줬다. `LinearConstraint(A, rhs)`라고만 쓰면 등식이 아니라 부등식이 된다는 건 문서를 안 읽으면 알기 어려운 함정이었고, 결과 그래프가 "이론과 대충 비슷해 보인다"고 해서 코드 감사를 건너뛰면 안 된다는 걸 다시 확인한 계기였다.
